# TASK-OSAN-PROJECT-001 Change 007 — 미리보기 편집·부분 등록·중복 확인

2026-09-09 사용자가 미리보기의 누락 필수값 직접 입력, 유효한 행만 등록, 동일 5항목(프로젝트명·코드·거래처·제품명·수량) 경고 후 등록을 요청했고 코드만 같고 나머지가 다른 경우도 허용한다고 확정했다. 구현·검증·기존 로컬 검수 서버 갱신까지 승인됐다. 원격 반영·공개배포는 포함하지 않는다.

기준은 `codex/osan-project-excel`의 `9a4f60e`, 소스는 `/private/tmp/emi-osan-progress-photo`다. Change006의 전체 파일 일괄 거부 및 코드 중복 금지는 이번 계약으로 대체한다. 원본 파일 안전 검사·서버 권한·등록 재시도·사업부 DB 분리는 유지한다.

- 미리보기 셀에서 입력을 고치고 즉시 필수값/형식 상태를 표시한다. 저장 직전 서버가 다시 검증한다.
- 유효행만 등록하고 미등록행을 남겨 보완 후 등록한다. 이미 등록한 행과 요청 결과 미확정 상태는 중복 제출되지 않도록 구분한다.
- 기존/파일 내 동일 프로젝트와 같은 코드 사용을 경고하고 명시 확인된 행만 허용한다. 확인 후 입력이 바뀌면 확인을 다시 받는다.
- 코드 유일성 변경은 추가 migration으로 오산에만 적용하고 기존 데이터·청주 계약을 보존한다.
- 관련 HTTP/DB/동시성/권한 검증과 PC·모바일 화면 확인, 독립 review 후 로컬 커밋한다. 검수 DB는 보존하고 원본 checkout WIP·로그인 임시 파일을 변경하지 않는다.

현재 상태: 구현·관련 검증·독립 review·로컬 검수 서버 갱신 완료. 사용자 최종 검수 대기. 원격 반영·배포 미실행.

Frontend 관련 테스트 25개·합성 브라우저 1개 PASS, TypeScript 포함 build·관련 ESLint PASS. 정상행만 등록→누락행 현장 편집→중복 확인→추가 등록, 이미 저장한 행 재전송 제외, 실패 응답의 동일 요청 재시도와 편집 잠금, 구버전 서버 등록 차단을 검증했다. PC1440·모바일390 화면을 직접 확인하고 표 결과 열 너비와 확인창 포커스를 보정했다. 기존 bundle 크기 안내 외 build 오류 없음. Backend 관련 고유 테스트 5개 PASS(파서, store, 실제 HTTP, 단건 생성, migration0090). 최신 소스 집중 4건 재실행도 PASS, Release build 경고·오류 0. 부분 등록, 잘못된 날짜·소수 수량의 행별 오류, 중복 확인, 경쟁 요청, rollback, 전체 선택 집합 replay, 권한 회수와 기존 단건 코드 예약을 확인했다.

독립 review의 P2 3건을 보정했다. 응답 유실 후 replay가403이어도 요청 잠금을 유지하고, 편집 날짜·수량을 행별로 검증하며, 선택한 전체 행과 내용을 요청 fingerprint에 포함한다. reviewer가 코드상 해소와 신규 P0~P2 없음으로 코드 GO를 확인했다. 실제 관련 분기 테스트도 통과했다.

2026-09-10 로컬 검수 handoff: 기존 `/private/tmp/emi-osan-preview-20260909/api.env`의 provider 경계를 유지하고 seed/startup migration을 false로 강제했다. migration-only 실행이 90개 migration 검증을 완료했고 API5096을 최신 Release로 재기동했다(session49535). Frontend5186은 같은 worktree 소스를 사용한다. 기존 API의 ledger mismatch는 갱신 후 해소됐고 실제 사업부 조회·프로젝트 목록·multipart 미리보기가 정상 동작했다. 갱신 전후 오산 9개 업무 테이블의 전체 행 해시·건수가 동일했다(사진 원본 포함). reset·seed·실제 프로젝트 추가 등록 없이 예제 정상1행/누락1행의 미리보기를 열었다. 검수 주소는 http://127.0.0.1:5186/projects 이다.

해당 change 소스·검증·문서만 로컬 커밋한다. 원본 checkout WIP와 미추적 login-review 파일은 제외한다. 다음은 사용자 검수이며 원격 반영·공개배포는 별도 승인 범위다.

2026-09-10 사용자 보정: 미리보기는 기본적으로 값만 텍스트로 표시하고 클릭한 셀 하나만 입력으로 전환한다. 빈 필수값은 ‘입력 필요’, 빈 선택값은 ‘—’로 표시한다. 바깥 클릭·Enter 후 텍스트로 복귀하며 기존 부분 등록·중복 확인·잠금은 보존한다. 관련 단위 테스트11개, TypeScript·ESLint·build PASS. 실제5186 화면에서 텍스트 표시와 클릭 후 단일 입력 전환을 직접 확인했다. 이번 합성 브라우저 재실행은 preview build의 API 주소와 기존 mock route 불일치로 초기 화면에서 실패하여 최신 E2E PASS로 기록하지 않는다. 이전 Change007 기능 검증 결과는 유지한다. 로컬 검수 서버 반영, 원격 반영·배포 없음.
