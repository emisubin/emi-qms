# TASK-UL891-SET-001 Change 008 사용자 검수 체크리스트

상태: `사용자 재검수 대기`

고정 검수 환경:

- Frontend: `http://127.0.0.1:42983`
- Backend: `http://127.0.0.1:41166`

검수 계정: 설계 담당자

## 자동 검증 완료

- [x] 프로젝트 상세 설계 조회 표에 규격 열이 없고 `Code · 패널명 · W × H × D`만 표시된다.
- [x] 별도 설계 수정 화면에 규격 입력칸이 없다.
- [x] `저장`은 현재 입력값 Draft 갱신 후 Publish를 순서대로 실행한다.
- [x] 규격이 없어도 패널명과 포장방식에 필요한 치수가 있으면 Publish된다.
- [x] Publish 뒤 패널이 `설계 입력 완료`로 계산되어 제조 준비 조건을 만족한다.
- [x] 기존 DB·API 호환 필드는 유지되고 기존 규격 데이터를 일괄 삭제하지 않는다.

## 사용자 재검수

- [ ] Draft인 세트의 패널명과 치수를 입력한다.
- [ ] `임시저장`을 누르지 않고 바로 `저장`을 한 번 누른다.
- [ ] “저장되었습니다” 안내와 `저장 완료` 상태가 표시되는지 확인한다.
- [ ] 프로젝트 상세 설계 탭으로 돌아와 저장한 패널명·치수가 조회되는지 확인한다.
- [ ] 패널이 설계 미완료로 남지 않고 후속 제조 준비가 가능한지 확인한다.

## 게시 경계

- local experiment runtime만 검수한다.
- 대표 repo·`main`·push·PR·merge는 포함하지 않는다.
- `main` merge 승인 상태: `0/3`.
