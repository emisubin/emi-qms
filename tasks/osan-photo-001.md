# TASK-OSAN-PHOTO-001 — 오산 진행 단계 완료 사진 등록·저장·조회

- 상태: `IMPLEMENTING` — 2026-09-09 권장안 구현 승인, 로컬 구현·검증 진행.
- 출처: 2026-09-09 사용자 사진 추가·DB 저장·조회 신설 요청과 “이것까지 포함해서 task 나누고 로드맵에 기입” 요청. 구현·운영 변경·원격 게시 승인은 이번 문서 작업과 구분한다.
- 목적 중복 확인: 현재 Task/로드맵 및 origin/main Task 경로에서 동일 오산 완료 사진 Task는 발견되지 않았다. 기존 첨부 기능은 재사용 후보이며 동일 오산 업무의 완료 근거가 아니다.
- 디자인·예외·시각 완료 기준: [UX-001](osan-ux-001.md). 이번 Task가 별도 UI를 창작하지 않는다.

## 범위와 선행조건

### 현재 확정 — 2026-09-09 구현 승인

사용자 “오케이 일단 권장안으로 구현하자”로 원본 PostgreSQL DB 보존, 선택 첨부 JPEG/PNG 최대 5장·장당 5MiB·요청 총15MiB, 자동 축소/HEIC 변환 없음, 일괄 사진 한 벌과 대상별 연결, 저장 후 조회까지를 확정했다. 설명·안내 사진은 기존 승인대로 영역 표시만 한다. 아래 과거의 제안/선택 대기 문구는 이 결정 전 이력이다. 같은 트랜잭션의 진행 완료와 필수 UI 연결·권한/실패/재시도 검증을 구현한다. 운영 DB/migration 적용·push·원격 병합·배포는 미포함이다.

원본 checkout의 다른 WIP를 보존하기 위해 `/private/tmp/emi-osan-progress-photo`, `codex/osan-progress-photo`에서 작업한다. `8fc3a44`의 현행 하네스/UX 문서와 최신 제품 `c3a3c79`를 로컬 통합한 기준은 `fdd049b`다. 충돌한 Roadmap과 frontend AGENTS는 현행 v2를 보존했고 최신 제품·배포 이력은 해당 Task들을 통해 보존했다. `git diff --cached --check`와 `scripts/test-change-scope.sh` PASS. 이 통합은 원격 main 병합이 아니다.

PROGRESS-001의 프로젝트·수량 대상·7단계 식별과 완료 계약을 선행조건으로 한다. 사진 저장 설계는 진행 구현과 함께 준비할 수 있지만 전체 완료 흐름 연결은 그 계약을 기준으로 한다.

1. 완료 팝업에서 모바일 카메라 촬영 또는 기존 이미지 선택, 저장 전 미리보기와 잘못 고른 사진 제거/재선택.
2. 사진 원본을 오산 PostgreSQL의 `bytea`로 영구 보존하고 프로젝트·대상·단계·등록자·등록 시각을 연결한다. 한 일괄 요청의 원본은 한 벌만 저장한다. 임시 미리보기 URL은 화면에서만 사용하고 DB 보존 근거로 삼지 않는다.
3. 완료 후 해당 대상·단계의 사진, 실제 작업자와 완료 일시 조회. 재접속·다른 기기에서도 동일 기록을 조회하고 다른 대상/단계의 사진이 섞이지 않게 한다.
4. 개별/전체 선택 완료와 연결한다. 일괄 적용 사진의 대상 범위를 저장 전 안내하고 사진과 각 대상의 연결을 보존한다.
5. 저장 중 중복 제출 방지, 실패 안내와 재시도. 선택한 사진 저장이 실패하면 성공/완료로 표시하지 않는다. 재시도·경쟁 요청이 사진이나 완료 이력을 중복 생성하지 않도록 한다.

## 적용 계약

- 직전 권장안을 수용한 사용자 응답에 따라 사진은 선택 첨부, 여러 장 허용, 일괄 선택 시 동일 사진 적용 안내를 기본안으로 한다. 저장 후 수정·삭제 및 완료 취소/재개는 첫 범위에 포함하지 않는다. 저장 전 재선택과 저장 후 기록 정정은 구분한다.
- JPEG/PNG·최대 5장·장당 5MiB·총15MiB·원본 무변환을 적용한다. 완료와 사진 저장이 같은 DB 트랜잭션에서 실패하거나 함께 확정되므로 미연결 외부 파일 정리 경로를 만들지 않는다. 운영 DB·백업 용량 확인은 배포 준비에서 별도로 수행한다.
- 단계 설명용 안내 사진은 PROGRESS-001 소유이며 이 Task의 완료 증빙과 혼합하지 않는다.
- 프로젝트 실데이터, Directory/청주/오산 DB 경계와 기존 서버 권한을 보존한다. 사진 조회/등록에도 사업부·프로젝트 권한을 검사하고 공개 URL로 우회 노출하지 않는다.
- 포장 선행조건과 마지막 대상 포장 시 프로젝트 자동 완료를 보존한다. 사진 처리 중 오류가 불완전한 완료 상태를 만들지 않도록 진행 Task와 저장 경계를 함께 설계한다.

## 완료 조건·검증

- 촬영/첨부 → 미리보기 → 저장 → 완료 화면 → 재접속 조회를 모바일에서 실제 확인한다. 기기 카메라 미검증은 별도 표시한다.
- 사진 없는 완료와 여러 사진 첨부, 잘못된 형식/크기, 권한 거부, 타 사업부 접근, 저장 실패·재시도·중복 요청, 일괄 대상 연결을 직접 대응하는 테스트로 확인한다.
- 사진 저장 구조가 확정되면 전용 disposable DB/storage에서 migration·기존 데이터 보존 및 조회를 검증한다. 운영 DB나 실제 provider를 일반 테스트에 사용하지 않는다.
- 지정 Figma 팝업·선택창·완료 후 화면과 동일 조건에서 육안 비교한다. 제품 구현 후 필요한 독립 검토를 수행하고 증거는 기존 완료 정책을 따른다.
- 전체 회귀는 VALIDATION-001의 사용자 검수 후 최종 후보 실행에 합친다. 개별 기능 검증을 생략하는 뜻이 아니다.

최초 문서 등록 당시에는 제품 코드·DB·runtime을 변경하지 않았다. 이후 구현 승인과 현재 결과는 위 승인 절 및 아래 구현 기록을 따른다.

## 2026-09-09 UX 기준 확정 중 저장 설계 제안 — 결정 전 이력

상태: `DESIGN_PROPOSED` — 아래는 최신 main 코드에 근거한 제안이며 사용자 확정 전이다. 신규 DB schema·운영 용량·저장 정책을 이미 승인받은 것으로 취급하지 않는다.

### 확인한 재사용 근거

기준 `origin/main c3a3c79374babc840dca054bd1a05237a2c49685`에서 별도 읽기 전용 조사를 수행하고 주요 코드·schema를 직접 대조했다.

- `database/migrations/0032_iqc_digital_reports.sql:139`: IQC 사진 원본은 `content bytea`이며 MIME·크기·SHA-256·작성자·일시를 함께 저장한다. `0036_logistics_execution.sql:99`의 물류 증빙도 DB bytes 방식이다. 기존 migration은 수정하지 않는다.
- `backend/src/Emi.Qms.Api/Materials/IqcReportStore.cs:17`: 기존 제한은 5장·장당 5MiB·총 15MiB이며 schema의 지원 형식은 JPEG/PNG다. 오산 제한으로 자동 확정하지 않는다.
- `frontend/src/IqcReportWorkspace.tsx:411`: 촬영 input 패턴은 재사용 후보다. 기기별 카메라·방향·형식 지원을 검증한 근거로 대체하지 않는다.
- `backend/src/Emi.Qms.Api/Manufacturing/ManufacturingStore.cs:743` 이후: operation ID·payload fingerprint·replay 경계는 진행 완료와 사진 통합 시 보존할 기존 구조다.
- 현재 검색 범위에서 준비된 별도 파일 저장소 adapter는 확인되지 않았다. 최신 배포 기록은 DB 구성을 설명하지만 실제 운영 용량·백업 수용성은 이번에 관측하지 않았다.

### 권장안과 사용자 결정 대기

1. **원본 DB 보존:** 오산 DB 사진 테이블에 최종 업로드 bytes·MIME·크기·SHA-256·등록자·등록 시각을 저장한다. 프로젝트·대상·단계의 완료 기록에는 별도 연결을 둔다. 일괄 완료 사진은 bytes 한 벌에 대상별 연결을 생성해 대상 수만큼 원본을 복제하지 않는다. DB와 백업 크기는 사진 용량만큼 증가한다.
2. **제한 후보:** JPEG/PNG 5장, 장당 5MiB, 한 완료 요청 총 15MiB. 자동 축소는 하지 않는 안을 제안했다. HEIC 변환·압축은 원본 보존 의미와 기기 지원을 바꾸므로 별도 결정한다. 지원하지 않는 파일은 성공처럼 처리하지 않고 이유와 재선택 방법을 알린다.
3. **대안:** 별도 private 파일 저장소를 선택하면 DB에 object key와 연결 정보를 두며 파일 자체의 영구 보존·권한·백업/복구가 추가로 필요하다. 임시 업로드와 DB 확정 사이의 실패·미연결 파일도 처리해야 한다. DB만 백업해서 원본 보존이 끝나는 구조가 아니다.

2026-09-09 사용자에게 위 DB 원본 보존·제한·자동 축소 없음 안과 HEIC 필요 여부를 질문했다. 사용자는 장단점을 포함한 여러 옵션을 요청했으며 저장 방식·제한은 아직 선택하지 않았다.

비교할 선택지는 A) DB에 업로드 원본 보존, B) 정해진 규격으로 변환/축소한 이미지를 DB에 보존, C) private 파일 저장소에 이미지와 DB에 연결 기록 보존이다. B는 저장된 이미지가 업로드 원본과 달라지며 HEIC 지원은 실제 decoder·기기 검증이 선행돼야 한다. C도 원본/최적화 보존 정책은 별도로 선택해야 한다. DB 원본 방식이 현재 코드와 가장 가까운 권장안이나 신규 정책으로 확정하지 않는다.

### 진행 완료와의 연결 초안

- 카메라·앨범 선택 → 미리보기·재선택 → 선택 대상과 동일 사진 적용 범위 안내 → 최종 저장 요청 → 완료 기록·사진 조회. 사진을 고르지 않은 완료도 허용하는 기존 계획 기본안을 보존한다.
- Figma `53:54`의 회색 `촬영`은 카메라 입력, 회색 `업로드`는 기존 파일 선택, 하단 빨간 `업로드`는 최종 제출로 연결하는 안이다. 버튼의 원본 문구를 임의 변경하지 않는다. 사진 선택 없이도 진행 완료 가능한 안내와 여러 장/실패 상태의 세부 배치는 아직 Figma 보완 설계 대상이다.
- DB 방식 채택 시 사진 저장·대상별 완료·마지막 포장에 따른 프로젝트 완료·audit/outbox를 동일 transaction에 넣는다. 어느 대상이든 권한·선행조건·version 검증에 실패하면 전체 rollback한다.
- 같은 operation ID와 같은 대상/단계/사진 hash 목록은 기존 성공 결과를 반환하고, 같은 ID의 다른 payload는 충돌로 처리한다. 사진이나 대상을 바꾸면 새 operation ID를 사용한다. 응답 유실 시 기존 ID 재시도 또는 재조회로 실제 결과를 확인한다.
- 선택 묶음의 사진·작업자·일시가 다르면 하나의 대표 기록처럼 표시하지 않는다. 대상별 완료 기록을 조회하고, 같은 사진을 공유하는 경우 적용 대상 연결을 유지한다. 구체적인 대상 전환·복수 사진 표시 위치는 원본 영역을 기준으로 별도 확정한다.
- 사진 bytes 조회에도 오산 사업부·프로젝트·대상 연결 권한을 서버에서 검사한다. 공개 URL을 제공하거나 다른 업무의 IQC 테이블/endpoint에 오산 사진을 억지로 넣지 않는다.

### 안내 자료와 구분

최신 `FormTemplateStore.cs:758`의 Manufacturing 분기는 Guidance를 null로 반환하며 `ManufacturingContracts.cs:86`의 단계 응답에도 안내 설명·이미지 필드가 없다. 따라서 기존 제조 양식에 재사용 가능한 안내 자료가 있다고 확정할 수 없다. 2026-09-09 사용자는 설명·안내 사진 없이 우선 진행하고 추후 자료를 제공하겠다고 확정했다. 해당 위치에는 설명/안내 사진이 들어갈 영역이라는 표시만 둔다. 실제 자료 제공을 현재 개발 선행조건으로 두지 않는다. 증빙 사진으로 안내 자료를 대신하거나 청주 자료를 자동 매핑하지 않는다.

이번 단계 검증은 코드·schema와 설계의 정적 대조다. 원본 자산·기기 촬영·실제 저장·재접속 조회·transaction 경쟁·운영 용량 검증은 미실행이다.

## 2026-09-09 로컬 구현과 직접 검증

- 추가 migration `0089_osan_progress_photo.sql`과 오산 전용 progress GET/start/completions/photo GET을 구현했다. 사진 bytes는 요청당 한 벌을 저장하고 대상별 단계 기록에 연결한다. 완료·프로젝트 자동 완료·audit/outbox는 같은 transaction으로 확정하며 operation ID와 payload 비교로 재전송을 처리한다.
- 서버가 사업부·프로젝트 권한과 대상 version/선행조건을 확인한다. 사진은 인증된 API에서 원본으로 반환하며 공개 URL을 만들지 않는다. JPEG/PNG 내용과 개수·크기를 검증하고 전체 HTTP 본문도 제한한다.
- 화면은 카메라/앨범 입력, 미리보기·재선택, 실패 후 같은 요청 재시도, 대상별 완료 기록·작업자·일시를 연결한다. 여러 대상의 사진은 한 대상씩 펼쳐 같은 원본을 대상 수만큼 동시에 내려받지 않게 했다. 권한 거부 재조회 시 이전 화면·사진을 제거한다.
- 프런트 직접 검증: OsanProjectRegistration/OsanProgressPage/osanProgress/BusinessUnitAccess/auth 5개 파일 72개 및 공통 API 17개, 합계 89개 테스트 PASS. TypeScript 검사·프로덕션 build PASS. Build에는 기존 큰 bundle 경고가 있으며 scoped ESLint는 오류 0, main.tsx의 기존 Fast Refresh 경고 1개다.
- 브라우저 합성 자료로 402px 사진 선택→미리보기→완료 기록과 실제 앱 390px 상세·팝업(278×329, 가로 넘침 없음), 모바일 로그인 정상·로딩을 직접 확인했다. 운영 DB·실제 인증·실제 기기 카메라·다른 기기 재접속을 확인한 것으로 확대하지 않는다.

프런트 독립 재검토(GPT-6 High)는 대량 사진 중복 다운로드 P2 해소, 신규 P0–P2 없음으로 판정했다. 서버 1차 직접 검증은 Osan/migration 8개, 실제 3DB HTTP 경계 1개, audit 12개로 총21개 PASS(0 skip)였다. 최종 보정 뒤 Osan/migration 8개 및 실제 HTTP 1개를 재실행해 PASS했고, 변경 영향이 없는 audit 12개 결과를 유지한다. 별도로 실행한 validator/catalog/null 3개는 위 8개에 포함되므로 중복 합산하지 않는다. 실제 HTTP 검사에는 일반 제조 역할·multipart·원본 다운로드·권한 철회가 포함된다. HTTP replay의 audit 건수를 별도 검증한 것으로 확대하지 않는다. 지정 Figma와 남은 차이는 [UX-001](osan-ux-001.md)과 [LOGIN Change 011](design-login-001-change-011.md)에 남겨 두며 100% 동일 구현 완료로 보고하지 않는다. 사용자 일괄 검수·최종 전체 회귀·원격 반영·운영 migration·배포는 미실행이다.

### 사용자 동작과 이후 검수

진행 현황에서 프로젝트·대상을 선택하고 단계를 이동해 바로 완료한다. 2026-09-09 검수 보정 승인으로 작업 시작과 개별 순서 제한을 제거한다. 개별·일괄 모두 1~6단계 중 원하는 단계를 완료한다. 포장은 앞 6단계가 완료돼야 한다. 완료 팝업에서 촬영은 사진을 추가하고 업로드는 앨범 선택으로 바꾸며, 하단 업로드가 단계 완료를 저장한다. 사진 없이도 저장할 수 있다. 저장 실패 시 같은 요청을 재시도하거나 새로고침해 상태를 확인한다.

사용자 일괄 검수에서는 실제 휴대전화 촬영·앨범·회전 사진, 제한 초과 안내, 재접속/다른 기기 원본 조회, 서로 다른 대상의 사진·작업자·시간, 마지막 포장 자동 완료와 Figma 남은 차이를 확인한다. 운영 반영 전 DB·백업 수용량과 승인된 migration/deploy 절차를 확인한다.

### 임시 자원 정리 상태

화면 검증용 Vite 5186/합성 API 5096을 종료하고 임시 preview 파일을 제거했다. 원본 checkout의 WIP와 기존 runtime은 보존했다. 백엔드 전용 `emi-osan-progress-test-20260909` 컨테이너 삭제는 자동 승인 정책에서 승인 필요/현재 Never로 거부돼 잔존한다. 같은 행동을 다른 경로로 우회하지 않았다. 승인 요청이 가능한 실행 환경에서 해당 전용 컨테이너만 정리해야 한다. 운영/UAT DB 변경은 없었다.

### 최종 이미지 검사 보정·독립 검토

JPEG는 Magick.NET-Q8-AnyCPU 14.17.1의 실제 디코딩과 경고 검사를 적용해 내용이 잘린 파일을 자동 복구해 성공 처리하지 않는다. PNG는 CRC·zlib·scanline/필터 검사와 Adam7 빈 패스 처리를 유지한다. 사진 원본은 재인코딩하지 않는다. 새 라이브러리의 Apache-2.0 및 포함 고지는 API의 THIRD-PARTY-NOTICES.txt와 빌드 출력에 보존했다. [공식 라이선스](https://github.com/dlemstra/Magick.NET/blob/main/License.txt)를 확인했다.

검사기의 자원 설정은 프로세스 시작 시 한 번만 적용한다: 메모리 128MiB, 단일 요청 64MiB, 디스크 256MiB, 스레드 1, 내부 이미지 목록 2. JPEG 검사는 직렬화하고 취소를 연결한다. 이 값은 서버 검사 자원 한도이며 사진 개수·원본 크기 계약이나 픽셀 치수 제한을 새로 확정한 것이 아니다. 자원 부족으로 검사할 수 없으면 완료하지 않고 다른 사진 선택을 안내한다.

독립 GPT-6 High 검토에서 P2a(이미지 검사), P2b(null 대상), P2c(복수 대상 사진 중복 다운로드)를 모두 해소했고 신규 P0–P2는 없었다. 최종 validator SHA-256은 `f560623ac8b6b31e283fdddbeba35bde5227d0d431ede975a14ac710d87d60b3`다. 작성자와 reviewer는 같은 소스를 동시 수정하지 않도록 최종 재검토 중 동결했다.

최종 validator와 동일한 파일의 독립 Linux probe를 Azure Linux 3.0.20260706, .NET 10.0.10, x64에서 실행했다. 정상 baseline/progressive JPEG 및 표준 1×1 Adam7 PNG는 원본 bytes 그대로 수용하고, 가짜 JPEG·entropy 제거 JPEG는 거부했다. 실행 이미지가 로컬 cached Azure pilot 이미지 `01a3a13af8fe`라는 한계를 남긴다. 현재 production base digest를 새로 받는 작업은 credential helper 대기로 중단했으므로 최종 운영 배포 검증으로 대체하지 않는다. 이 probe는 네트워크·DB 없이 읽기 전용 파일로 실행했고 자체 컨테이너는 자동 종료·제거됐다.

기능 코드와 직접 검증·독립 검토를 마친 부분을 scoped local commit한다. Figma 전체 동일성, 실제 기기 촬영·재접속 사용자 검수와 최종 회귀는 여전히 남아 있어 Task 전체 완료 상태로 바꾸지 않는다.

## 2026-09-09 사용자 요청에 따른 로컬 검수 서버

사용자 “서버 열고 보여줘”와 검수 항목·다음 작업 안내 요청에 따라 제품 코드 `30e2965`의 격리 검수 서버를 열었다. UI는 `http://127.0.0.1:5186`, API는 `http://127.0.0.1:5096`이며 proxy `/health/ready`의 DB ready와 실제 브라우저 오산 진행 상세·콘솔 오류 없음을 확인했다. 계정은 검수용 `dev-admin`, 사업부는 오산이다. 소스는 `/private/tmp/emi-osan-progress-photo`; Frontend PID 52129/실행 세션 70857, Backend PID 53150/실행 세션 89339다. 실제 PID와 cwd를 확인했으며 다음 재개에서는 다시 관측한다.

기존 전용 테스트 컨테이너의 55489 포트에 `emi_qms_e2e_osan_preview_20260909_directory`, `_cheongju`, `_osan` DB를 새로 만들고 migration/runtime 역할 6개를 분리했다. 기존 DB를 reset/drop하지 않았다. 실제 알림·Teams·메일·Web Push·주기 worker와 삭제 worker는 비활성화했다. 운영 Azure·Persistent UAT는 변경하지 않았다. 이 DB는 로컬 샘플 검수용이며 운영 보존 환경이 아니다. 현재 사용자의 검수를 위해 프로세스와 샘플 상태를 유지한다.

[샘플 진행 상세](http://127.0.0.1:5186/progress?projectId=2c1a4880-9e5d-4db5-b26d-c113c057d7de)는 “오산 생산 진행·사진 검수 샘플”, 수량 대상 2개·단계 14개다. 최초 제공 상태는 두 대상 모두 시작 전이다. 이 주소는 현재 컴퓨터 전용이며 실제 휴대전화 촬영·다른 기기 접속은 별도 HTTPS 검수 환경에서 수행한다.

### 사용자가 순서대로 확인할 항목

아래 전체 시나리오는 아직 완료 기록이 없는 두 대상 기준이다. 현재 검수 샘플의 이미 완료된 단계는 재저장하지 않고 기록 조회로 확인한다. 전체 시나리오를 다시 실행하려면 별도 샘플 프로젝트를 만들고 기존 기록을 초기화하지 않는다. 완료 취소·저장 후 사진 수정/삭제는 첫 버전에 없다.

| 순서 | 조작 | 기대 결과 |
| --- | --- | --- |
| 1 | 패널 선택에서 전체 2개를 선택하고 입고검사 완료 버튼으로 팝업을 연 뒤 저장 없이 닫기 | 작업 시작 없이 완료 팝업이 열리고 두 대상 적용 안내가 보임 |
| 2 | 대상 1만 선택하고 입고검사 완료 팝업 열기. 먼저 제한 초과 파일/6장 선택을 시도한 뒤 정상 JPEG/PNG로 재선택 | 제한 초과는 안내와 함께 저장 차단; 정상 파일의 미리보기·제거·재선택 가능. 적용 한도는 5장, 장당 5MiB, 합계15MiB |
| 3 | 대상 1 입고검사를 사진 1장과 함께 저장 | 해당 대상·단계에 사진, 작업자, 완료 시각 표시. 새로고침 후에도 동일 기록 조회 |
| 4 | 대상 2로 바꾸고 입고검사를 사진 없이 완료 | 대상 1 사진이 섞이지 않음. 사진 없이 완료 가능하며 사진 없음 표시 |
| 5 | 두 대상을 전체 선택하고 배치검사를 남겨 둔 채 배선검사로 이동해 사진 2장을 첨부하여 일괄 완료 | 동일 사진의 선택 대상 적용 안내. 배선검사만 완료되고 배치검사는 미완료 유지. 대상별 기록을 유지하고 사진은 대상 하나씩 펼쳐 조회 |
| 6 | 배치검사가 미완료인 상태에서 대상 1만 선택해 8계통으로 이동; 이후 두 대상을 선택해 포장으로 이동 | 개별 8계통 완료 가능. 포장은 앞 6단계가 모두 끝나지 않아 차단 |
| 7 | 남은 배치검사·8계통·동작검사·출하검사를 단계별로 아직 미완료인 대상만 선택하여 완료 | 단계별 상태와 진행률이 반영되고 두 대상의 앞 6단계가 모두 끝나면 포장 가능 |
| 8 | 대상 1만 포장 완료하고 목록·상세 확인 | 대상 1은 완료되지만 대상 2가 남아 프로젝트 전체는 아직 완료되지 않음 |
| 9 | 대상 2 포장 완료 후 새로고침·목록 재조회 | 별도 프로젝트 완료 버튼 없이 프로젝트 전체 완료·100% 반영 |
| 10 | 완료한 대상·단계를 다시 선택해 사진과 기록 확인 | 원본 사진과 작업자·시각 유지, 저장 후 사진 수정/삭제·완료 취소·중단/펜딩 버튼 없음 |

파일 합계 한도는 장당 한도 이하인 파일 4개를 골라 합계가 15MiB를 넘는 경우로 별도 확인할 수 있다. 서버 단절·권한 우회·경쟁 요청은 이번 사용자 수동 체크와 분리해 기존 자동 검증 근거를 유지한다. 실제 기기 카메라·다른 기기 재조회 및 최종 Figma 동일성은 아직 확인하지 않았다.

### 다음 작업

이번 검수에서 발견한 저장·표시 결함을 우선 보정한다. 이어 UX-001 및 LOGIN Change 011의 남은 원본 자산·PC/모바일 로그인·진행 상세/팝업 Figma 차이를 해소하고, DASHBOARD-001의 지정 모바일 요약·목록·검색·필터·진행률·페이지 이동을 구현한다. 현재 진행 현황의 진입 목록은 기존 프로젝트 목록 재사용이며 지정 현황판 구현 완료가 아니다. 이후 VALIDATION-001에서 실기기·재접속을 포함한 일괄 검수, 보정 후 최종 전체 회귀 1회와 required CI를 진행한다. 원격 main 병합과 Azure 배포는 각각 명시 승인 이후다. 전체 순서의 소유자는 Roadmap이며 이번 기록은 현재 검수와 다음 미완료 범위를 연결한다.

## 2026-09-09 검수 요청 6건 보정과 서버 갱신

기준은 `d1f77f5` 이후 같은 branch의 승인 보정이다. 최신 업무 계약은 [PROGRESS-001](osan-progress-001.md) 최상단이 소유한다. 작업 시작 API·클라이언트를 제거하고 개별·일괄 모두 1~6단계를 바로 완료하도록 변경했다. 포장 선행조건과 마지막 대상 포장 시 자동 완료를 유지한다. 목록의 고정0%를 실제 완료 수로 바꾸고 목록·상세·진행 응답의 상태를 통일했다. 프로젝트 상세의 대상 행은 해당 targetId가 선택된 진행 상세로 이동한다. 촬영은 image/* 카메라 입력을 직접 열고 결과 원본을 즉시 첨부한다. 패널 선택은 재확인한 Figma 펼침 목록과 원본 화살표로 보정했다.

- Backend 실제 격리 DB 검증 22개 고유 PASS: 최종 OsanProjectRegistrationApiTests + Migration0089 9개, AuditInfrastructureTests 12개, ThreeDatabaseBoundary_EnforcesRoutingRolesWorkersAndPendingLogin 1개. 중간 endpoint+audit 실행의 중복을 제외했다. 최신 Debug 테스트 빌드와 API Release 빌드 모두 경고/오류0.
- 작성과 분리된 기존 GPT-6 reviewer를 재사용했다. P2-F1(과거 Start-only 대상과 프로젝트 표시 상태 불일치)을 발견하여 GET·화면만 완료 수로 투영하고 원본 이력을 보존했다. 실제 PG legacy fixture로 검증했고 재검토에서 해소/신규P0–P2없음. 이후 실제 UI에서 이름 클릭 전 null blur로 선택창이 닫히는 문제를 재현·보정하고 좁은 재검토에서도 신규P0–P2없음을 확인했다.
- 1440px PC와 402/390px 모바일에서 목록·상세·선택창·완료 전후·팝업·포장 차단을 직접 확인했다. 390px 가로 넘침0. 실제 검수 샘플은 목록과 상세 모두 진행 중·7%, 두 번째 대상 행을 누르면 두 번째 대상이 선택되고 시작 없이 배선검사 완료 버튼이 활성화된다. 앞6미완료 포장은 비활성화와 이유를 표시한다. 시각 검증 중 사용자 진행 단계는 추가 완료하지 않았다.
- 같은 frontend 5186/PID52129를 유지하고 최신 Release API 5096/PID63504/세션61519로 교체했다. seed·migration 자동 실행을 명시적으로 끄고 외부 provider/worker 비활성·dry-run을 유지했다. 프록시 health ready 확인. 재시작 전후 대상2/완료1/전체14/사진1 및 완료 일시·작업자·사진 메타데이터의 동일 canonical hash를 확인했다. 기존 검수 데이터를 reset하거나 운영/UAT를 변경하지 않았다.

실제 휴대전화의 카메라 전환·지원 포맷·다른 기기 재조회, 지정 Figma 전체 동일성, 사용자 최종 수락과 전체 회귀는 아직 남아 있다. 로컬 검수 환경은 현재 컴퓨터 전용이다. 원격 push·병합·Azure 운영 DB migration·배포는 수행하지 않았다. 다음 개발은 이번 수정 검수 후 기존 UX/LOGIN의 남은 Figma 차이와 DASHBOARD 범위로 이어간다.

최종 Frontend 검증은 32개 고유 PASS(OsanProgressPage14, osanProgress6, OsanProjectRegistration12)이며 typecheck·최신 build·변경 파일 lint가 통과했다. 마지막 선택창 테스트 실행 중 기존 직접 완료 테스트1개가 10초 시간초과로 실패했으나 빌드 완료 후 같은 파일을 코드 변경 없이 단독 재실행하여14/14 PASS했다. 기존 큰 bundle 경고는 남아 있다. 최신 서버 재접속 후 두 번째 대상 선택·완료 버튼과 콘솔 오류0을 확인했다. 완료 코드·문서만 로컬 커밋하며 사용자 최종 검수와 원격 반영·배포는 별도 상태다.

## 2026-09-09 사용자 검수 완료와 다음 작업 승인

사용자가 `2afbda6` 검수 서버를 확인한 뒤 “검수 완료 다음 작업 시작해”라고 응답했다. 직전 제공한 6건 보정의 사용자 검수 완료로 기록한다. 이 응답은 미실행인 원격 병합·운영 배포 승인으로 확대하지 않는다. 다음 작업은 실행 큐의 미완료 UX 원본 대조와 LOGIN Change 011 보정부터 이어간다. 실기기 촬영·다른 기기 조회와 지정 화면 전체의 동일성은 관측 근거가 별도로 없으므로 자동 완료 처리하지 않는다.
