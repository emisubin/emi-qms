# TASK-INTERIOR-BUSBAR-001 — 청주 인테리어 부스바

## 현재 범위·승인

2026-09-14 이카운트 범위 확장: 사용자는 프로젝트 등록 시 주문서 입력, 프로젝트 완료 시 판매 입력을 추가로 구현하도록 요청했다. 기존 단방향/외부 송신 제외 계약은 이 두 입력 기능에 한해 변경한다. 최신 사용자 정정에 따라 단가는 제품군별로만 관리하며 프로젝트별 변경·단가 snapshot을 두지 않는다. 프로젝트 금액 조회는 제품군의 현재 단가를 사용한다. 원화·부가세 별도 10%. 거래처는 엘에스일렉트릭(주), 출하창고는 청주캠퍼스로 고정한다. 고객 업무번호는 W/O No이며 PO No는 입력하지 않는다. 아래 Change 021이 현재 진행 상태를, Change 020이 단가 계약을, Change 019가 외부 API 확인 사항을 소유한다.

2026-09-09 사용자가 대화에서 확정한 전체 신설 계획의 구현을 명시 요청했다. 범위는 전용 메뉴/권한/기준정보 → 구매·입고·재고 → 생산계획·사진 자동완료 → 프로젝트·분할출하 → 독립 QR 게시 → 기본 통합 검수 → 마지막 이카운트 발주 조회 연동이다. 실제 자원 생성·운영 migration·provider 호출·외부 게시·push/PR/merge/배포는 이번 로컬 구현 승인과 별개다.

2026-09-10 최신 게시 승인: 사용자가 “승인. 어차피 애저 공개배포할 때부터는 새로 시작이라서 언제부터 적용하나 상관없어.”로 자동 게시 적용 범위를 승인했다. Azure 운영 시작 시 기존 게시 대기 제품과 이후 완료 제품을 모두 대상으로 하며 시점 제한/과거 제품 제외 기능은 추가하지 않는다. 공개 내용은 앞뒤 사진·제조일시·작업자명이고 제품번호는 제외한다. 이 범위의 외부 게시 승인은 재요청하지 않는다. 운영 배포 선행 검증·해당 배포 실행 승인과는 구분하며, “새로 시작”을 검수/운영 DB 삭제나 초기화 승인으로 해석하지 않는다. 현재 로컬 worker는 비활성이고 운영 관리 ID/RBAC 적용·실제 인증 검증은 아직 미실행이다.

## 작업 기준

- source `f5b6f71` (시작 시 origin/main), branch `codex/interior-busbar`, worktree `/private/tmp/emi-interior-busbar`.
- 원본 checkout과 타 작업 WIP/runtime 보존. 현재 v2 Root/Backend/Frontend/Scripts 정책 적용.
- root: 조정, 독립 게시 provider·테스트, 통합 검증, 마지막 이카운트 연동. backend 담당: schema/core API/store/권한/tests. frontend 담당: UI/client/tests. 서로 다른 파일 소유.

## 보존할 업무 계약

- 활성 청주 조회, 별도 담당자·총괄만 입력/사유 정정. 외주 작업자 명단은 로그인 계정과 분리.
- 제품군 공용 수량재고, 제품군 하나/프로젝트 등록 건, 제품별 사진·번호 분리. 프로젝트명 중복 허용; 실제 확인 없는 제품번호 출하 귀속 금지.
- 도급/사급은 자재 속성, 공통 프로젝트 코드 대상 발주 자재 전부 관리. 발주 수신은 재고 불변, 분할 입고 시 증가.
- 버전 BOM으로 생산완료 자재 자동 차감. 자재 음수 허용, 완제품 음수 금지. BOM 미설정 제품은 완료 금지.
- 앞/뒤 사진 등록: 직접 촬영·기존 이미지 허용, 두 번째 필수 사진의 서버 등록 시각(한국시간 표시)이 제조시간. 두 장이면 자동 완료; 실제 작업자명과 등록 관리자 구분. 1장/업로드실패는 미완료. 생산·BOM snapshot·완제품/자재 증감 원자적 1회.
- 일별 제품군 목표, 계획 없음/초과 생산 허용. 부분 출하 시 재고·요청 잔여 초과 금지, 누적 전량 시 완료. 납기 자동출하/예약 없음.
- 기초재고·정정은 이력 보존. 취소는 과거 BOM으로 복원. 사진 정정·QR재출력은 재고·최초 제조시간 불변.
- 외부 사이트는 제품번호·앞뒤 사진·제조일시·작업자만 정적 게시. PMS 호출/로그인/내부 사진주소 의존 없음. 생산완료와 게시 분리, 성공 전 QR출력 금지, 재시도·최신revision 보호, 취소 시 공개중지.
- 이카운트 수신은 마지막 구현; 공통 코드 단방향 발주조회만. 반복수신 중복방지, 입고보존, 변경충돌표시. 외부 실제 사양을 검증하지 않은 wire contract를 사실로 만들지 않음.
- 현장 품질검사는 현행 유지, PMS 판정/성적서·실물반품/재작업/원가정산 제외.

## 검증·완료 조건

격리 합성 DB의 migration fresh/existing, 실제 서버 권한/타사업부 거부, 재고 경합·idempotency·snapshot취소·부분출하, 두 사진의 서버시간과 자동완료를 검증한다. 외부 provider는 fake/local로 독립 HTML·failure/retry/latest revision 확인. UI는 desktop/390px을 직접 확인한다. 권한/DB/동시성/공개 경계는 작성과 분리된 독립 검토 필요. 사용자 검수 뒤 최종 후보 전체 회귀 책임 실행 1회; required CI 별도 유지.

## 구현 결과·현재 상태

| 범위 | 현재 결과 |
| --- | --- |
| Task 1 전용 메뉴·권한·기준정보 | 구현 및 관련 자동 검증 완료. 청주 전용 메뉴/서버 권한, 담당자 role, 제품군·자재·외주 작업자·공통 코드·버전 BOM |
| Task 2 구매·입고·재고 | 구현 및 관련 자동 검증 완료. 직접/엑셀 발주, 분할 입고, 기초재고·보정·원장 역분개 |
| Task 3 생산·사진 | 구현 및 관련 자동 검증 완료. 날짜별 계획, 사진 두 장 자동 완료, 서버 제조시간, BOM snapshot, 작업자/등록 관리자 분리 |
| Task 4 프로젝트·출하 | 구현 및 관련 자동 검증 완료. 식별자 기반 엑셀 preview/apply, 제품군 공용 재고, 분할 출하·완료/취소 재개 |
| Task 5 독립 조회 사이트·QR | 실제 Azure 저장소·단일 실제 제품 공개 및 사용자 확인 완료(Change 014–016). 관리 ID 인증 코드·관련 테스트·독립 검토 완료(Change 017). 자동 게시 전체 대상 승인 완료. 운영 ID 권한/실인증 검증·활성화는 운영 배포 단계에 남음 |
| Task 6 기본 통합 검수 | 합성 DB·실제 HTTP 사진 API·mock 브라우저 흐름 검증 완료. 실제 기기 카메라·제품 매칭·프린터/QR 현장 검수는 대기 |
| Task 7 마지막 이카운트 연동 | **미구현·자료 대기.** 공식 세부 API 문서와 비식별 발주 응답 샘플을 요청했다. 실제 wire contract를 추측하여 연결하지 않았다 |

발주·자재의 이카운트 품목 코드와 공통 프로젝트 코드를 직접 입력할 수 있으며, 자동 연동 준비 미완료 상태를 화면에 표시한다. 공식 [이카운트 Open API 안내](https://www.ecount.com/kr/ecount/product/erp_open-api)에서 발주서 조회 제공과 로그인 후 세부 매뉴얼 확인을 검증했다(2026-09-09). Task 7은 발주/행의 안정적 식별자, 공통 프로젝트 필드, 수정·취소 표현, pagination/제한을 실제 문서·샘플과 대조한 뒤 진행한다. 완료한 내부 업무에 실제 연동 완료 표시를 붙이지 않는다.

## 검증 증거

- Backend Release build 및 `FullyQualifiedName~InteriorBusbar|FullyQualifiedName~AuditMutation`: **36개 통과, 실패/skip 0**. 전용 `busbar_test` DB(127.0.0.1:55490)의 매 테스트 고유 schema와 합성 계정만 사용했다.
- 추가 DB 경계 테스트 **2개 통과, 실패/skip 0**: 게시 worker에 오산/Directory identity DB를 연결하면 제품을 읽거나 외부 sink를 호출하기 전에 거부. 서버 관련 검증 총 **38개**(36개 실행 + 신규 경계 2개 실행).
- 실제 HTTP 검증: 익명 차단, 기존 부서 입력 거부, 담당자 입력/권한 회수 즉시 거부, trusted OSAN 컨텍스트의 GET/POST가 실제 사업부 middleware에서 403. 실제 multipart의 잘못된 이미지 거부 → 앞면 미완료 → 뒷면 자동 완료 → 제조시각/등록자/재고 → 중복 재고 방지 → 미게시 QR 409.
- 재고 검증: BOM 당시 소모량으로 취소, 자재 음수, BOM 누락 rollback, 30개 재고 동시 20개 출하 경쟁, 60개를 20개씩 3회 출하 및 취소 후 재개, Excel 전부 적용/전부 rollback, 계획 변경 전 값 보존.
- 게시 검증: 실패해도 생산 유지, 정정 후 최신 내용 재시도, 동일 토큰으로 취소 페이지, safe error, HTML escape, 한국시간, Azure conditional header/412, 지연된 과거 PUT보다 최신 PUT이 먼저 저장되면 과거 요청 거부.
- 독립 페이지: 생성 HTML을 `file://`로 별도 Chromium에서 열고 모든 HTTP(S)를 차단. 외부 요청 **0**, 두 이미지 로딩(각 320px) 성공, 제조시간 표시, 390px 가로 넘침 없음. 합성 증거 `/private/tmp/emi-busbar-public.html`, `/private/tmp/emi-busbar-public-390.png` 직접 확인. Azure 실제 게시/네트워크 장애 검수를 대신하지 않는다.
- Migration: 격리 컨테이너 `emi-busbar-test-20260909` 안의 별도 생성 DB에서 **fresh 90개 / 기존 0089→0090 업그레이드** 적용 통과. 기존 sentinel 보존, 담당자 role·설정 singleton 확인. 스크립트 `/private/tmp/emi-busbar-verify-migrations.py`; 검증용으로 생성한 두 DB만 종료 후 삭제했다.
- Frontend: `pnpm typecheck`, `pnpm build`, 변경 파일 ESLint 통과. 기존 bundle 크기 경고는 유지. Playwright 기본 5개 통과 후 리뷰 영향 신규 2개(BOM 최신값/미완료 정정·취소) 통과. PC/390px과 사진 완료 화면 직접 시각 확인. 합성 증거 `/private/tmp/emi-busbar-*.png`.
- 사용자 검수 후의 최종 후보 전체 회귀와 required CI는 아직 실행하지 않았다.

## 독립 검토

요청 모델 GPT-6 Astra/high, 도구의 실제 모델 별도 관측값은 `NOT_REPORTED`. 작성과 분리된 `/root/busbar_review`가 base `f5b6f71`부터 누적 WIP/신규 파일을 검토했다. P0/P1 없음. P2 BOM 편집값의 과거 버전 잔류, P2 Draft 정정·취소 미노출, P3 계획 감사 before 누락을 모두 보정했다. 수정분 재검토에서 모두 해소, 새 Finding 없음. 테스트 실행은 parent/구현자의 증거이며 reviewer가 별도 실행했다고 기록하지 않는다.

재검토 내용 hash: page `b6e29c1441c1b1fda687dfc3c2150bf6d2eabaed45160bb71119a8cff4b47d6c`, store `842e5bfecc5a30892cf2985ca4173f27912f574806f5ef77353aaef8e1f31705`, migration `8f07b337b72f9ed65283a81df11e51fd0e3d17e6a17f70e3fb17691e68f92245`.

## 사용·운영 준비

1. 승인된 대상에 migration 배포 후 관리 화면에서 별도 `인테리어 부스바 담당자` 역할을 부여한다. 기존 부서 역할만으로 입력 권한이 생기지 않는다.
2. 기준정보에서 제품군·자재 코드/단위/사급·도급·외주 작업자·공통 프로젝트 코드를 입력한다. 제품군당 1개 기준 소요량을 설정하고 도입 전 재고를 기초재고로 등록한다.
3. 생산·사진 화면에서 제품군/실제 작업자를 고르고 앞·뒤 사진을 등록한다. 두 번째 등록으로 완료되며 게시 상태가 성공하면 같은 제품번호의 QR을 출력한다. 납품 프로젝트에서 실제 출하 수량을 나눠 입력한다.
4. 잘못된 기록은 사유를 입력해 정정·취소한다. 완료 제품 사진 변경과 작업자 정정은 기존 제조시간을 보존하고 같은 공개 주소를 다시 게시한다.

외부 게시 기본값은 비활성이며 ReviewSafe에서는 켜지지 않는다. 실제 게시 승인을 받은 뒤 **PMS 사진 저장소와 별개인 Azure Storage 계정**의 정적 웹사이트를 준비한다. HTML에 정규화된 사진 두 장을 포함하여 한 blob의 조건부 교체로 사진/본문 버전을 일치시킨다. 공개 페이지/사진은 PMS 서버에 의존하지 않는다.

설정은 secret-safe 환경 구성으로 제공한다(실제 비밀/URL 값을 저장소에 기록하지 않음).

- `InteriorBusbar:Publication:Enabled`: 명시적 `true`로 게시 worker 실행.
- `InteriorBusbar:Publication:PublicBaseUrl`: 해당 계정의 `https://<account>.z<번호>.web.core.windows.net/` 기본 주소.
- `InteriorBusbar:Publication:BlobEndpoint`: 같은 계정의 `https://<account>.blob.core.windows.net/` 기본 주소.
- `InteriorBusbar:Publication:SasToken`: `$web` 게시 blob의 HEAD와 생성/교체에 필요한 읽기·생성·쓰기 범위의 비밀. 별도 secret 저장소로 공급하며 로그·Task에 남기지 않는다.

worker는 청주 DB만 명시적으로 선택하고 사업부 identity를 확인한다. 5초마다 대기 제품 한 건을 처리하며 실패 건은 내부에서 재요청한다. 현재는 최신 내용 보장을 위해 게시 중 동일 재고 mutation lock을 최대 게시 timeout 20초 동안 보유하므로 게시 장애 시 입력 지연이 생길 수 있다. 성공 전 QR 출력은 제공하지 않는다. 실제 Azure 설정/접근권한/만료/게시 실패 모니터링은 배포 전에 확인해야 한다.

## 남은 일·Git 상태

- Task 7: 세부 API 문서와 비식별 응답 샘플 확보 → 필드/식별자 확정 → 단방향 조회/중복·변경·취소 충돌/재시도 구현 → 합성 및 승인된 연동 환경 검증.
- 사용자 검수: 실제 촬영/사진 선택/제품 매칭/임시 스티커·라벨 출력 동선, 청주 담당 권한, 업무 수량 검수.
- 실제 Azure 독립 사이트 준비·게시, 공유 runtime handover, 운영 migration/배포는 미실행. 생성한 로컬 검증 자원 외 실제 provider/운영 데이터 변경 없음.
- branch `codex/interior-busbar`; 검증·독립 검토를 마친 내부 업무 변경은 로컬 commit으로 보존한다. push/PR/merge/운영 배포 미실행. 원본 checkout의 타 작업 WIP는 보존했다.

## 2026-09-09 로컬 검수 서버 공개

사용자가 “서버 열어서 보여줘”라고 요청하여 `8b15e48` source를 별도 검수 서버로 실행했다. Backend `http://127.0.0.1:5097`, Frontend `http://127.0.0.1:5197/interior-busbar`. DB는 이 작업 소유 컨테이너 `emi-busbar-test-20260909`의 신규 `busbar_review_20260909`이며 기존 테스트 DB 및 공유/운영 DB는 변경하지 않았다. 전체 migration 및 개발용 합성 계정 seed 후 readiness OK, 부스바 workspace 200/canWrite=true 확인. 부스바 기준정보/업무 데이터는 빈 상태다.

Codex 브라우저에서 개발 검수 계정 `dev-admin`을 선택하고 전용 화면 6개 탭·빈 현황 표시를 확인해 열어 두었다. 외부 게시·이카운트·알림 발송 worker는 비활성. Backend PID 43408, Frontend launcher PID 43409. 로그/PID 파일은 `/private/tmp/emi-busbar-review-{backend,frontend}.{log,pid}`. 다른 작업의 5096 서버는 보존했다. 사용자 검수 완료를 의미하지 않으며 원격/운영 배포는 없다.

## Change 002 — 계획에서 미완료 제품 선생성

사용자가 확인 후 수정 구현을 승인했다. 계획 수량만큼 작업자·사진·정식번호 없는 제품을 먼저 생성하고, 관리자가 기존 제품을 열어 실제 작업자와 앞·뒤 사진을 입력한다. 두 장 등록이 완료될 때 기존 제조시간·재고 원자처리와 제품번호 발급을 유지한다. 생성/촬영 UI를 분리하고 완료번호를 눈에 띄게 표시한다.

계획 재저장은 중복 생성하지 않는다. 수량 증가는 차이만 추가하고 감소는 작업자/사진 등록 전 미완료 제품만 취소 이력으로 보존한다. 작업을 시작한 제품 수 아래로 줄이거나 생성 제품의 제품군/계획일을 바꾸는 요청은 거부하여 기존 실물 식별을 보존한다. 기존 수동 생성/완료 제품은 임의로 계획에 연결하지 않는다. 0090은 이미 검수 DB에 적용되었으므로 additive 0091을 사용한다. 적용 대상은 현재 5097/5197의 별도 합성 검수 환경이며 외부 게시·이카운트는 계속 비활성이다.

상태: 구현·관련 검증 진행 중. 기존 사용자 검수 기록과 타 작업 서버는 보존.

Change 002 결과: 구현·관련 자동 검증·독립 검토 완료, 현재 검수 서버 반영. 계획 저장 시 수량별 Draft 생성, planId 필터/임시 순번, 사진 등록 시 작업자 선택, 두 장 완료 번호 강조와 화면 이동을 적용했다. Backend 관련 41개 통과(실패/skip 0), frontend 기본/신규 9개 및 리뷰 반례 2개 통과, typecheck/대상 ESLint 통과. 0091을 포함한 migration 91개 fresh/upgrade 적용과 기존 sentinel 보존을 확인했다. 독립 reviewer `/root/plan_review`(요청 Astra/high, 실제 모델 NOT_REPORTED)의 유일한 P2 R1(업로드 중 계획 변경 후 과거 조회가 덮음)은 최신 load ref로 보정하고 필터 직접 변경/탭 제품보기 두 반례를 통과했으며 재검토에서 해소 판정했다.

5097 Backend를 새 binary로 갱신(PID 47761, 개발 계정 재seed 비활성)하고 readiness와 실제 API를 확인했다. 기존 계획 30개에 미착수 제품 30개를 준비했고 기존 수동 제품 2개의 상태·번호·미연결 상태를 보존했다. 현재 5197에서 계획→제품 보기→작업자 선택·앞뒤 사진 화면을 직접 열어 확인했다. 제품 선택 시 사진 영역으로 이동하고 고정 헤더가 내용을 가리지 않게 위치를 조정했다. 실제 사진을 대신 등록하거나 기존 제품을 완료/취소하지 않았다. 사용자 최종 검수·원격 push/merge·실제 외부 게시/배포는 여전히 미실행이다.

## Change 003 — 제품군 중심 현황·출하·생산계획

2026-09-10 사용자 확인 및 구현 승인. 종합현황 전체합산 KPI 4개 제거, 제품군별 표·부족품목 유지. 프로젝트 행 전체/키보드로 상세 펼침, 내부 편집 버튼은 분리. 상세에 공용 재고·요청/출하/잔여·최대 출하 가능량 min(재고,잔여)와 날짜별 생산계획/완료/잔여 표시. 생산 사진 목록은 제품군·계획 날짜 범위·상태로 서버 페이지 전 필터. 생산계획은 제품군×주간 7일 표, 완료/계획 및 남은 수량 표시, 셀에서 해당 제품군/날짜 사진 목록으로 연결. 기존 입력/권한/재고/번호·사진 업무는 보존하고 schema 변경 없이 진행한다.

구현·관련 자동 검증·독립 검토 완료. 기존 5197/5097 별도 검수 서버에 반영했으며 사용자 입력과 실제 provider 비활성을 유지했다. DB schema 변경·seed·기존 업무 데이터 변경은 없다. 5097은 최종 Release binary로 갱신(PID 5686), 5197 실제 화면에서 제품군별 주간 표·계획 셀의 제품군/날짜 필터·프로젝트 일반 셀 클릭·상세 재고/잔여/최대 출하량을 확인했다.

검증: Backend 부스바/AuditMutation 관련 43개 통과(실패·skip 0, `/private/tmp/busbar-family-final-tests.log`), frontend 합성 UI 14개 통과, 대상 ESLint·TypeScript 통과. 1440px/390px 주간 표·프로젝트 상세를 구현자가 직접 확인했고 모바일은 표 내부 가로 스크롤을 제공한다. 페이지 전 서버 필터와 count 일치, 날짜 양끝 포함, 잘못된 날짜/상태 거부, 신규 제품 상세 GET 오산 거부 및 사진·번호 조회를 검증했다. 전체 회귀는 사용자 검수 후 최종 후보에서 수행한다.

독립 reviewer `/root/change003_review`(별도 맥락, 상속 모델·실제 모델 NOT_REPORTED)는 누적 Change 003 diff에서 P2 두 건을 발견했다. R1 미완료 필터에서 두 번째 사진 완료 후 번호가 사라지는 문제는 필터 목록과 별도 제품 상세 조회로 보정하고, 늦은 상세 응답 폐기 및 완료번호 유지 UI 반례를 통과했다. R2 새 제품군/날짜 경로에서 대기 순번 정렬을 잃는 문제는 계획일·제품군·대기 순번·ID 정렬과 페이지 경계 반례로 보정했다. 두 보정 재검토에서 해소 및 추가 finding 없음 판정. 기존 늦은 사진 업로드 중 필터/탭 변경 반례도 유지했다.

사용자 검수 항목: 주간 표에서 원하는 제품군/날짜 클릭 후 작업자·사진 입력, 미완료 필터에서도 두 장 등록 직후 제품번호 확인, 프로젝트 행 펼침 후 공용 재고·생산 예정과 현재 최대 출하량으로 수량 판단. 구현/자동 검증과 사용자 최종 수락은 별개이며 사용자 검수·원격 push/merge·운영 배포·실제 외부 게시 미실행.

## Change 004 — 날짜 선택형 생산계획 캘린더

2026-09-10 사용자 요청: 생산계획 탭에 캘린더를 넣고 날짜를 지정하여 제품군별 계획을 입력. 기준선 `a0525e3`, branch `codex/interior-busbar`. 월간 달력과 월 이동·제품군 필터, 날짜별 제품군 계획/완료 표시, 선택 날짜의 제품군별 신규 등록·기존 수정 동선으로 변경한다. 날짜·제품군이 미리 지정된 편집에서 수량을 저장한 뒤 캘린더를 유지하여 다른 제품군도 이어 입력할 수 있다. 제품 목록 이동은 별도 동작으로 제공한다. 기존 계획 저장에 따른 제품 생성·수량 보정·사진·번호·재고 및 서버 권한 계약은 그대로 사용한다.

프론트엔드 한정 구현·관련 검증 완료, 기존 5197 서버에 반영. 월간 캘린더 날짜 선택 시 상세로 포커스·스크롤 이동하고 제품군별 등록/수정에서 목표 수량에 바로 입력한다. 저장 후 같은 날짜 상세로 복귀하며 달력으로 돌아가기와 별도 제품 보기를 제공한다. 공용 편집기는 첫 활성 필드에 포커스를 맞추어 날짜·제품군이 고정된 새 동선을 지원한다. API/DB/schema/provider 변경은 없고 기존 검수 데이터도 수정하지 않았다.

검증: 합성 UI 15개 전체 통과, 포커스/CSS 보정 후 관련 2개, 최종 첫 활성 필드 보정 후 기존 출하·작업자 정정·캘린더 등록 3개 통과. 관련 ESLint·TypeScript 통과. 연말/연초 이동, 윤년 2월 29일/평년 28일, 날짜/제품군 고정 POST, 동일 ID 수정, 저장 후 달력·선택 날짜 유지, 기존 완료번호 및 필터 반례를 확인했다. 1440px/390px 최종 이미지(`/private/tmp/emi-busbar-month-calendar-1440.png`, `…-390.png`)를 구현자와 parent가 직접 확인했고 모바일은 달력/상세 표 내부 가로 스크롤을 사용한다. parent가 누적 diff와 실제 5197의 빈 날짜 선택→제품군 등록→날짜/제품군 자동 입력·수량 포커스→닫기·달력 복귀를 확인했다. 가역적 단일 FE 화면 변경으로 parent 직접 검토와 관련 검증을 적용했다.

사용자 검수: 원하는 월/날짜 선택→제품군 계획 등록 또는 수정→수량 저장→같은 날짜의 다른 제품군 입력. 사용자 최종 수락·원격 반영·운영 배포는 미실행이며 전체 회귀는 최종 검수 후 후보에서 수행한다.

## Change 005 — 계획 입력 팝업·달력 높이·프로젝트 현황

2026-09-10 사용자 요청, 기준선 `93979fc`. 캘린더 날짜 클릭 시 날짜별 제품군 계획을 팝업 안에서 등록·수정하며 저장 후 팝업을 유지한다. 달력의 모든 날짜 칸(서로 다른 주 포함)은 표시 중 가장 큰 내용에 맞춰 같은 높이를 유지한다. 종합 현황에 납품 잔여가 있는 진행 프로젝트를 납기 오름차순으로 표시한다. 기존 생산/재고/사진/번호/권한 계약을 유지하는 FE 한정 변경이다.

구현·관련 검증 완료, 기존 5197 서버에 반영. 공용 DsDialog 안에 날짜별 제품군 목록과 수량 편집을 넣고 저장 후 유지한다. 팝업은 키보드 포커스 순환·Escape 닫기·선택 날짜 포커스 복귀를 지원하며 저장 중 닫기와 제품 보기 이동을 막는다. 실패 시 팝업 안에서 오류와 입력값을 유지한다. CSS grid의 동일 비율 행으로 월 전체 날짜(월 바깥 칸 포함)를 가장 큰 내용과 같은 높이로 표시한다. 종합 현황의 진행 프로젝트는 잔여>0 조건과 납기/이름/ID 순으로 정렬하고 행에서 기존 프로젝트 상세로 연결한다.

검증: 관련 UI 18개 실행 중 17개 통과, 팝업 실패 후 닫기에서 이전 오류 알림이 포커스를 빼앗는 1개 실패를 발견해 팝업 닫기 시 피드백 정리로 보정했다. 해당 반례와 월 경계·등록/수정 2개 재실행 통과. 최종 ESLint·TypeScript·diff 검사 통과. 합성 5제품군 긴 이름을 다른 주 날짜에 배치해 PC1440/390에서 35칸 높이 차이 1px 미만과 제품군 필터 후 축소를 확인했다. 완료 프로젝트 제외·납기순·동일 납기 순서·프로젝트 상세 이동을 검증했다. 구현자와 parent가 popup 및 동일 높이 이미지(`/private/tmp/emi-busbar-plan-popup-{1440,390}.png`, `/private/tmp/emi-busbar-calendar-equal-{1440,390}.png`)를 직접 확인했다. parent는 누적 diff 검토와 실제 5197에서 종합현황 표·날짜 팝업·수량 입력·Escape 닫기를 확인했다. 실제 입력 저장은 하지 않았으며 사용자 검수 데이터와 API/DB/provider는 변경하지 않았다.

사용자 검수 대기: 날짜 클릭→팝업에서 제품군 선택·수량 입력, 계획이 많은 날짜와 다른 주의 높이 비교, 종합현황 프로젝트 납기순 확인. 원격 반영/운영 배포 미실행, 전체 회귀는 사용자 검수 후 최종 후보에서 수행한다.

## Change 006 — 사진 등록 팝업·조회 전용 현황·프로젝트 상태 필터

2026-09-10 사용자 확인 후 구현 승인, 기준선 `99bee06`. 생산·사진·QR 목록 맨 앞 열에 사진 등록/보기 버튼을 배치하고 팝업 안에서 작업자·앞뒤 사진과 완료 제품번호를 표시한다. 기존 정정·QR·취소 기능과 필터 밖 완료 제품 상세 보존은 유지한다. 종합 현황 프로젝트 행은 탭 이동 없는 조회 전용으로 바꾸고 납품 프로젝트는 전체/진행 중/완료 상태 필터를 기존 검색과 함께 적용한다.

FE 구현·관련 검증 완료, 기존 5197 서버에 반영. 공용 팝업 안에 작업자·앞뒤 사진·완료번호·정정·취소·QR 미리보기를 배치했다. 업로드 중 닫기 제한, Escape 및 원래 버튼 포커스 복귀를 지원하며 완료 후 필터에서 행이 사라진 경우 목록 제목으로 돌아간다. QR 미리보기는 현재 선택한 제품 상세의 버전·게시 상태를 검사하여 필터 목록 밖에서도 유지한다. 종합현황 행 action을 제거하고 프로젝트 상태 전체/진행 중/완료와 텍스트 검색을 AND 적용했다.

검증: 합성 UI 21개 전체 통과, 최종 포커스 보정 후 관련 3개와 늦은 상세 응답/포커스 2개 재검증 통과. ESLint 경고 없이·TypeScript·diff 검사 통과. 기존 업로드 지연 반례는 업로드 중 팝업 닫기 차단→완료 후 필터/계획 이동 동선으로 갱신했고, 이전 제품 GET 응답이 새 제품 팝업을 덮지 않는 별도 반례를 확인했다. Draft 필터 완료번호 유지, 첫 열 사진 버튼, 종합현황 클릭 무동작, 프로젝트 상태+검색을 검증했다. 구현자가 PC1440/390 사진 팝업을 직접 확인하고 parent는 실제5197 팝업 및 합성390 이미지(`/private/tmp/emi-busbar-photo-popup-390.png`)를 직접 확인했다. print media 라벨 표시 확인은 합성 이미지 fixture로 수행했으며 실제 QR 인쇄/스캔 검증은 하지 않았다. parent가 누적 diff 및 실제5197 overview조회전용·프로젝트상태선택·첫열사진버튼·worker/사진팝업·닫기를 확인했다. 실제 사진/작업자/출하 데이터를 변경하지 않았고 API/DB/provider 변경도 없다.

사용자 검수 대기: 사진 팝업 입력과 두 장 완료번호, 종합현황 클릭 무이동, 프로젝트 상태 필터 확인. 원격 반영/운영 배포 미실행, 전체 회귀는 사용자 검수 후 최종 후보에서 수행한다.

## Change 007 — 필터 배치 정리

2026-09-10 사용자 요청, 기준선 `8731ef6`. 필터의 라벨/입력 높이·간격·너비와 액션 버튼 정렬을 통일한다. 현재 5197 사진 화면에서 검색/제품군 사이에 이동 버튼이 끼고 버튼·입력 바닥선과 폭이 불규칙한 것을 직접 확인했다. 전용 필터 스타일로 프로젝트·계획·사진 및 관련 조회 도구를 정리하고 기존 모달/입력 폼/페이징과 업무 동작은 유지한다. PC/390px 줄바꿈 및 기존 관련 UI 검증을 수행한다. API/DB/provider와 실제 검수 데이터 변경은 없다.

구현·관련 검증 완료, 5197에 반영. 프로젝트·월간 계획·생산 사진·발주·소요량 제품군 선택에 전용 filterbar를 적용했다. 입력/선택/버튼 44px, 라벨 간격 6px, 항목 간격 12px로 맞추고 날짜쌍을 인접하게 유지했다. 이동/초기화는 필터 뒤 액션으로 분리했다. 1280/700px 기준으로 PC/중간 폭/모바일 배치를 조절한다. 모달·등록 폼·출하 액션·페이징 및 필터 동작은 보존했다.

관련 기존 UI 4개 통과, 최종 1440/1200/1101/390px × 6탭 시각 검증 및 page overflow 0 확인. ESLint·TypeScript·diff 검사 통과. 구현자는 각 폭 이미지를 직접 확인했고 parent는 실제 5197 전후와 최종 합성1440/390 사진필터 이미지(`/private/tmp/emi-busbar-{1440,390}-생산-사진-QR.png`)를 직접 확인했다. parent 누적 diff 검토 중 중간 폭의 최소 그리드 폭 위험을 찾아 1280px 전환으로 보정하고 해당 폭을 검증했다. 사용자 검수 대기, 원격 반영/운영 배포 미실행.

## Change 008 — 사진 진행 단계·자동 QR·일괄 라벨 인쇄

2026-09-10 사용자 확인 후 구현 승인, 기준선 `19b9c40`. 오늘은 파스텔 빨간색, 선택일은 구분 표시. 목록은 선택 체크박스와 사진등록/제품군/작업자/생산일시/생산 상태/제품번호/외부게시 순서로 표시한다. 사진등록 팝업은 작업자·앞뒤 사진 입력 중심, 기존 정정·취소 등은 별도 제품번호 관리에서 유지한다. 사진 없음/한 장/두 장에 따라 사진 등록 전/1차 사진 등록 완료/생산 완료를 표시하고 취소 상태는 사실대로 유지한다. 두 번째 필수 사진 등록 완료 시 기존 원자적 생산·재고·정식 번호 발급을 유지하면서 QR을 생성·보존한다. 완료 전에는 등록 대기 번호를 표시한다.

외부게시 최신 버전 완료 제품을 여러 개 선택해 QR+제품번호 라벨을 일괄 인쇄한다. 준비 실패 시 일부만 누락된 출력을 제공하지 않으며 출력 직전 상태·버전을 재확인한다. QR 목적은 출력이고 사진등록 팝업과 출력 동선을 분리한다. 공개주소가 미설정인 환경에서는 생산을 막거나 가짜주소를 발급하지 않고 QR 설정 대기로 표시한다. 실제 외부게시 성공 전 출력 금지 기존 계약은 사용자 확인대로 유지한다.

구현·관련 자동 검증·독립 검토 완료, 기존 5197/5097 검수 환경 반영. 두 번째 사진 완료 트랜잭션에서 공개주소가 설정된 경우 QR PNG와 고정 주소를 저장한다. 기존 완료 건은 출력 조회 시 한 번만 보완하며 ReviewSafe 조회는 DB 읽기 전용으로 유지한다. 공개주소 변경·게시 대기·오래된 게시 버전·취소 제품의 출력을 거부한다. 공개주소 미설정 시 생산/번호/재고는 완료되고 QR 설정 대기를 표시한다.

일괄 출력은 선택 제품 전체 준비와 최신 상태 재확인, 실제 라벨 이미지 디코딩을 마친 뒤에만 인쇄 영역을 활성화한다. 일반 미리보기는 브라우저 인쇄에서도 숨기고 afterprint/닫기/실패 시 인쇄 영역을 해제한다. 사진 입력 팝업과 제품 관리·일괄 인쇄 팝업을 분리하고 필터 밖 완료 제품번호 보존을 유지했다.

검증: backend 관련 45개 통과(실패·skip 0), ReviewSafe 보정 후 QR 관련 3개 통과(실제 HTTP 읽기 전용 반례 포함). PNG를 디코딩해 대상 주소를 확인했고 동시 보완·게시 최신성·설정 없음·취소 및 재고 불변을 검증했다. frontend 합성 UI 25개 통과, TypeScript·대상 ESLint·diff 검사 통과. 정확한 헤더/사진 단계, 여러 라벨, 일부 준비 실패, 준비 중 정정/취소, 인쇄 전 이미지 준비와 afterprint 해제를 포함한다. PC1440/390 팝업과 print media를 직접 확인했고 parent는 실제 5197 목록과 오늘 색상·두 QR 라벨 이미지를 확인했다. 실물 프린터 출력·스캔은 미실행이다.

0092 additive migration은 격리된 합성 DB 두 개에서 fresh 92개 전체 및 0091→0092 업그레이드를 검증했다. 기존 검수 DB에 적용 전후 기존 부스바 18개 테이블의 행 수·해시가 동일함을 확인했다. 최종 backend PID 19442, 재기동 시 자동 migration/seed 비활성, 외부 provider 비활성 유지. 현재 검수 환경에는 공개주소가 없어 실제 QR 생성·출력은 설정 대기이며 합성 공개주소로 자동 생성·일괄 인쇄를 검증했다.

독립 검토는 새 reviewer 생성이 agent 한도로 실패하여 기존 `/root/change003_review` 맥락을 재사용했다(실제 모델 NOT_REPORTED, 새 모델 override 없음). P2 R1 ReviewSafe GET의 legacy QR 쓰기는 읽기 전용 분기로, P2 R2 일반 브라우저 인쇄의 최신성 확인 우회는 검증 후 인쇄 영역 활성화로 보정했다. 최종 재검토에서 두 finding 해소 및 추가 P2 이상 없음 판정.

사용자 검수 대기: 오늘 표시, 사진 단계와 두 장 완료번호, 공개주소·게시가 준비된 환경에서 여러 제품 선택→선택 QR 인쇄→실물 라벨 부착. 전체 회귀는 사용자 검수 후 최종 후보에서 수행한다. 사용자 실제 사진/작업자/출하 기록을 대신 수정하지 않았고 원격 push/merge·실제 외부게시·자원 생성·운영 배포는 미실행이다.


## Change 009 — 조회 텍스트·재고 열·선택 및 납기 색상

2026-09-10 사용자 확인 후 구현 승인, 기준선 `240c94d`. 제품번호 관리 팝업과 진입 버튼을 제거하고 사진 보기만 유지한다. 프로젝트명은 일반 텍스트로 표시하고 행 펼침은 유지한다. 상세 최대 출하 가능 KPI 제거, 날짜별 계획에 현재 제품군 공용 재고 열 추가. 캘린더 선택은 파스텔 회색(오늘 빨강 유지), 종합현황 D-3부터 당일까지 진행 프로젝트는 노란색으로 표시한다. FE 한정 구현·관련 검증 및 PC/390px 시각 확인을 진행하며 API/DB/provider·사용자 데이터는 변경하지 않는다.


구현·관련 검증 완료, 기존 5197 Vite 검수 서버에 반영. 제거한 제품번호 관리 팝업의 정정/취소/게시 재시도 UI도 함께 제거했고 사진 입력·보기와 일괄 QR 출력은 유지했다. API/권한/DB 계약은 변경하지 않았다. 표 재고 열은 날짜별 예정량이 아닌 현재 제품군 공용 재고를 표시한다.

합성 UI 25개 실행에서 23개 통과 후, 프로젝트명 버튼을 기대하던 기존 검증과 공용 표 스타일에 가려진 노란색 우선순위를 보정하고 해당 2개 재검증 통과. TypeScript·대상 ESLint·diff 검사 통과. 초기 5197 테스트 시도는 합성 API 주소 설정 불일치를 확인해 중단했고 기존 5196 합성 전용 설정으로 검증했다. 초기 시도에서 실제 사진·출하 저장은 실행하지 않았다. PC1440/390 프로젝트 상세·납기 임박 행·캘린더 이미지를 직접 확인했다. 오늘 빨강, 선택일 회색, D-3/당일 포함 및 D-4/지난 납기 제외, 프로젝트 행/키보드 펼침과 제품번호 일반 텍스트를 검증했다. 사용자 검수 대기이며 전체 회귀·원격 반영·운영 배포는 미실행이다.


## Change 010 — 작업자 정정·게시 재시도 위치

2026-09-10 사용자 지정: 작업자 정정은 사진 보기, 생산 취소 UI는 제공하지 않음, 게시 재시도는 외부게시 열. 기존 서버 API를 재사용하고 사유·권한·제조시간 보존 계약을 유지한다. 실제 외부 자원 생성은 승인되지 않았으며 Azure 비용 추정만 수행한다.

구현·관련 UI 3개(정정 사유 저장/외부게시 열 재시도/읽기 전용/사진 팝업 경계) 및 TypeScript·ESLint 통과. PC1440/390 합성 팝업을 직접 확인했다. 5197에 반영, API/DB/실제 provider 변경 없음. 생산 취소 UI는 계속 없음. 사용자 검수·원격 반영·운영 배포 미실행.

비용 추정 근거(2026-09-10): Microsoft Retail Prices API 한국 중부 General Block Blob v2/Hot LRS 조회, GB·월 $0.02, 쓰기 1만회 $0.05, 읽기 1만회 $0.004. 공개 자료 3MB/제품, 월 조회수=월 생산량×5, 1달러=1,400원 계산용 가정, 세금/PMS내부비용/별도 CDN·도메인·로그 제외. 5년 누적 월1000개=180GB 저장·15GB월송신, 월5000개=900GB·75GB, 월10000개=1800GB·150GB. 인터넷 송신 무료100GB 가용/미가용 두 경우로 추정한다. 보관기간 자동 삭제 정책 확정이나 실제 자원 생성 승인은 아님.


## Change 011 — 외부 QR 페이지 디자인 시안

사용자 요청에 따라 공개 제품 페이지 1차 HTML 시안을 frontend/public/interior-busbar-preview.html로 작성했다. 제품번호·제조일/시간/작업자·앞뒤 사진, 모바일 세로/PC 두 열 구성. 가상 데이터와 명시된 SVG 샘플을 사용했으며 실제 게시 renderer/업무 데이터/외부 자원은 변경하지 않았다. 5197 별도 미리보기 주소 제공. 390/1440px 이미지 로딩·가로 넘침 0 확인 및 두 이미지를 직접 시각 검토했다. 디자인 사용자 검토 대기이며 승인 후 실제 공개 페이지 생성 코드에 적용할 수 있다.


## Change 012 — 공개 HTML 제품번호 제외

사용자가 프로젝트명 기반 번호·출하 시 제품 연결 제안을 철회하고 공개 HTML에서 제품번호만 제외하도록 요청했다. 기존 내부 제품번호 발급·QR 라벨·고정 주소·생산 완료 게시 시점과 수량 기반 출하를 유지한다. 디자인 시안에서 제품번호를 제조정보 제목으로 대체하고 번호 대조 안내를 제거했다. 실제 공개 HTML renderer에서도 제품번호를 제외한다. 외부 게시/자원 생성은 수행하지 않는다.

정상/중지 HTML 렌더링 2개 통과(skip 0), 제품번호 미포함·사진/제조정보 보존 확인. 390/1440 시안 직접 시각 확인 및 diff 검사 통과. 미리보기 반영 완료. 실제 게시 renderer source는 수정·검증했으며 backend runtime binary 갱신/외부 게시는 미실행(현재 게시 비활성). 사용자 디자인 검토 대기.


## Change 013 — 승인된 QR 디자인 실제 renderer 적용

사용자가 제품번호 제외 시안으로 진행 승인. EMI 헤더·제조정보 카드·앞뒤 사진 모바일 세로/PC 두 열을 실제 공개 HTML renderer에 적용한다. 샘플/디자인 시안 문구는 실제 게시에서 제외한다. 사진은 자르지 않고 원본 비율 유지, 제품번호 미표시·독립 데이터 이미지·CSP·이름 escaping·공개 중지 경계를 유지한다. 내부 번호/출하/API/DB 계약 및 외부 자원은 변경하지 않는다.

실제 renderer 합성 출력의 정상/중지 테스트 2개 통과(skip 0). 생성 HTML을 HTTP(S) 차단 후 390/1440px로 열어 사진 두 장 로딩·가로 넘침 0과 시각 배치를 직접 확인했다. 악성 형식 작업자명은 실행 없이 텍스트로 표시됨을 유지한다. 실제 게시 source 적용 완료, runtime binary 갱신·Azure 자원 생성/게시는 미실행이며 실제 연결 시 반영한다.


## Change 014 — Azure 공개 저장소 실제 연결

2026-09-10 사용자 “좋아. 시작해”로 Azure 연결 시작 승인. 기존 구독 Azure subscription 1의 PMS 그룹 emi-pms-pilot 안에 emipmsbusbarqr 신규 StorageV2/한국 중부/Standard_LRS/Hot 저장소 생성. HTTPS 전용/TLS1.2/Blob 익명접근 false, $web ACL off 유지. 기존 PMS 저장소·운영 앱·업무 DB 변경 없음. 기본 페이지는 QR 안내, 404는 일반 안내이며 제품 목록은 없음.

확정 공개주소: https://emipmsbusbarqr.z12.web.core.windows.net/ . Blob endpoint: https://emipmsbusbarqr.blob.core.windows.net/ . 별도 도메인/CDN 없음. 실제 자원은 유지 중이며 사용량 과금 대상이다. 합성 공개 검증 token a6f771f081f75edbeec65db2f1df069ee2025777d0380231053565544dd6e05b, 사진2개는 단색 합성 이미지, 작업자 연결검증(가상). 실제 업무 사진은 전송하지 않았다.

격리된 /private/tmp/emi-busbar-azure-probe에서 실제 renderer/sink를 호출했다. 최초게시→동일주소 정정→정보중지→합성자료복원 통과. HTTPS 익명webGET, UTF-8 HTML/no-store, 번호·내부링크 미포함, oldETag PUT412, 익명blobGET/list 거부, root목록없음/404, 출력QR PNG ZXing 해독 URL일치 확인. 실제 공개사이트 390/1440 이미지2개 로딩·overflow0·HTTP1/PMS0 확인 및 직접 시각검토. /private/tmp/emi-busbar-live-qr.png 와 /private/tmp/emi-busbar-azure-live-{390,1440}.png는 합성 증거.

최소권한 container SAS rcw/HTTPS/2026-09-12 02:42 UTC 만료, 저장소 밖 /private/tmp/emi-busbar-azure-private/publication.json 0600/부모0700으로 임시보관. 토큰·계정키는 로그/기록에 남기지 않는다. 자동게시 운영 활성화 전 지속 자격증명 관리/갱신을 준비해야 한다. SAS가 만료되어도 이미 게시한 공개페이지 조회에는 영향 없음.

독립 reviewer /root/azure_qr_review 새 맥락(요청 Astra/high, 실제 NOT_REPORTED) 코드·계획 검토, 차단finding없음. worker는 기존 Pending 전체를 게시하므로 로컬5097 게시 활성화 보류 및 최종 URL확인 뒤 QR주소 연결 지적을 반영했다. 사용자에게 기존 대기사진도 게시할지 비동기 확인 요청, 아직 답변 없음. 실제 검수 사진의 일괄 공개는 진행하지 않는다.

로컬5197/5097에 최신Release와 확정 PublicBaseUrl/BlobEndpoint 연결, Publication Enabled=false/SAS미주입/seed=false/migration=false 유지. 재기동 중 .NET 경로 오류로 한 차례 시작 실패 후 실제 /usr/local/share/dotnet/dotnet 경로로 복구, PID38316 ready200/workspace200 canWrite=true 제품65개 조회 확인. 기존 검수 DB 유지. production 배포/push/PR/merge 미실행. 남은 단계: 기존 대기 제품 공개 여부 응답→승인 범위의 자동게시 활성화와 실제 사진/라벨 현장검수, 이후 마지막 이카운트 연동. 현재는 공개사이트 실연결 검증 완료·자동게시 비활성이다.


## Change 015 — 실제 제품 한 건 공개 테스트 준비

사용자 “하나만 테스트로 하자”로 단일 테스트 지시. Pending Complete 중 최초 완료 제품 IB-00000001(id88a740bc-1bdd-4faf-ab09-d8fdca72e4db, revision2)을 선정. singleID FORUPDATE/sharedlock→실 renderer/sink→공개본문 해시→해당행만 게시상태 갱신→타제품/원장/사진 해시 보존→PMS QR 해독 검증 도구를 /private/tmp/emi-busbar-one-product에 준비했다. 독립 /root/azure_qr_review(기존 reviewer 맥락 재사용)는 차단finding없음, 외부게시 뒤 후속단계 실패 시 동일대상 상태확인 경계를 확인했다.

실제 실행 요청은 자동 승인 검토가 거부: 특정 실제 제품 및 정확한 사진·제조정보·작업자명 공개 payload의 명시 승인이 부족하다는 이유. 우회 실행하지 않았고 실제 제품은 게시하지 않았다. 5097 worker는 false 유지. 허용된 읽기 전용 DB/로컬 renderer로 /private/tmp/emi-busbar-local-preview/product.html(0600)을 준비, 해당 건 Pending 확인. 공개예정 HTML SHA256 4B5355208B518934EA779BC115FE6279DFC661BEF886199F825D1F4F0DA47A26. 이름·실사진은 Task에 복제하지 않았다. 127.0.0.1:56318/product.html 한 경로만 제공하는 로컬 미리보기(session37240)를 열어 사용자가 정확한 payload를 확인할 수 있도록 했다. 특정 제품·정확한 내용에 대한 공개 승인 대기. 다른 실제 제품 변경 및 외부 전송 없음.


## Change 016 — 승인된 실제 제품 한 건 게시

사용자가 정확한 로컬 미리보기·대상 IB-00000001·앞뒤 사진·제조일시·작업자명 공개 내용을 확인한 뒤 “굿! 승인”으로 명시 승인했다. 이전 자동 승인 거부 사유인 특정 대상/payload 승인을 충족한다. 실행 도구에 승인 id88a740bc-1bdd-4faf-ab09-d8fdca72e4db/rev2 및 HTML SHA256 4B5355208B518934EA779BC115FE6279DFC661BEF886199F825D1F4F0DA47A26 일치 조건을 추가했다. 다른 제품과 자동게시 활성화는 범위 밖이며 비활성을 유지한다.

게시 실행 성공. 승인된 HTML 해시 일치 확인 후 해당 1행만 Published/revision2 반영, 타제품·원장·사진 해시 불변 통과. 인증된 PMS QR GET 성공 및 ZXing 해독 URL 정확히 일치. 공개 URL https://emipmsbusbarqr.z12.web.core.windows.net/p/dd1b60c6a00a443ad50b162c703fe8284022316fbec649f902f312c8d3f94643.html . API 재조회 Complete/Published/revision=publishedRevision=2/qrReady=true, 다른 완료 Pending1건 유지, 자동worker=false 확인. 출력 QR /private/tmp/emi-busbar-one-product-qr.png. 실제 페이지 PC/390 이미지2개 정상/번호미표시/overflow0/HTTP1/PMS0 검증, 실사진 screenshot은 추가 보관하지 않음. 사용자 실제 휴대폰 스캔·물리 프린터 출력 검수 대기. 운영 앱 배포·다른 제품 게시 없음.


### 단일 제품 사용자 확인 완료

사용자가 서버/실제 공개 페이지 안내 후 “좋아 굿. 확인 완료”로 IB-00000001 단일 QR 게시·조회 결과를 수락했다. 해당 단일 테스트의 사용자 검수 완료로 기록한다. 물리 프린터 출력·실물 부착·스캔을 각각 수행했다고 추정하지 않으며, 전체 업무 검수/다른 제품 공개/자동게시 활성화/운영 배포 승인을 의미하지 않는다. 자동 게시 비활성과 다른 제품 대기 상태를 유지한다. 다음 미완료 범위는 지속 게시 자격증명·자동게시 적용 범위 확정, 마지막 이카운트 연동 자료 확보 및 구현, 최종 전체 검수·회귀/원격 반영·운영 배포다.


## Change 017 — 자동 게시 운영 준비

사용자 다음 작업 시작 요청. 현재 임시 SAS 만료(2026-09-12) 대신 Azure 기존 backend 사용자할당 관리 ID 인증을 코드로 준비한다. 실제 운영 앱의 UserAssigned identity 존재를 읽기 전용 확인했다. 인증 방식 명시 선택/혼합금지/고정 storage scope/기존 SAS 호환/ETag 경계 유지. 기존 대기 제품 포함 여부는 비동기 질문 대기이며 자동 게시 활성화·추가 실사진 공개·운영 앱 배포/RBAC 변경은 아직 실행하지 않는다.

구현: `AuthenticationMode` 기본값은 기존 `Sas`; 새 `ManagedIdentity` 모드는 유효한 `ManagedIdentityClientId`를 명시하고 SAS를 제거해야 한다. Azure.Identity 1.17.2(기존 전이 의존성과 같은 버전)를 직접 참조한다. 지정된 사용자할당 ID만 사용하고 개발자 계정/다른 자격증명으로 fallback하지 않는다. HEAD/조건부 PUT 양쪽에 Storage 범위 Bearer 인증을 적용하며 ReviewSafe/비활성 상태에서는 게시 요청하지 않는다. 제품번호·사진·생산·재고·게시 대상 선정 로직은 이번 변경에 포함하지 않는다.

운영 적용 준비값(아직 적용하지 않음):

| 설정 | 값 |
| --- | --- |
| `InteriorBusbar__Publication__Enabled` | 적용 범위·배포 검증 전 `false` 유지 |
| `InteriorBusbar__Publication__AuthenticationMode` | `ManagedIdentity` |
| `InteriorBusbar__Publication__ManagedIdentityClientId` | 배포 대상 backend에 실제 연결된 사용자할당 ID의 client ID |
| `InteriorBusbar__Publication__SasToken` | 제거 |
| `InteriorBusbar__Publication__PublicBaseUrl` | `https://emipmsbusbarqr.z12.web.core.windows.net/` |
| `InteriorBusbar__Publication__BlobEndpoint` | `https://emipmsbusbarqr.blob.core.windows.net/` |

실제 적용 시 해당 backend ID에 전용 저장소 `$web` 컨테이너 범위의 Blob 데이터 읽기/쓰기 권한을 부여하고, 승인된 제품 하나로 실제 ID 인증·게시를 검증한 뒤 승인된 대상 범위로 활성화한다. 현재 로컬 Testing runtime은 Azure 관리 ID를 사용하지 않으며 임시 SAS 자동 주입도 하지 않는다. 운영 backend 배포·DB migration·RBAC 변경은 각각 실제 승인 범위에서 진행한다. 향후 제품만 게시하도록 선택하면 기존 Pending 제외 기준을 먼저 구현해야 하며 현재 worker를 그대로 켜지 않는다.

검증: 격리 `busbar_test` DB와 합성 데이터, fake TokenCredential/HTTP를 사용한 관련 인증·게시 테스트 29/29 통과(건너뜀 0). 잘못된 인증 설정, 토큰 획득 실패 시 전송 없음, 비활성 시 인증/전송 없음, 새 파일/기존 파일 조건부 쓰기를 포함한다. 실제 Azure 관리 ID 인증 및 운영 배포 검증은 미실행이다.

독립 검토: 새 reviewer `managed_identity_review`(요청 Astra/high, 실제 모델 NOT_REPORTED)가 HEAD `4ce512d` 대비 이번 4개 파일을 읽고 계약/품질 GO, P0–P3 finding 없음으로 판정했다. reviewer의 초기 테스트 근거는 26 pass/3 skip이며, 이후 parent가 격리 DB 포함 29 pass/0 skip을 직접 확인했다. 실제 ID/RBAC 검증 미완료 경계는 동일하다. 로컬 코드 준비 완료이며 자동 활성화 범위는 사용자 응답 대기다.

### 자동 게시 범위 승인 수신

사용자가 Azure 공개배포부터 새로 시작하므로 적용 시작 시점은 상관없다고 승인했다. 위 응답 대기는 해소되었다. 기존 Pending 및 앞으로 완료되는 제품 전체를 대상으로 하며 현재 worker의 대상 선정 규칙을 유지한다. 승인된 공개 필드와 정정 시 같은 URL 재게시 규칙은 그대로다. 이 승인으로 즉시 운영 배포하거나 로컬 검수 데이터를 초기화하지 않았다. 로컬 환경에는 Azure 관리 ID endpoint가 없으므로 임시 SAS를 다시 연결하여 상시 자동 게시하지 않고, 운영 배포 단계에서 관리 ID 권한과 실제 게시를 검증한 뒤 활성화한다. 이카운트는 여전히 마지막 구현 Task이며 문서/비식별 발주 응답 샘플 대기다.

## Change 018 — 마지막 이카운트 발주 연동 착수

사용자 “시작하자”에 따라 Task 7 착수. branch `codex/interior-busbar`, 기준 `c0cfaf8`, WIP 없음 확인 후 기존 계약·코드·공식 공개 안내를 읽었다. 이카운트 발주 조회 제공 및 로그인 후 세부 매뉴얼 확인을 공식 안내에서 재확인했다. 세부 API 문서/응답 샘플은 저장소에 없으며 사용자에게 파일 경로 또는 로그인된 매뉴얼 위치를 요청했다. 인증키·비밀번호를 채팅으로 요청하지 않았다.

확인한 연결 지점: `busbar_purchases`는 자재별 발주 행으로 `external_id` unique, `external_status`, `conflict_note` 준비 필드가 있다. `SavePurchase`는 신규 요청에 UUID를 발급하므로 반복 API 응답을 그대로 전달하면 중복 방지가 되지 않는다. 입고는 별도 Receipt 동작에서만 원장·재고를 증가시키며 외부 수신은 이 동작을 호출하지 않아야 한다. 기존 입고 수량보다 작아지는 발주 변경은 충돌로 남겨야 한다.

세부 사양 확인 후 구현할 대조 항목: 인증/세션·테스트 endpoint, 발주 및 품목행의 안정적 식별자, 공통 프로젝트 코드의 실제 필드와 조회 조건, 품목 코드·단위·수량, 변경·취소·삭제의 응답 표현, 페이지/기간/호출 제한. 목록 누락을 취소로 추정하거나 발주번호만으로 서로 다른 행을 합치지 않는다. 기존 준비 필드의 적합성은 이 대조 후 판단한다. 외부 송신 기능은 추가하지 않는다.

이번 착수는 읽기 전용 조사와 상태 기록까지이며 연동 구현·실제 API 호출·DB 변경·새 테스트 실행·배포는 미실행이다. 현재 필요한 입력은 세부 매뉴얼과 비식별 응답 샘플이다. 이를 확보한 뒤 실제 필드 매핑, 반복 수신/변경/취소/재시도와 입고·재고 보존 반례를 합성 환경에서 구현·검증한다.

## Change 019 — 주문·판매 범위 확장 및 실제 회사 양식 확인

2026-09-14 Chrome에 사용자가 연 이카운트 공식 API 매뉴얼의 주문서입력·판매입력·발주서조회·FAQ·이용방법과 회사 판매/주문 입력 및 웹자료올리기 항목을 UI로 확인했다. 브라우저 세션 URL과 인증키는 기록하지 않는다. ERP 전표 저장/API 실행/설정 변경은 하지 않았다. 빈 입력 화면에서 사이트 자체 임시저장 표시가 관측되어 화면 열기를 서버 상태가 전혀 변하지 않았다고 단정하지 않는다.

기존 `/private/tmp/emi-interior-busbar`는 Git 연결과 소스가 사라졌고 일부 기록만 남아 있었다. 보존된 마지막 commit `7645001`에서 `/private/tmp/emi-interior-busbar-ecount`, branch `codex/interior-busbar-ecount`를 만들어 복구했다. 이전 폴더/검수 데이터/타 작업 WIP를 삭제하거나 복원 덮어쓰기 하지 않았다. 복구 source의 v2 AGENTS를 유지한다.

### 확인된 API 계약

| 기능 | 확인한 사양 |
| --- | --- |
| 주문서 | POST `/OAPI/V2/SaleOrder/SaveSaleOrder`, `SaleOrderList[].BulkDatas` |
| 판매 | POST `/OAPI/V2/Sale/SaveSale`, `SaleList[].BulkDatas` |
| 공통 입력 | `UPLOAD_SER_NO`는 전표 묶음 순번이며 재시도 중복 방지 키로 설명되어 있지 않다. `WH_CD`, `PROD_CD`, `QTY` 필수. 회사 양식별 추가 필수 항목 존재 |
| 결과 | HTTP/본문 Status 200만으로 성공 판단 금지. `SuccessCnt`, `FailCnt`, `ResultDetails`, `SlipNos`를 검사하며 전표번호 보관 |
| 수정/삭제 | 공식 FAQ에서 불가능, 신규 코드/전표 입력만 지원한다고 명시. 전송 완료 후 PMS 수정은 자동 덮어쓰기 없이 정정 필요 상태로 처리해야 함 |
| 불명확 결과 | 원격 생성 뒤 응답 유실 가능. 중복 방지 보장이 확인되지 않아 무조건 자동 재전송 금지. 전표 확인 후 조정 필요 |
| 발주 조회 | POST `/OAPI/V2/Purchases/GetPurchasesOrderList`. 날짜 범위 최대 30일, PAGE_SIZE 최대 100. 응답 `PJT_CD`로 공통 코드 필터 가능하나 요청 필드에는 프로젝트 조건이 없음 |
| 발주 제한 | 문서상 응답은 `ORD_DATE`, `ORD_NO`, `PJT_CD`, `P_FLAG`, `UPDATE_DATE`, 수량/금액 합계 등. 품목별 PROD_CD/수량/안정적 행 ID는 확인되지 않음. 합계를 자재 입고로 만들지 않는다. P_FLAG 종결을 취소로 간주하지 않는다 |

### 회사 양식과 확정 매핑

- 사용자가 지정한 엘에스일렉트릭(주)의 거래처검색 결과 코드 `1168119273`, 청주캠퍼스의 창고검색 결과 코드 `CC` 확인. 이름이 다른 청주캠퍼스 자재창고와 구분했다. 코드값은 배포 기준정보로 입력할 대상이지 합성 테스트 기본값으로 하드코딩하지 않는다.
- 주문서/판매의 입력 및 웹자료올리기 양식에 거래처·출하창고·PO No·프로젝트·품목·수량·단가·공급가액·부가세가 존재한다. 주문서에는 W/O No와 납기도 별도 존재한다. 주문서 기본입력화면설정에서 PO No는 `u_memo1`(문자형식1), W/O No는 `u_memo2`(문자형식2)로 대조했다. 사용자 응답에 따라 주문서 `U_MEMO2`에 고객 업무번호를 전달하고 `U_MEMO1`은 생략한다. 판매 양식에는 W/O 항목이 없어 임의 필드에 복사하지 않는다.
- 제품군별 이카운트 품목코드·기준 단가를 관리하고 프로젝트에 단가 snapshot/필요한 변경값을 저장하는 방향. 공통 프로젝트 코드는 `PJT_CD`, 제품군 품목코드는 `PROD_CD`, 수량 `QTY`, 단가 `PRICE`, 공급가액 `SUPPLY_AMT`, 부가세 `VAT_AMT`, 주문 납기는 `TIME_DATE`로 대조한다.
- 사용자 확정: 원화·부가세 별도 10%, 고정 거래처/창고. 원 미만 금액 처리와 회사 API 거래유형 코드의 실제 설정은 검증 전 확인한다.
- 프로젝트 등록 시 주문 1건, 마지막 분할 출하로 해당 등록 건 완료 시 총 수량에 대한 판매 1건을 생성하는 계약. 완료 취소/재완료로 전체 판매를 중복 생성하면 안 된다. 이미 보낸 전표의 수량/가격 변경은 수정 불가 계약에 따라 정정 필요 상태로 관리한다.

사용자 응답으로 번호 매핑은 확정되었다: W/O No만 사용하고 PO No는 입력하지 않는다. 제품군별 실제 코드·가격과 공통 프로젝트 코드 값은 기준정보로 입력할 대상이다. 원 미만 최종 전송 금액 처리, 회사 거래유형 코드, 발주 자재별 상세 취득 경로는 확인이 필요하다.

### 상업 데이터 선행 구현·검증

기준정보에 제품군별 이카운트 품목 코드·기준 단가와 고정 거래처/창고 코드를 추가했다. 신규 프로젝트는 제품군 기준 단가를 복사하고 별도 변경할 수 있다. 기존 프로젝트의 단가는 이후 제품군 단가 변경·일반 정정에 영향받지 않는다. 미설정은 null이며 명시적인 0과 구분한다. 단가 생략은 신규 건의 기본값 적용/기존 건의 보존을 의미한다. 추가 migration 0093은 기존 자료와 재고를 유지하며 과거 가격을 임의로 만들지 않는다.

프로젝트 상세에 원화 공급가액·부가세 10%·합계와 누락 기준정보를 읽기 전용으로 표시한다. preview는 정확한 소수 계산값을 반환하며 아직 확정하지 않은 ERP 반올림 정책을 적용하지 않는다. 실제 전송 비활성을 명시한다. 엑셀은 기존 8열 뒤에 선택 단가 9열만 추가해 기존 양식을 유지한다. 화면 고객 업무번호 라벨은 W/O No로 명확히 했다.

검증: 격리 합성 `busbar_test`(127.0.0.1:55490)에서 부스바 관련 56/56 통과, 건너뜀 0. 가격 보존/변경/미설정/0/잘못된 단가, 설정 저장 후 workspace 재조회, 0093 이전 실제 테이블의 기존 프로젝트·재고 보존, 기존 단가 없는 import 신규/정정 및 읽기 전용 금액 계산을 포함한다. 새 migration 테스트의 취소 토큰 정적검사 실패는 보정 후 통과했다. frontend typecheck/변경 파일 eslint 및 신규 합성 Playwright 1/1 통과. 첫 UI 테스트의 label 선택 실패는 실제 combobox role을 사용해 보정했다. 1440/390px 입력·금액 조회 화면을 직접 시각 확인했다. 증거는 `/private/tmp/emi-busbar-commercial-{editor,preview}-{1440,390}.png`이며 합성 데이터만 포함한다.

독립 reviewer `commercial_review`(요청 Astra/high, 실제 NOT_REPORTED)가 설정 workspace에서 고정 코드를 누락하는 P2를 발견했고 보정 및 회귀 테스트를 추가했다. 최종 diff·테스트·390px 입력/1440px 조회 이미지를 독립 재검토해 계약/품질 GO, 열린 차단 finding 없음으로 확정했다. 이번 완료 범위는 단가·기준정보·조회 선행 구현이다. 주문/판매 전송 대기열·중복 방지·응답 불명확 상태·인증/실제 API 연결과 발주 수신은 아직 미구현이다. 운영 DB migration·실제 전표 전송·공유 서버 갱신·push/PR/merge/배포를 수행하지 않았다. 사용자 검수 후 최종 병합 후보 전체 회귀는 별도 단계로 남긴다.


## Change 020 — 제품군 단가 단일 기준으로 정정

사용자 “단가는 제품군별 단가 고정임. 바뀌면 제품군별 단가를 바꿔야함.”에 따라 Change 019의 프로젝트별 단가 변경/보존 계약을 대체한다. 제품군 기준정보만 단가를 소유하며 프로젝트 신규·정정·엑셀에서 별도 단가를 입력하지 않는다. 프로젝트 금액 조회는 제품군의 현재 단가로 계산한다. W/O만 사용·PO 공란·원화 VAT 별도 10%·고정 거래처/창고는 유지한다. 아직 구현하지 않은 외부 전송의 전표 이력은 향후 전송 당시 값을 보존해야 하며 기준 단가 변경으로 이미 보낸 전표를 덮어쓰지 않는다.

프로젝트 요청/화면/엑셀의 단가와 snapshot 로직을 제거하고 조회를 제품군 기준으로 바꿨다. 제품군 단가 변경 시 펼친 금액 조회도 다시 읽도록 한다. 0093은 main 미반영·운영/검수 DB 미적용이며 disposable 테스트에만 사용했으므로 미배포 migration의 프로젝트 가격 열 추가를 제거했다. 기존 업무 데이터와 재고는 변경하지 않았다.

검증: 합성 busbar_test DB에서 해당 가격·import·설정·기존 데이터 migration 테스트 5/5 통과(0 skip), frontend typecheck/lint 및 PC·390px 합성 UI 테스트 1/1 통과. 프로젝트 요청에 단가가 없고 단가 필드도 표시되지 않는 것을 확인했다. PC 금액 조회와 모바일 입력 이미지를 직접 확인했다. 독립 reviewer `family_price_review`(새 맥락, 요청 Astra/high, 실제 NOT_REPORTED)가 코드·계약·테스트 및 두 화면을 확인해 GO, 열린 finding 없음으로 판정했다. 실제 서버 반영·외부 전표 전송·push/merge/배포 없음.


## Change 021 — 주문·판매 전송 대기와 중복 방지

사용자 다음 작업 시작 요청으로 전송 대기 저장·상태 조회·안전한 재시도 기반을 구현한다. 프로젝트 등록과 주문 대기, 마지막 출하와 판매 대기를 같은 업무 transaction에 저장하고 프로젝트/종류별 1건만 허용한다. 완료 취소 시 미전송 판매는 보류하며 재완료로 중복 생성하지 않는다. 전송 시점 제품군 가격·설정·프로젝트 값을 별도 시도 이력에 보존한다. 결과 불명확 건은 자동 재시도하지 않는다. 실제 인증·회사 거래유형·금액 처리의 확정과 실제 API 연결은 분리하고 외부 전표를 생성하지 않는다. 기존 프로젝트를 migration만으로 주문 전송 대상으로 만들지 않는다.


사용자 추가 확정: 공급가액·부가세는 원 미만 소수 금액을 그대로 유지한다. 임의 반올림·버림을 하지 않는다. 확인된 API 허용 정밀도(공급가액/VAT 소수 4자리, 주문서 PRICE numeric18,6)를 초과하면 전송 시도 생성 전에 보류한다. 제품군 단가 자체의 입력 정밀도는 현재 4자리이며, 부가세 곱셈으로 5자리가 생기는 경우에도 원값을 변형하지 않는다.

구현 결과: migration0094에 프로젝트/종류 unique 대기 건과 불변 전송 시도 payload를 추가했다. 신규 프로젝트/엑셀 등록과 주문 대기를 함께 저장하고 마지막 분할 출하와 판매 대기를 함께 저장한다. 완료 취소는 미전송 판매를 보류하고 재완료 시 기존 대기를 사용한다. 청주 조회/담당자 입력 경계 내 상태 조회 및 사유를 남기는 다시 대기 API/UI를 추가했다. 실패는 전표 미생성이 확정된 경우에만 다시 대기할 수 있고 성공/불명확/변경 확인 건은 차단된다. 동시에 claim해도 시도는 하나이며 과거 응답이 새 시도 상태를 덮어쓰지 않는다. 실제 전송 직전 사용할 제품군 단가·코드·프로젝트·정확한 금액을 시도별로 보존한다.

중요한 구현 경계: 인증된 이카운트 HTTP adapter/주기 실행기는 아직 연결하지 않았다. Claim/Finish/Recover는 후속 adapter용 내부 상태 처리 함수로 합성 테스트에서만 실행했다. 운영에서 대기 내역을 자동으로 보내거나 중단 상태를 자동 복구하는 worker는 아직 없다. 화면에 실제 전송 연결 전임을 명시한다. 결과 확인이 필요한 원격 전표의 대조/해결 흐름도 실제 응답 처리와 함께 후속 구현한다. 현재 코드만으로 실전송 완료라고 표현하지 않는다.

검증: 전용 합성 busbar_test에서 부스바 관련65/65 통과(0skip). 주문 대기/분할 출하/재완료 중복 방지, 동시claim, 시도 당시 금액 보존, 확정실패 후 재시도, 이전 응답 차단, 주문 성공 전 판매 보류, 성공판매 취소·재완료 차단, 중단상태 복구, 소수1234.5 보존/정밀도 초과 보류, 청주 조회·쓰기/오산 직접API 거부를 포함한다. 초기 실행의 새 mutation 감사 registry 누락 및 합성 테스트 숫자 cast 오류를 보정했다. frontend typecheck/lint, 합성 UI2/2 통과. 최초 모바일 페이지 overflow를 발견해 전송상태를 상세 전체 너비로 분리하고 표 내부 스크롤을 유지했다. 최신 390/1440 화면을 직접 확인했으며 합성 증거는 `/private/tmp/emi-busbar-ecount-status-{390,1440}.png`이다.

독립 reviewer `ecount_queue_review`(새 맥락, 요청 Astra/high, 실제 NOT_REPORTED)가 P2 세 건(확정실패 후 변경확인 잔류, 무변경 저장 invalidation, 업무 수정에 따른 복구 기준시각 연장)을 발견했다. 실제 payload 비교, 확정실패 플래그 정리, 불변 시도 시작시각 기준으로 보정하고 각 반례를 추가했다. 최종 재검토에서 계약·품질 GO, 열린 차단 finding 없음으로 판정했고 1440/390 이미지를 직접 확인했다. 로컬 코드/검증만 진행했으며 검수·운영 DB migration, 공유서버 갱신, 실제 ERP 전송, push/PR/merge/배포는 하지 않았다.


## Change 022 — 실제 API 연결 코드 및 자동 실행

사용자 다음 작업 요청으로 인증/단건 주문·판매 HTTP adapter와 자동 실행을 구현한다. 외부 매뉴얼은 읽기 전용 Chrome UI에서 Zone/login/주문 응답을 재확인했고 세션 URL·인증키를 기록하지 않았다. Zone Data.ZONE/DOMAIN, 로그인 Data.Datas.SESSION_ID와 Code00, 단건 전표 SuccessCnt/FailCnt/ResultDetails/SlipNos를 대조한다. 인증 실패는 영속 중지하며 운영 로그인 10분/테스트10초 간격, 저장11초 간격을 DB에서 통제한다. 테스트·운영 환경은 대기 DB에 바인딩하여 동일 DB의 테스트 성공을 운영 성공으로 재사용하지 않는다.

사용자는 자동로그아웃 시간을 모른다고 응답했다. Self-Customizing→보안관리→보안설정에서 자동로그아웃 `4 시간`을 읽기 전용으로 직접 확인했다. ERP 설정을 바꾸지 않았다. 운영 준비값 SessionIdleMinutes는 240분보다 짧은230분으로 권장하며 환경변수에 명시한다. 활성화 기본값 false, ReviewSafe에서는 인증/DB처리/외부전송 모두 차단한다. 실제 API키·발신IP등록/운영설정·provider 호출·배포는 이번 구현에서 실행하지 않는다.

구현: Zone 조회와 인증키 API 로그인, 세션 재사용, 단건 주문/판매 전송 adapter를 연결했다. 사용자의 웹사이트 직접 로그인은 정상 전송에 필요하지 않다. 요청 필드 대소문자를 그대로 보존하고 W/O는 주문 U_MEMO2만, PO는 생략한다. 소수 금액을 유지하며 전송일·거래유형도 시도에 고정한다. 0095에 환경·회사 바인딩, 영속 호출 간격·중지 상태를 추가했다. worker는 legacy 설정을 포함해 청주 DB identity를 반드시 확인하며 동시에 하나만 전송한다. 잘못된 DB·다른 회사/테스트 환경에서는 인증 전에 중지한다. 응답 유실·형식 불명확·중단 복구는 Unknown으로 남기고 자동 전송을 중지한다. 확정 실패 3회도 중지한다. 판정과 중지 상태는 같은 transaction에 저장한다.

담당자 화면에 연결 상태·사유를 남기는 자동 전송 재개·전표 확인 반영을 추가했다. Unknown은 실제 ERP 조회 후 전표 있음(번호 필수)/없음으로 확인한다. 없음도 자동 재전송하지 않고 별도 다시 대기 동작이 필요하다. 성공 후 변경 확인은 실제 ERP 정정·대조 사유와 현재 기준정보/누적 출하 수량을 보존해 해소한다. 원래 전송 payload는 유지하고, 확인 이후 단가 또는 일부 분할 출하 수량이 다시 바뀌면 재확인을 표시한다. 전표 확인 반영은 외부 전표를 수정하거나 연결을 자동 재개하지 않는다. 재개/확인은 worker 진행 중 거부하며 청주 담당 권한·감사 이력을 적용한다.

운영 준비 설정 이름: `InteriorBusbar__Ecount__Enabled`(기본 false), `Environment`(Test 또는 Production), `CompanyCode`, `UserId`, `ApiKey`, `SessionIdleMinutes`(확인한 4시간보다 짧은230), 선택 `IoType`(검증된2자리, 공란이면 회사 기본값). 뒤의 이름도 같은 `InteriorBusbar__Ecount__` 접두사를 사용한다. 인증키는 승인된 환경변수/비밀 저장소에 주입하며 저장소·채팅에 기록하지 않는다. 실제 테스트 전에는 테스트 인증키 유효성, 등록 발신 IP, 테스트용 품목/공통 프로젝트/거래처/창고와 전송할 합성 건을 대조해야 한다. 실제 연결 활성화·전표 생성은 해당 환경의 실행 승인 후 진행한다.

검증: 합성 전용 busbar_test(127.0.0.1:55490)와 fake HTTP를 사용했다. adapter63/63(0skip), 최초 부스바138/138 통과 후 확인 흐름/권한/회사 경계 추가 검증144건 중143통과·테스트 SQL 숫자 변환1실패를 확인했다. 실패는 `100.0000` 문자열을 bigint로 직접 변환한 테스트 오류였고 numeric 변환으로 고쳤다. 추가 분할 출하 반례까지 포함한 최종 queue10/10(0skip) 통과로 영향 범위를 재검증했다. 운영 로그인10분 간격, 실패3회 중지, 다른 회사/오산/Directory DB 인증 차단, 동시 worker/재개 거부, 중단 상태의 원자적 중지, 실제 Store snapshot→HTTP adapter→가짜 ERP의 소수1234.5/W/O/납기 전달을 확인했다. frontend typecheck/eslint 통과, 관련 합성 UI3/3 및 최종 연결 화면1/1 통과. 최종390/1440 전체 확인 입력창을 직접 시각 확인했다. 합성 화면 증거는 `/private/tmp/emi-busbar-ecount-connection-{390,1440}.png`이다. 초기 테스트 실행기/화면 서버의 로컬 소켓 제한은 승격된 합성 테스트 실행으로 해소했다.

미실행: 실제 ERP 인증·전표 생성, 사용자 검수, 공유 runtime 전환, 검수/운영 DB migration, push/PR/merge/배포. 사용자 검수 후 최종 병합 후보 전체 회귀는 별도다. 다음 실전 연결 검증은 승인된 테스트 환경의 합성 주문1건→완료판매1건 대조이며, 발주 자재별 상세 취득 경로 확인/수신 연동은 마지막 단계로 남는다.

독립 reviewer `ecount_connection_review`(요청 Astra/high, 실제 NOT_REPORTED)가 기준fa8230d 이후 누적 diff·반례·최종390/1440 이미지를 확인해 계약/품질 GO를 판정했다. R1청주 identity 생략(P1), R2결과/중지 원자성(P1), R3재개 경쟁(P2), R4불명확 오류 배열(P2), R5확인 이후 추가 부분출하 감지(P2)를 모두 보정했고 열린 P0/P1/P2 없음. 추가 감사 mutation coverage4/4 통과. 완료한 범위는 로컬 commit으로 보존하며 원격/운영 반영과 구분한다.

## Change 023 — 이카운트 테스트 환경 단건 실전 검증 준비

사용자 “진행하자”는 직전 제안의 테스트 환경 합성 주문1건→완료판매1건 대조 실행 승인으로 확인한다. 운영 endpoint·운영 전표·공유 runtime/배포 승인은 포함하지 않는다. 기준1e3a330, clean branch 확인. 현재 process 환경과 해당 앱 설정에 Ecount 인증 설정이 없으며 UserSecretsId도 없다. 실제 인증키·회사코드·API사용자 및 테스트 대상 품목/공통 프로젝트 코드가 필요해 인증/전표 요청은 아직 실행하지 않았다. 저장소 밖 소유자 전용 입력 파일 `/private/tmp/emi-busbar-ecount-test-settings.json`을 생성했다. Environment=Test, Enabled=false, 수량1·단가12345·W/O=PMS-API-TEST이며 실제 계약의 고정 거래처/창고 코드를 준비했다. 비밀값은 기록·출력하지 않는다. 제공된 설정/기존 비밀 파일을 확인한 뒤 승인된 두 전표만 처리하는 격리 실행을 준비·실행하고 결과를 대조한다. 현재 브라우저 도구는 이전 Chrome 제어 도구를 제공하지 않고 Aside CLI도 없어 이전 열린 세션에서 키를 가져왔다고 주장하지 않는다. 설치나 브라우저 설정 변경은 하지 않았다.

사용자 추가 요청: 사이트에서 인증 설정을 직접 확인·주입하고 사용자 본인이 로컬 화면에서 데이터를 입력해 검증한다. 이 범위의 테스트 연결 준비를 진행한다. 현행 도구에는 이전 Chrome 제어 기능이 없고 Aside CLI는 미설치여서, 적용 aside-browser 스킬의 명시 지침에 따라 CLI 설치 허용을 요청했다(응답 대기, 미설치). 기존5197/5097 프로세스는 이전 `/private/tmp/emi-interior-busbar` source 소유임을 확인했으며 변경하지 않았다. 현재1e3a330 Release build 경고0/오류0으로 실행 파일을 준비했다. 사이트 인증 확인·설정 주입·새 로컬 검수 연결은 미완료이므로 현재5197 입력이 실제 이카운트로 전송된다고 안내하지 않는다.

사용자가 Aside CLI 설치를 명시 승인해 공식 설치를 실행했다(1.26.906.1630). Aside 브라우저를 열어 연결 복구 후 공식 이카운트 페이지의 로그인 링크를 따라 로그인 화면에 도달했다. 이전 Chrome 세션은 이 브라우저에 공유되지 않으며 회사코드·아이디·비밀번호 필드가 비어 있고 자동 입력 경로도 표시되지 않았다. 사용자에게 열린 Aside 화면에서 최초1회 로그인만 요청했다. 로그인 정보·API키를 채팅으로 요청하지 않았다. 실제 인증키 확인/주입이나 API 전송은 아직 미실행이다. 로그인 후에는 사용자 직접 입력 검증 요청을 우선하며 합성 주문/판매를 대신 등록하지 않는다.

사용자 로그인 완료 후 Aside에서 실제 API인증키발급과 공식 API 이용방법을 확인했다. 테스트 키는 API인증현황에서 별도 발급(2주 유효)하며 테스트용 허용 IP가 필요하다. 테스트 키발급 시 이카운트가 '테스트 인증키 발급 횟수를 초과하였습니다. [제공 횟수 : 5/3]' 안내를 반환했다. 반복 발급·운영키 대체 호출 없이 추가 발급 요청 양식을 열어 SUBIN.PARK를 선택하고 주문서/판매 각1건의 로컬 테스트 목적·소수금액 검증 사유를 입력했다. 지원 담당자에게 외부 요청을 전송하는 승인은 별도 필요하여 전송 버튼은 누르지 않았다. 현재 실제 테스트 키 발급·로컬 주입·ERP API 인증/전표 전송 미완료이며, 테스트 키 승인 후 사용자가 직접 데이터를 입력하는 검증을 계속한다.

사용자 정정: 사용할 계정은 SUBIN.PARK 또는 NAYOON.LEE이며 테스트 키 필요성을 재확인 요청. 추가 테스트 키 요청창을 전송하지 않고 닫았다. 실제 API인증현황의 렌더링된 내용에서 주문서입력·판매입력이 '검증이 완료된 API 리스트'에 포함됨을 확인했다. 기존 테스트 키(NAYOON.LEE)는 2026-05-25 만료지만, 현재 필요한 두 API의 운영 검증은 이미 완료 상태다. 따라서 기존 유효 운영 키로 운영 API를 이용하는 경로에 추가 테스트 키 발급이 선행조건이라고 안내한 것은 정정한다. 별도 테스트 endpoint에서 시험하는 경우는 테스트 키가 필요하다. 로컬 PMS도 운영 endpoint에 연결하면 실제 전표가 생성되므로, 기존 테스트 환경 승인과 구분해 운영 전표 생성 연결 여부를 사용자에게 확인한다. 실제 API키 추출/주입·IP설정 변경·운영 API 요청/전표 생성은 아직 하지 않았다.


사용자 최종 승인: SUBIN.PARK 운영 계정으로 진행하고 실제 회사 주문서·판매 전표 생성도 허용한다. 생성된 전표 종류·번호를 명확히 보고한다. 사용자가 로컬에서 직접 입력하는 흐름을 우선하며 임의 합성 프로젝트/전표를 대신 만들지 않는다. 이 승인으로 위 테스트 환경 한정 실행 경계를 해당 로컬 연결의 Production 사용으로 확장한다. 원격 반영·운영 PMS 배포·ERP 전표 삭제 승인은 포함하지 않는다.

2026-09-14 로컬 연결 적용: ERP에서 유효한 SUBIN.PARK 운영 키와 저장된 로그인 회사코드66294를 확인해 저장소 밖0600 설정에 주입했다(키 비출력). 현 발신 IP는 이미 ERP 허용 목록과 일치하여 IP 설정을 변경하지 않았다. SessionIdleMinutes230 적용. 기존 소유 preview5097/5197의 PID·cwd를 대조하고 현재1e3a330 source로 전환했다. API는5097, 화면은 http://127.0.0.1:5197/interior-busbar 이다. 최초 프론트엔드 API 기본주소 연결 실패를 발견해 VITE_API_BASE_URL 공란·5097 proxy로 재기동했고 실제 관리자 기준정보 화면 정상 표시를 직접 확인했다.

검수 DB busbar_review_20260909(Docker emi-busbar-test-20260909)는 삭제/초기화하지 않았다. 컨테이너 내부 새 custom backup을 생성·비어있지 않음을 확인한 후 공식 --migrate-only로0093~0095를 적용해95개 migration 검증 성공을 확인했다. 프로젝트1건·제품65개 보존. 기존 비어 있던 DB identity는 해당 청주 검수 DB에 한정해 CHEONGJU/0086 계약으로 바인딩했다. Ecount runtime은 Production/66294, paused=false로 확인했다. migration/seed/QR공개게시/알림 provider는 비활성 유지. ERP 고정 거래처1168119273·창고CC는 관리자 API로 저장해 감사 기록을 남겼다.

현재 전표 전송 대기0건·시도0건·생성 전표0건이며 API 로그인은 아직 실행되지 않았다. 이카운트 실제 인증과 주문/판매 성공은 사용자 첫 입력 이후 대조할 검증으로 남는다. 기존 공통 프로젝트 코드'a'와 제품군6000a/6000a dc의 미설정 이카운트 품목코드·기준 단가는 사용자 실제 기준정보로 채워야 한다. 임의 코드·단가를 만들지 않았다. 기존 프로젝트를 주문 대기로 소급 생성하지 않는다. 사용자는 기준정보를 먼저 저장한 후 새 프로젝트를 등록하고, 마지막 출하 완료로 판매를 확인한다. Git 원격 반영·merge·운영 PMS 배포는 수행하지 않았다.


독립 runtime 검토에서 launcher의 AdminDeletionPurge 명시 비활성 누락(P1)을 발견했다. 기본값에 의존하지 않도록 AdminDeletionPurge__Enabled=false를 추가하고 해당 backend만 재기동했다. 인증된 runtime-mode 응답에서 adminDeletionPurgeWorkerEnabled=false·backgroundWorkersEnabled=false를 확인했다. backup을 복원하지 않고 COPY 행 수만 현재 DB와 대조해 사용자12→12, 부서10→10, 휴일0→0, 부스바 프로젝트1→1, 제품65→65로 삭제 영향 없음을 확인했다. 공통 코드'a'는 비어 있지 않아 자동 차단 대상이 아니므로, 실제 코드로 먼저 변경한 뒤 신규 프로젝트를 등록해야 한다고 안내한다. 신규 프로젝트는 당시 공통 코드를 보존하므로 나중에 기준정보만 바꿔도 기존 프로젝트 코드가 자동 변경되지 않는다.

독립 reviewer ecount_connection_review가 보정된 실행기·loopback listener·비밀파일 권한과 실행자가 제공한 runtime/보존 증거를 재검토하여 Change023 로컬 연결 준비 GO, 열린 차단 finding 없음으로 판정했다. 실제 ERP 인증·전표 검증은 아직 대기다.


## Change 024 — 최초 사용자 등록 확인과 실제 응답 형식 보정

사용자 '생성 완료' 후 신규 'pms 테스트'(수량1) 등록과 Order Pending1건을 확인했다. 시도0·전표번호 없음. 인증 worker는2026-09-14 08:47 UTC에 중지되었다. 실제 읽기 전용 Zone 응답은 Status 숫자200·Error 생략인데 adapter가 문자열200·Error:null만 허용해 로그인 전에 실패한 원인을 확인했다. 비밀값·원시 응답은 기록하지 않는다.

Envelope은 숫자200 또는 문자열200과 Error 생략/null을 허용하도록 최소 보정했다. 그 외 상태·타입·nonnull Error는 거부하며 회사·사용자·세션·호스트 검증과 저장 성공 증거·Unknown/재시도 경계는 유지한다. 합성 회귀6건 추가 후 adapter69/69(0skip), Release build 경고0/오류0, diff check 통과. 최초 테스트의 실행기 소켓 sandbox 제한은 승인된 로컬 실행으로 해소했다. 작성과 분리된 기존 reviewer ecount_connection_review가 이번 diff와 결과를 재검토해 GO·열린 finding 없음으로 확정했다.

수정을 해당 로컬5097에 적용했으며 영속 전송 중지는 유지한다. 실제 인증 성공·전표 생성은 여전히 미확인이다. 사용자 신규 건의 제품군 품목코드/단가가 미설정이고 프로젝트 공통코드는'a'여서 실제 사용할 세 값을 요청했다. 사용자가 기준정보의 공통코드만 변경해도 이미 등록한 프로젝트는 자동 갱신되지 않으므로 승인된 실제 코드 확인 후 해당 등록 건의 보정 경로를 확인한다. 승인되지 않은 추측 코드·가격으로 전송하거나 프로젝트를 다시 만들어 중복 대기를 만들지 않는다. 원격/merge/운영 배포 없음.


사용자 '입력해놨어' 후 공통코드ps26-045, 6000a 품목코드/단가307407 입력을 확인했다. 기존 테스트 프로젝트에만 남은 임시코드는 사용자 요청에 따라 DB·paused·Pending·attempt0을 transaction에서 확인하고 advisory lock 및 before/after 감사 기록을 남겨 보정했다. 새 프로젝트나 전송 대기를 추가 생성하지 않았다. 재개 후09:01:32UTC 인증이 다시 실패해 attempt0·전표0 상태다.

인증 실패 원인 구분을 위해 adapter의 고정 단계명과 공식 매뉴얼 오류코드에 대응하는 고정 분류명만 로그에 남기도록 보완했다. provider 원문·인증키·세션·URL·예외값은 로그에 포함하지 않는다. 인증 성공/실패 조건과 재시도 간격은 변경하지 않았다. 합성 adapter74/74(0skip), Release0warnings/0errors, 독립 reviewer GO·열린 finding 없음. 로컬 runtime에 반영해 기존09:01:32 기준10분 제한을 유지하며 재개했다. 웹사이트 키 재추출·파일 저장을 통한 비교는 자동 승인 검토가 거부하여 중단했다. 우회하거나 다시 추출하지 않고 기존 설정을 사용한다.


09:11:37UTC 재개 진단은 ZoneHost에서 종료되어 실제 로그인 요청 전 실패임을 확인했다. 인증키 없는 별도 .NET Zone 조회에서 JsonContent를 버퍼링하지 않으면 Data에 EMPTY_ZONE만 오고, 전송 전 본문을 읽어 버퍼링하면 정상 ZONE/DOMAIN이 반환되는 것을 재현했다. 실제 adapter를 UTF-8 application/json StringContent로 바꿔 Content-Length를 제공했다. 같은 직렬화 옵션·소수·필드 매핑은 유지한다. fake handler가 본문 읽기 전 ContentLength를 검사하게 해 테스트 자체 버퍼링이 실제 문제를 가리던 부분을 보정했다. 합성74/74, Release0warnings/0errors 및 고정 길이 실제 Zone 조회 정상 응답 확인. 독립 reviewer GO. 해당 local runtime 반영·재개했고09:21:37UTC 이전 로그인 재시도는 차단된 채 유지한다.


09:21:40UTC 실제 Zone은 통과했으며 다음 실패는 LoginCode로 확인했다. 공식 로그인 Result 표는 Data에 COM_CODE/USER_ID/SESSION_ID를 직접 나열하고 과거 Example은 Code00+Datas로 감싸고 있어 두 구조를 지원하도록 보정했다. nested 또는 Code가 존재하면 반드시 문자열00이어야 하며 성공 envelope·정확한 회사/사용자·유효 세션 검증은 공통으로 유지한다. Datas가 잘못되면 flat로 대체하지 않는다. 실제 응답 분별용 로그는 해당 필드의 존재 여부·JSON형식만 기록한다. 합성80/80(0skip), Release0warnings/0errors, 독립검토 GO. local 적용·재개했으며09:31:40UTC 이전 재로그인 제한을 유지한다. 로그인 성공과 전표번호는 아직 대기다.


09:31:44UTC 응답의 Code=String/Datas=Object와 flat 필드 없음이 확인되었으며 LoginCode 실패가 지속됐다. 실제 성공 코드 불일치를 구분하도록 Data.Code만 최대10자 ASCII영숫자/_/-로 제한해 진단한다. 그 외 값은 Unrecognized로 표시하고 응답 메시지·세션·키는 기록하지 않는다. logger capture4건을 추가해 총84/84(0skip), Release0warnings/0errors, 독립 reviewer GO. auth조건을 변경하지 않고09:41:44 이후1회 확인하도록 local반영·재개했다. 아직 전송 시도와 전표 생성은0이다.


09:41:45UTC 실제 로그인 Data.Code=205를 확인했다. 공식 매뉴얼에서205는 허용되지 않은 접속IP다. 현재.NET/Python 발신IP61.39.208.57은 이전 확인값14.33.156.30과 달랐으며 ERP 허용목록에 없었다. 승인된 로컬 실전 연결 범위에서 API IP등록에 현재 단일IP만 추가했다. 저장 후 팝업을 다시 열어 새IP와 기존4개IP 모두 보존됨을 확인했다. 초기 DOM 집계는 popup 로딩 전0건으로 나왔으나 후속 scoped snapshot에서 저장을 확인했다. 키 재발급·기존IP삭제·전체IP허용은 하지 않았다.09:51:45UTC 이후 재개하도록 기존간격을 유지했다.


09:51:47UTC SUBIN.PARK 운영 인증 성공 후 사용자 등록 건 Order를 정확히1회 전송했다. 저장 응답은 Unknown으로 분류되어 자동 전송을 중지했다. ERP 주문서조회에서 공통 프로젝트PS26-045로 검색하고 2026/09/14 -2 상세를 열어 품목 주문서-INT-BB-6000A, 수량1, 공급가액307407, 부가세30740.7, 거래처1168119273, 창고CC 및 빈 PO/W/O를 대조했다. 실제 주문서 생성 확인 후09:57:57UTC reconcile API(Recorded)로 해당 전표번호를 연결했다. 최종 Order Succeeded/needsReview=false/attemptCount1이며 판매 전표는 생성하지 않았다. ERP 수정·삭제·중복 전송은 하지 않았다. 자동 전송은 paused=true로 유지한다. 저장 성공 응답 자동 판정은 아직 해결되지 않았으며 실제 저장 응답 구조 근거를 확보해 보정하는 작업이 남는다. 수동 전표 확인으로 성공 연결한 것을 자동 응답 처리 성공으로 간주하지 않는다. 사용자에게 추가 데이터 입력 전 이 한계를 안내한다. 원격 반영·merge·운영 PMS 배포 없음.
