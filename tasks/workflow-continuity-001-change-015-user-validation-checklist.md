# TASK-WORKFLOW-CONTINUITY-001 Change 015 사용자 검수 체크리스트

상태: `사용자 검수 대기 — 마지막 일괄 검수`

고정 검수 환경:

- Frontend: `http://127.0.0.1:42983`
- Backend: `http://127.0.0.1:41166`

## 자동 검증 완료

- [x] 상세 IQC에서 모두 적합이면 합격 확정만, 하나라도 부적합이면 부적합 확정만 표시한다.
- [x] LQC·OQC 체크리스트에서 모두 적합이면 합격 확정만, 하나라도 부적합이면 부적합 확정만 표시한다.
- [x] 상세 IQC 서버는 응답과 반대되는 합격·부적합 우회 요청을 거부한다.
- [x] 기존 IQC·상세 IQC·LQC·OQC 재검사 실패는 같은 Pending을 `조치 요청`, 같은 내 업무를 `시작 전`으로 재활성화한다.
- [x] 전진검수·FAT도 같은 공통 Pending 재조치 전이를 사용하며 패널 통합 판정 방식은 유지한다.
- [x] 재조치 알림은 기존 정·부 담당자와 멱등 계약을 유지한다.
- [x] 자재 입고 확정 내 업무 상세는 품목명·수량·단위만 포함한 한 줄이다.
- [x] Backend 전체 `424/424`, Frontend 전체 `132/132`이 통과한다.
- [x] 고정 검수 주소 desktop·390px 품질 화면 로드, horizontal overflow 0, console error 0을 확인했다.

## 사용자 수동 검수

- [ ] IQC 초안의 모든 항목을 적합으로 입력하고 최종확인에 `합격 · 성적서 확정`만 보이는지 확인한다.
- [ ] IQC 초안의 한 항목을 부적합으로 입력하고 최종확인에 `부적합 · 입고 차단`만 보이는지 확인한다.
- [ ] LQC·OQC에서도 같은 단일 판정 동작이 보이는지 확인한다.
- [ ] IQC·LQC·OQC·전진검수·FAT 재검사를 다시 부적합 처리하고 같은 Pending이 `조치 요청`인지 확인한다.
- [ ] 조치 정·부 담당자의 내 업무에 같은 Pending 업무가 `시작 전`으로 나타나고 재조치 알림이 도착하는지 확인한다.
- [ ] 자재 역할 내 업무에서 `IQC 합격 도착분의 입고 확정을 진행해 주세요. (품목명 수량 단위)` 형식의 한 줄 상세를 확인한다.

## 게시 경계

- local experiment 검수만 수행한다.
- 대표 repo·`main`·Persistent UAT·실제 알림 provider는 이 체크리스트 범위가 아니다.
- Local commit은 사용자 승인에 따라 현재 누적 experiment checkpoint에 포함했다.
- push·PR·merge는 별도 사용자 요청 전까지 수행하지 않는다.
- `main` merge 승인 상태: `0/3`.
