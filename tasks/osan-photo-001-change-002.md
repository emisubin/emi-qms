# TASK-OSAN-PHOTO-001 Change002 — 모바일 원본 사진 업로드와 전체 40MiB 정책

2026-09-14 사용자 승인: 자동 압축·해상도 축소 없이 장당 5MiB 제한을 없애고 사진 전체만 40MiB로 제한한다. 기존 최대 5장·JPEG/PNG·악성코드 검사·민감 메타데이터 제거와 원본 픽셀 보존은 유지한다. 사진 수정은 유지 사진과 새 사진의 합계를 적용한다. 이번 변경의 원격 병합·공개배포 승인은 아직 없다.

기준선 origin/main d76790e, branch codex/osan-photo-total-40mib. 다른 작업 WIP/runtime은 보존한다. 서버 파서·검증·저장, 화면 제한·안내, 오산에 한정한 scanner 파일 한도40MiB, 요청 transport42MiB 및 신규0099 DB 제약 보정을 구현한다. 기존 migration은 수정하지 않는다.

실제 첨부 두 JPG는 각각8590934/9710149bytes로 종전5MiB 초과를 확인했다. 변경 후 용량은 통과하나 기존 JPEG 구조 검증에서 둘 다 여전히 거부된다. 이는 별도 원본 호환성 결함이며 이번 용량 정책 변경만으로 사진 저장이 해결됐다고 보고하지 않는다. 원본 사진은 저장소·증빙에 복사하지 않는다.

검증: 진행 중. frontend 경계·실제 선택/원본전송, 서버 정확40MiB/초과·5MiB초과 성공, scan 범위·실패차단, DB additive 제약, PC/390px 화면 및 독립 검토를 확인한다. 공개 데이터 수정·배포 없음.

## 검증 결과

- Frontend 관련35개 테스트 통과: 첨부 파일과 같은 용량2개(실제 사진 미포함)의 선택·미리보기 요소·버튼 활성·동일 File객체 전송, 정확40MiB/1byte초과·유지+신규 합계. typecheck/build 통과. lint 오류0, 기존 main.tsx Fast Refresh 경고1; 번들크기 경고는 기존 수준.
- Backend 관련11개 통과(실패/skip0): 실제 파서의6MiB·정확40MiB 합성 PNG byte보존, 합계초과, scoped scanner40MiB 허용/일반32MiB 제한/감염422/불가503, 기존 손상·메타데이터 검사. 최초 테스트의 DI 누락/합성 PNG 시간메타데이터 기대값은 fixture에서 보정 후 통과.
- 동일11개 중 실제 disposable DB 테스트: 전체migration 적용과20MiB 원본 저장, 유지20MiB+신규20MiB+1byte 거부, 정확40MiB 수정 성공·원본/수정 byte비교. 전용 임시DB/container만 정리 완료. HTTP endpoint 대신 store를 직접 호출한 DB 검증이다.
- 실제 HTTP multipart40MiB를 완료·사진수정 두 endpoint로 보내 scanner에 도달하고 fake 감염 판정422로 차단됨을2개 추가 테스트로 확인했다. 실제 운영 nginx/ClamAV 네트워크 검증은 미실행.
- Chromium 및 WebKit26.0 로컬 브라우저에서 사용자 첨부2개 JPEG의 미리보기2개·업로드 활성 확인.40MiB초과 오류·버튼 비활성도 확인. PC1440/모바일390의 합성 화면을 직접 열어 안내·버튼을 확인했다. 실제 아이폰 기기 검수와 운영 저장은 미실행.
- 독립 GPT-6 high 요청 reviewer photo_limit_review(실제모델 NOT_REPORTED) GO. 초기 DB5MiB 제약P1은0099와 실제DB 테스트로 해소. 원격 push/PR/main병합/공개배포 없음.

## 용량 정책 커밋 당시 남아 있던 호환성 결함

첨부 JPEG2개 모두 MPF와 HDR gain-map 표식을 포함한다. 변경 후 용량은 허용되고 브라우저 미리보기는 정상이나, 기존 서버JPEG 검사가 부가 이미지 구조를 거부한다. 이 변경은 용량정책만 구현하며 해당 원본의 최종 저장 성공 또는 전체 모바일 결함 해결로 표현하지 않는다. 민감메타데이터 제거/원본해상도 보존 계약을 유지하는 별도 보정과 검증이 필요하다.

## 후속 확장 — HDR JPEG·HEIC와 미리보기 오류 해결

2026-09-14 사용자 “다 해결해” 승인으로 앞서 남긴 HDR JPEG 저장 거부·HEIC 미지원·미리보기 무반응을 이 Change에서 이어서 수정한다. 기존 전체40MiB/최대5장·무재압축/원본해상도 계약과 악성코드 검사·민감메타데이터 제거는 유지한다. 원격게시·공개배포 승인은 별개로 아직 없다.

- HDR JPEG: MPF 색인으로 실제 각JPEG 경계를 검증한다. 색인과 맞지 않는 추가데이터·손상된 보조이미지는 거부하며 정상 기본/HDR 이미지의 인코딩 데이터는 보존한다. 메타데이터 제거 후 MPF 위치·길이를 다시 계산하고 HDR용 XMP만 제한적으로 보존한다.
- 첨부2개 원본은 변경 후 실제validator 통과. 각2개 프레임의 이미지 인코딩 구간 byte동일 확인. 실제파일은 commit/외부업로드하지 않는다.
- HEIC: 원본 HEVC 데이터는 재인코딩하지 않고 민감 메타데이터만 정리해 보관한다.0100은 두사진테이블의 MIME제약을 확장한다. 미리보기는 별도 표시용JPEG를 생성하고 서버에는 처리된HEIC원본이 저장된다.
- 미리보기: 준비 중/읽기 실패/표시 실패/60초지연 안내와 취소·재선택 시 늦은응답 무시. 인증된 오산 프로젝트 읽기 경계 및 기존업로드검사를 적용한 미리보기API, 저장사진은 기존 권한GET의preview옵션을 사용한다.
- 검증·독립검토 진행 중. 초기에 확인한HEIC 부가정보 처리/문자열경계 문제와미리보기watchdog 종료시점은 검토피드백에따라 보정하며, 모두검증한 후 완료판정한다.

### 후속 검증 및 검토

- 최신 validator로 실제 첨부 JPG 2개를 다시 읽어 모두 통과했고, 각 기본/HDR 2개 프레임의 인코딩 구간이 정리 전후 동일함을 확인했다. 파일은 로컬에서만 사용했다.
- HEIC 집중 7개 검증: 합성 192×192 HEIC의 해석·원본 HEVC 바이트 보존·가짜 기기 메타데이터 제거·표시용 JPEG 생성, hidden item·idat·MIME 이름 경계·멱등성·ICC/HDR, 외부 참조/미지원/손상 구조 거부. 실제 사용자 HEIC 원본이나 실제 아이폰 기기는 확보하지 않았다.
- Frontend 관련 39개 통과. 빌드/typecheck 통과, lint 오류 0·기존 main.tsx 경고 1. 미리보기 응답 후 이미지가 표시되기 전까지 60초 감시가 유지되는 회귀 검증 포함.
- Chromium/WebKit26.0에서 실제 JPG 2개 선택·미리보기·버튼 활성 및 40MiB 초과 차단을 확인했다. HEIC 표시 경로는 합성 응답으로 확인했고, 실제 HEIC 해석/저장은 별도 backend 검증 대상이다. 합성 PC/모바일 화면만 캡처·직접 시각 확인했다. 임시 preview runtime은 종료했다.
- 독립 reviewer mobile_photo_review GO: HEIC 개인정보 잔존·문자열 경계·hidden flag·ICC 호환성·중복 구조 처리 및 미리보기 감시 시점 Findings 모두 보정 확인. 요청 모델 GPT-6 high, 실제 모델은 NOT_REPORTED. HEIC 표시용 JPEG는 ICC가 있을 때 sRGB로 변환한 다음 메타데이터를 제거한다.
- 통합 테스트에서 새 POST 미리보기의 Audit registry 및 오산 capability 허용 목록 누락을 발견해 정확한 경로만 추가했다. 기존 인증·사업부·프로젝트 권한과 scanner 경계는 유지한다. Audit trigger 수는 기존 98개를 유지하고 최신 migration 기대값만 0100으로 갱신했다.
- 최종 격리 DB/API 25개 모두 통과(실패/skip 0): HDR/HEIC 구조·원본 보존, 정확40MiB/초과, 완료·수정·미리보기 HTTP40MiB scanner 경계, 실제HEIC preview POST·원본/표시GET·사업부/프로젝트 권한거부, HDR DB저장과 HEIC 수정이력 저장, 전체 migration 및 audit trigger98 검증. 전용 임시DB/container 정리 완료.
- 구현·관련 자동 검증·독립 코드 검토 완료. 용량정책4498e80에 이어 동일 branch에서 호환성 수정 로컬 커밋으로 보존한다. 원격 push·PR·main 병합·공개배포와 실제 아이폰 사용자 검수는 미실행이다.

## 통합 릴리스 후속 (2026-09-14)
- 사용자 HOLD/UI/QR 수정과 함께 원격 main 병합·공개배포 승인. 이전 미승인 기록은 당시 상태.
- 통합 migration은 0099 HOLD 다음 0100 사진40MiB, 0101 HEIC로 재번호. 기존 운영 migration은 변경하지 않음.
- 통합 FE44 및 격리 DB/API26 통과. 자세한 현재 릴리스 상태는 프로젝트 Change008 참조.
