# TASK-WORKFLOW-CONTINUITY-001 Change 011 사용자 검수 체크리스트

상태: `사용자 검수 대기`

고정 검수 환경:

- Frontend: `http://127.0.0.1:42983`
- Backend: `http://127.0.0.1:41166`
- 역할: 품질 검수 사용자

## 자동 검증 완료

- [x] LQC·OQC 판정 확정은 별도 임시저장 API 없이 응답과 판정을 한 요청으로 보낸다.
- [x] 부적합 응답을 합격으로 보내면 400이고 report version과 저장 응답 수가 변하지 않는다.
- [x] 같은 expected version으로 수정 재시도하면 확정된다.
- [x] 서버 field error가 열린 판정 dialog 안에 표시된다.
- [x] 빠르게 확정 버튼을 두 번 눌러도 요청은 한 번만 전송된다.
- [x] 기존 LQC→OQC, OQC→전진검수·필수 FAT, Pending 재검사 인계가 유지된다.

## 사용자 수동 검수

- [ ] 품질 사용자로 진행 중 OQC를 새로고침해 연다.
- [ ] 부적합 항목이 있으면 `판정 확정` dialog가 `부적합`을 기본 선택하고 합격 버튼을 막는지 확인한다.
- [ ] 조치 부서를 선택하지 않거나 근거가 부족할 때 dialog 안에 수정 방법이 표시되는지 확인한다.
- [ ] 조건을 충족해 확정하면 한 번의 클릭으로 Pending이 생성되고 검사 화면이 완료 상태로 바뀌는지 확인한다.
- [ ] LQC의 모든 적합 응답을 확정하면 새로고침 없이 완료되고 제조 완료 조건 충족 시 OQC로 인계되는지 확인한다.
- [ ] Desktop과 Mobile 390px에서 오류 문구·재시도 버튼·확정 버튼이 잘리지 않는지 확인한다.

## 게시 경계

- local experiment runtime만 검수한다.
- 대표 repo·`main`·Persistent UAT·실제 알림 provider는 이 체크리스트 범위가 아니다.
- push·PR·merge는 별도 사용자 승인 전까지 수행하지 않는다.
- `main` merge 승인 상태: `0/3`.
