# TASK-INTERIOR-BUSBAR-001 — 청주 인테리어 부스바

## 현재 범위·승인

### 2026-09-15 최신 검수·보류 상태

사용자가 주문·판매 API 검수 완료를 명시했다. Change025/027/028의 로컬 주문 자동 등록, 분할 출하 판매 및 품목 적요 변경을 포함한 주문·판매 API 사용자 검수 대기를 해소한다. 이는 사용자 검수 보고이며 에이전트가 이번 턴 실제 전표를 다시 대조한 것은 아니다. 발주 연동은 사용자 요청으로 추후 진행한다. 공식 문의2481480(2026-09-15 10:04:52 답변)은 발주 품목별 조회와 요청 프로젝트 필터 미지원을 확인했고 발주서현황 엑셀 활용을 안내했다. 행 식별자 질문은 수정 API 질문으로 해석되어 명확한 답을 받지 못했다. 기존 직접입력·PMS 양식 엑셀 기능과 ERP 내보내기 양식 자동 매핑은 구분한다.

업무별 입력 권한 분리(Change029)와 홈 개선(Change030)은 구현·관련 검증·로컬 적용을 마쳤다. 현재 Change031에서 QR 스캔 기반 개별 패널 출하 연결, 생산 완료 사진 잠금, 독립 프로젝트 상세 페이지를 구현한다. 완료 사진 교체를 허용했던 이전 계약은 이번 사용자 요청으로 대체하며 작업자 정정은 유지한다. 발주 API는 보류한다.

남은 범위: Change031 구현·검증 및 별도 승인 후 로컬 검수 DB migration/재시작, 실제 기기 촬영·QR 스캔·제품 매칭·일괄 QR 출력 현장 확인, Azure 운영 관리 ID 권한/인증 및 자동 게시 활성화, 운영 기준정보·기초재고·사용자 연결 준비, 최종 후보 전체 회귀 및 원격 CI/병합/배포. 기존 홈·생산계획·발주/입고·생산·기준정보 기본 업무는 재구현 대상이 아니다.


2026-09-14 이카운트 범위 확장: 사용자는 프로젝트 등록 시 주문서 입력, 프로젝트 완료 시 판매 입력을 추가로 구현하도록 요청했다. 기존 단방향/외부 송신 제외 계약은 이 두 입력 기능에 한해 변경한다. 최신 사용자 정정에 따라 단가는 제품군별로만 관리하며 프로젝트별 변경·단가 snapshot을 두지 않는다. 프로젝트 금액 조회는 제품군의 현재 단가를 사용한다. 원화·부가세 별도 10%. 거래처는 엘에스일렉트릭(주), 출하창고는 청주캠퍼스로 고정한다. 고객 업무번호는 W/O No이며 PO No는 입력하지 않는다. 아래 Change 021이 현재 진행 상태를, Change 020이 단가 계약을, Change 019가 외부 API 확인 사항을 소유한다.

2026-09-09 사용자가 대화에서 확정한 전체 신설 계획의 구현을 명시 요청했다. 범위는 전용 메뉴/권한/기준정보 → 구매·입고·재고 → 생산계획·사진 자동완료 → 프로젝트·분할출하 → 독립 QR 게시 → 기본 통합 검수 → 마지막 이카운트 발주 조회 연동이다. 실제 자원 생성·운영 migration·provider 호출·외부 게시·push/PR/merge/배포는 이번 로컬 구현 승인과 별개다.

2026-09-10 최신 게시 승인: 사용자가 “승인. 어차피 애저 공개배포할 때부터는 새로 시작이라서 언제부터 적용하나 상관없어.”로 자동 게시 적용 범위를 승인했다. Azure 운영 시작 시 기존 게시 대기 제품과 이후 완료 제품을 모두 대상으로 하며 시점 제한/과거 제품 제외 기능은 추가하지 않는다. 공개 내용은 앞뒤 사진·제조일시·작업자명이고 제품번호는 제외한다. 이 범위의 외부 게시 승인은 재요청하지 않는다. 운영 배포 선행 검증·해당 배포 실행 승인과는 구분하며, “새로 시작”을 검수/운영 DB 삭제나 초기화 승인으로 해석하지 않는다. 현재 로컬 worker는 비활성이고 운영 관리 ID/RBAC 적용·실제 인증 검증은 아직 미실행이다.

## 작업 기준

- source `f5b6f71` (시작 시 origin/main), branch `codex/interior-busbar`, worktree `/private/tmp/emi-interior-busbar`.
- 원본 checkout과 타 작업 WIP/runtime 보존. 현재 v2 Root/Backend/Frontend/Scripts 정책 적용.
- root: 조정, 독립 게시 provider·테스트, 통합 검증, 마지막 이카운트 연동. backend 담당: schema/core API/store/권한/tests. frontend 담당: UI/client/tests. 서로 다른 파일 소유.

## 보존할 업무 계약

- 활성 청주 조회, 별도 담당자·총괄만 입력/사유 정정. 외주 작업자 명단은 로그인 계정과 분리.
- 제품군 공용 수량재고, 제품군 하나/프로젝트 등록 건, 제품별 사진·번호 분리. 프로젝트명 중복 허용; 실제 확인 없는 제품번호 출하 귀속 금지.
- 도급/사급은 자재 속성, 공통 프로젝트 코드 대상 발주 자재 전부 관리. 발주 수신은 재고 불변, 분할 입고 시 증가.
- 버전 BOM으로 생산완료 자재 자동 차감. 자재 음수 허용, 완제품 음수 금지. BOM 미설정 제품은 완료 금지.
- 앞/뒤 사진 등록: 직접 촬영·기존 이미지 허용, 두 번째 필수 사진의 서버 등록 시각(한국시간 표시)이 제조시간. 두 장이면 자동 완료; 실제 작업자명과 등록 관리자 구분. 1장/업로드실패는 미완료. 생산·BOM snapshot·완제품/자재 증감 원자적 1회.
- 일별 제품군 목표, 계획 없음/초과 생산 허용. 부분 출하 시 재고·요청 잔여 초과 금지, 누적 전량 시 완료. 납기 자동출하/예약 없음.
- 기초재고·정정은 이력 보존. 취소는 과거 BOM으로 복원. 사진 정정·QR재출력은 재고·최초 제조시간 불변.
- 외부 사이트는 제품번호·앞뒤 사진·제조일시·작업자만 정적 게시. PMS 호출/로그인/내부 사진주소 의존 없음. 생산완료와 게시 분리, 성공 전 QR출력 금지, 재시도·최신revision 보호, 취소 시 공개중지.
- 이카운트 수신은 마지막 구현; 공통 코드 단방향 발주조회만. 반복수신 중복방지, 입고보존, 변경충돌표시. 외부 실제 사양을 검증하지 않은 wire contract를 사실로 만들지 않음.
- 현장 품질검사는 현행 유지, PMS 판정/성적서·실물반품/재작업/원가정산 제외.

## 검증·완료 조건

격리 합성 DB의 migration fresh/existing, 실제 서버 권한/타사업부 거부, 재고 경합·idempotency·snapshot취소·부분출하, 두 사진의 서버시간과 자동완료를 검증한다. 외부 provider는 fake/local로 독립 HTML·failure/retry/latest revision 확인. UI는 desktop/390px을 직접 확인한다. 권한/DB/동시성/공개 경계는 작성과 분리된 독립 검토 필요. 사용자 검수 뒤 최종 후보 전체 회귀 책임 실행 1회; required CI 별도 유지.

## 구현 결과·현재 상태

| 범위 | 현재 결과 |
| --- | --- |
| Task 1 전용 메뉴·권한·기준정보 | 구현 및 관련 자동 검증 완료. 청주 전용 메뉴/서버 권한, 담당자 role, 제품군·자재·외주 작업자·공통 코드·버전 BOM |
| Task 2 구매·입고·재고 | 구현 및 관련 자동 검증 완료. 직접/엑셀 발주, 분할 입고, 기초재고·보정·원장 역분개 |
| Task 3 생산·사진 | 구현 및 관련 자동 검증 완료. 날짜별 계획, 사진 두 장 자동 완료, 서버 제조시간, BOM snapshot, 작업자/등록 관리자 분리 |
| Task 4 프로젝트·출하 | 구현 및 관련 자동 검증 완료. 식별자 기반 엑셀 preview/apply, 제품군 공용 재고, 분할 출하·완료/취소 재개 |
| Task 5 독립 조회 사이트·QR | 실제 Azure 저장소·단일 실제 제품 공개 및 사용자 확인 완료(Change 014–016). 관리 ID 인증 코드·관련 테스트·독립 검토 완료(Change 017). 자동 게시 전체 대상 승인 완료. 운영 ID 권한/실인증 검증·활성화는 운영 배포 단계에 남음 |
| Task 6 기본 통합 검수 | 합성 DB·실제 HTTP 사진 API·mock 브라우저 흐름 검증 완료. 실제 기기 카메라·제품 매칭·프린터/QR 현장 검수는 대기 |
| Task 7 마지막 이카운트 연동 | **미구현·자료 대기.** 공식 세부 API 문서와 비식별 발주 응답 샘플을 요청했다. 실제 wire contract를 추측하여 연결하지 않았다 |

발주·자재의 이카운트 품목 코드와 공통 프로젝트 코드를 직접 입력할 수 있으며, 자동 연동 준비 미완료 상태를 화면에 표시한다. 공식 [이카운트 Open API 안내](https://www.ecount.com/kr/ecount/product/erp_open-api)에서 발주서 조회 제공과 로그인 후 세부 매뉴얼 확인을 검증했다(2026-09-09). Task 7은 발주/행의 안정적 식별자, 공통 프로젝트 필드, 수정·취소 표현, pagination/제한을 실제 문서·샘플과 대조한 뒤 진행한다. 완료한 내부 업무에 실제 연동 완료 표시를 붙이지 않는다.

## 검증 증거

- Backend Release build 및 `FullyQualifiedName~InteriorBusbar|FullyQualifiedName~AuditMutation`: **36개 통과, 실패/skip 0**. 전용 `busbar_test` DB(127.0.0.1:55490)의 매 테스트 고유 schema와 합성 계정만 사용했다.
- 추가 DB 경계 테스트 **2개 통과, 실패/skip 0**: 게시 worker에 오산/Directory identity DB를 연결하면 제품을 읽거나 외부 sink를 호출하기 전에 거부. 서버 관련 검증 총 **38개**(36개 실행 + 신규 경계 2개 실행).
- 실제 HTTP 검증: 익명 차단, 기존 부서 입력 거부, 담당자 입력/권한 회수 즉시 거부, trusted OSAN 컨텍스트의 GET/POST가 실제 사업부 middleware에서 403. 실제 multipart의 잘못된 이미지 거부 → 앞면 미완료 → 뒷면 자동 완료 → 제조시각/등록자/재고 → 중복 재고 방지 → 미게시 QR 409.
- 재고 검증: BOM 당시 소모량으로 취소, 자재 음수, BOM 누락 rollback, 30개 재고 동시 20개 출하 경쟁, 60개를 20개씩 3회 출하 및 취소 후 재개, Excel 전부 적용/전부 rollback, 계획 변경 전 값 보존.
- 게시 검증: 실패해도 생산 유지, 정정 후 최신 내용 재시도, 동일 토큰으로 취소 페이지, safe error, HTML escape, 한국시간, Azure conditional header/412, 지연된 과거 PUT보다 최신 PUT이 먼저 저장되면 과거 요청 거부.
- 독립 페이지: 생성 HTML을 `file://`로 별도 Chromium에서 열고 모든 HTTP(S)를 차단. 외부 요청 **0**, 두 이미지 로딩(각 320px) 성공, 제조시간 표시, 390px 가로 넘침 없음. 합성 증거 `/private/tmp/emi-busbar-public.html`, `/private/tmp/emi-busbar-public-390.png` 직접 확인. Azure 실제 게시/네트워크 장애 검수를 대신하지 않는다.
- Migration: 격리 컨테이너 `emi-busbar-test-20260909` 안의 별도 생성 DB에서 **fresh 90개 / 기존 0089→0090 업그레이드** 적용 통과. 기존 sentinel 보존, 담당자 role·설정 singleton 확인. 스크립트 `/private/tmp/emi-busbar-verify-migrations.py`; 검증용으로 생성한 두 DB만 종료 후 삭제했다.
- Frontend: `pnpm typecheck`, `pnpm build`, 변경 파일 ESLint 통과. 기존 bundle 크기 경고는 유지. Playwright 기본 5개 통과 후 리뷰 영향 신규 2개(BOM 최신값/미완료 정정·취소) 통과. PC/390px과 사진 완료 화면 직접 시각 확인. 합성 증거 `/private/tmp/emi-busbar-*.png`.
- 사용자 검수 후의 최종 후보 전체 회귀와 required CI는 아직 실행하지 않았다.

## 독립 검토

요청 모델 GPT-6 Astra/high, 도구의 실제 모델 별도 관측값은 `NOT_REPORTED`. 작성과 분리된 `/root/busbar_review`가 base `f5b6f71`부터 누적 WIP/신규 파일을 검토했다. P0/P1 없음. P2 BOM 편집값의 과거 버전 잔류, P2 Draft 정정·취소 미노출, P3 계획 감사 before 누락을 모두 보정했다. 수정분 재검토에서 모두 해소, 새 Finding 없음. 테스트 실행은 parent/구현자의 증거이며 reviewer가 별도 실행했다고 기록하지 않는다.

재검토 내용 hash: page `b6e29c1441c1b1fda687dfc3c2150bf6d2eabaed45160bb71119a8cff4b47d6c`, store `842e5bfecc5a30892cf2985ca4173f27912f574806f5ef77353aaef8e1f31705`, migration `8f07b337b72f9ed65283a81df11e51fd0e3d17e6a17f70e3fb17691e68f92245`.

## 사용·운영 준비

1. 승인된 대상에 migration 배포 후 관리 화면에서 별도 `인테리어 부스바 담당자` 역할을 부여한다. 기존 부서 역할만으로 입력 권한이 생기지 않는다.
2. 기준정보에서 제품군·자재 코드/단위/사급·도급·외주 작업자·공통 프로젝트 코드를 입력한다. 제품군당 1개 기준 소요량을 설정하고 도입 전 재고를 기초재고로 등록한다.
3. 생산·사진 화면에서 제품군/실제 작업자를 고르고 앞·뒤 사진을 등록한다. 두 번째 등록으로 완료되며 게시 상태가 성공하면 같은 제품번호의 QR을 출력한다. 납품 프로젝트에서 실제 출하 수량을 나눠 입력한다.
4. 잘못된 기록은 사유를 입력해 정정·취소한다. 완료 제품 사진 변경과 작업자 정정은 기존 제조시간을 보존하고 같은 공개 주소를 다시 게시한다.

외부 게시 기본값은 비활성이며 ReviewSafe에서는 켜지지 않는다. 실제 게시 승인을 받은 뒤 **PMS 사진 저장소와 별개인 Azure Storage 계정**의 정적 웹사이트를 준비한다. HTML에 정규화된 사진 두 장을 포함하여 한 blob의 조건부 교체로 사진/본문 버전을 일치시킨다. 공개 페이지/사진은 PMS 서버에 의존하지 않는다.

설정은 secret-safe 환경 구성으로 제공한다(실제 비밀/URL 값을 저장소에 기록하지 않음).

- `InteriorBusbar:Publication:Enabled`: 명시적 `true`로 게시 worker 실행.
- `InteriorBusbar:Publication:PublicBaseUrl`: 해당 계정의 `https://<account>.z<번호>.web.core.windows.net/` 기본 주소.
- `InteriorBusbar:Publication:BlobEndpoint`: 같은 계정의 `https://<account>.blob.core.windows.net/` 기본 주소.
- `InteriorBusbar:Publication:SasToken`: `$web` 게시 blob의 HEAD와 생성/교체에 필요한 읽기·생성·쓰기 범위의 비밀. 별도 secret 저장소로 공급하며 로그·Task에 남기지 않는다.

worker는 청주 DB만 명시적으로 선택하고 사업부 identity를 확인한다. 5초마다 대기 제품 한 건을 처리하며 실패 건은 내부에서 재요청한다. 현재는 최신 내용 보장을 위해 게시 중 동일 재고 mutation lock을 최대 게시 timeout 20초 동안 보유하므로 게시 장애 시 입력 지연이 생길 수 있다. 성공 전 QR 출력은 제공하지 않는다. 실제 Azure 설정/접근권한/만료/게시 실패 모니터링은 배포 전에 확인해야 한다.

## 남은 일·Git 상태

- Task 7: 세부 API 문서와 비식별 응답 샘플 확보 → 필드/식별자 확정 → 단방향 조회/중복·변경·취소 충돌/재시도 구현 → 합성 및 승인된 연동 환경 검증.
- 사용자 검수: 실제 촬영/사진 선택/제품 매칭/임시 스티커·라벨 출력 동선, 청주 담당 권한, 업무 수량 검수.
- 실제 Azure 독립 사이트 준비·게시, 공유 runtime handover, 운영 migration/배포는 미실행. 생성한 로컬 검증 자원 외 실제 provider/운영 데이터 변경 없음.
- branch `codex/interior-busbar`; 검증·독립 검토를 마친 내부 업무 변경은 로컬 commit으로 보존한다. push/PR/merge/운영 배포 미실행. 원본 checkout의 타 작업 WIP는 보존했다.

## 2026-09-09 로컬 검수 서버 공개

사용자가 “서버 열어서 보여줘”라고 요청하여 `8b15e48` source를 별도 검수 서버로 실행했다. Backend `http://127.0.0.1:5097`, Frontend `http://127.0.0.1:5197/interior-busbar`. DB는 이 작업 소유 컨테이너 `emi-busbar-test-20260909`의 신규 `busbar_review_20260909`이며 기존 테스트 DB 및 공유/운영 DB는 변경하지 않았다. 전체 migration 및 개발용 합성 계정 seed 후 readiness OK, 부스바 workspace 200/canWrite=true 확인. 부스바 기준정보/업무 데이터는 빈 상태다.

Codex 브라우저에서 개발 검수 계정 `dev-admin`을 선택하고 전용 화면 6개 탭·빈 현황 표시를 확인해 열어 두었다. 외부 게시·이카운트·알림 발송 worker는 비활성. Backend PID 43408, Frontend launcher PID 43409. 로그/PID 파일은 `/private/tmp/emi-busbar-review-{backend,frontend}.{log,pid}`. 다른 작업의 5096 서버는 보존했다. 사용자 검수 완료를 의미하지 않으며 원격/운영 배포는 없다.

## Change 002 — 계획에서 미완료 제품 선생성

사용자가 확인 후 수정 구현을 승인했다. 계획 수량만큼 작업자·사진·정식번호 없는 제품을 먼저 생성하고, 관리자가 기존 제품을 열어 실제 작업자와 앞·뒤 사진을 입력한다. 두 장 등록이 완료될 때 기존 제조시간·재고 원자처리와 제품번호 발급을 유지한다. 생성/촬영 UI를 분리하고 완료번호를 눈에 띄게 표시한다.

계획 재저장은 중복 생성하지 않는다. 수량 증가는 차이만 추가하고 감소는 작업자/사진 등록 전 미완료 제품만 취소 이력으로 보존한다. 작업을 시작한 제품 수 아래로 줄이거나 생성 제품의 제품군/계획일을 바꾸는 요청은 거부하여 기존 실물 식별을 보존한다. 기존 수동 생성/완료 제품은 임의로 계획에 연결하지 않는다. 0090은 이미 검수 DB에 적용되었으므로 additive 0091을 사용한다. 적용 대상은 현재 5097/5197의 별도 합성 검수 환경이며 외부 게시·이카운트는 계속 비활성이다.

상태: 구현·관련 검증 진행 중. 기존 사용자 검수 기록과 타 작업 서버는 보존.

Change 002 결과: 구현·관련 자동 검증·독립 검토 완료, 현재 검수 서버 반영. 계획 저장 시 수량별 Draft 생성, planId 필터/임시 순번, 사진 등록 시 작업자 선택, 두 장 완료 번호 강조와 화면 이동을 적용했다. Backend 관련 41개 통과(실패/skip 0), frontend 기본/신규 9개 및 리뷰 반례 2개 통과, typecheck/대상 ESLint 통과. 0091을 포함한 migration 91개 fresh/upgrade 적용과 기존 sentinel 보존을 확인했다. 독립 reviewer `/root/plan_review`(요청 Astra/high, 실제 모델 NOT_REPORTED)의 유일한 P2 R1(업로드 중 계획 변경 후 과거 조회가 덮음)은 최신 load ref로 보정하고 필터 직접 변경/탭 제품보기 두 반례를 통과했으며 재검토에서 해소 판정했다.

5097 Backend를 새 binary로 갱신(PID 47761, 개발 계정 재seed 비활성)하고 readiness와 실제 API를 확인했다. 기존 계획 30개에 미착수 제품 30개를 준비했고 기존 수동 제품 2개의 상태·번호·미연결 상태를 보존했다. 현재 5197에서 계획→제품 보기→작업자 선택·앞뒤 사진 화면을 직접 열어 확인했다. 제품 선택 시 사진 영역으로 이동하고 고정 헤더가 내용을 가리지 않게 위치를 조정했다. 실제 사진을 대신 등록하거나 기존 제품을 완료/취소하지 않았다. 사용자 최종 검수·원격 push/merge·실제 외부 게시/배포는 여전히 미실행이다.

## Change 003 — 제품군 중심 현황·출하·생산계획

2026-09-10 사용자 확인 및 구현 승인. 종합현황 전체합산 KPI 4개 제거, 제품군별 표·부족품목 유지. 프로젝트 행 전체/키보드로 상세 펼침, 내부 편집 버튼은 분리. 상세에 공용 재고·요청/출하/잔여·최대 출하 가능량 min(재고,잔여)와 날짜별 생산계획/완료/잔여 표시. 생산 사진 목록은 제품군·계획 날짜 범위·상태로 서버 페이지 전 필터. 생산계획은 제품군×주간 7일 표, 완료/계획 및 남은 수량 표시, 셀에서 해당 제품군/날짜 사진 목록으로 연결. 기존 입력/권한/재고/번호·사진 업무는 보존하고 schema 변경 없이 진행한다.

구현·관련 자동 검증·독립 검토 완료. 기존 5197/5097 별도 검수 서버에 반영했으며 사용자 입력과 실제 provider 비활성을 유지했다. DB schema 변경·seed·기존 업무 데이터 변경은 없다. 5097은 최종 Release binary로 갱신(PID 5686), 5197 실제 화면에서 제품군별 주간 표·계획 셀의 제품군/날짜 필터·프로젝트 일반 셀 클릭·상세 재고/잔여/최대 출하량을 확인했다.

검증: Backend 부스바/AuditMutation 관련 43개 통과(실패·skip 0, `/private/tmp/busbar-family-final-tests.log`), frontend 합성 UI 14개 통과, 대상 ESLint·TypeScript 통과. 1440px/390px 주간 표·프로젝트 상세를 구현자가 직접 확인했고 모바일은 표 내부 가로 스크롤을 제공한다. 페이지 전 서버 필터와 count 일치, 날짜 양끝 포함, 잘못된 날짜/상태 거부, 신규 제품 상세 GET 오산 거부 및 사진·번호 조회를 검증했다. 전체 회귀는 사용자 검수 후 최종 후보에서 수행한다.

독립 reviewer `/root/change003_review`(별도 맥락, 상속 모델·실제 모델 NOT_REPORTED)는 누적 Change 003 diff에서 P2 두 건을 발견했다. R1 미완료 필터에서 두 번째 사진 완료 후 번호가 사라지는 문제는 필터 목록과 별도 제품 상세 조회로 보정하고, 늦은 상세 응답 폐기 및 완료번호 유지 UI 반례를 통과했다. R2 새 제품군/날짜 경로에서 대기 순번 정렬을 잃는 문제는 계획일·제품군·대기 순번·ID 정렬과 페이지 경계 반례로 보정했다. 두 보정 재검토에서 해소 및 추가 finding 없음 판정. 기존 늦은 사진 업로드 중 필터/탭 변경 반례도 유지했다.

사용자 검수 항목: 주간 표에서 원하는 제품군/날짜 클릭 후 작업자·사진 입력, 미완료 필터에서도 두 장 등록 직후 제품번호 확인, 프로젝트 행 펼침 후 공용 재고·생산 예정과 현재 최대 출하량으로 수량 판단. 구현/자동 검증과 사용자 최종 수락은 별개이며 사용자 검수·원격 push/merge·운영 배포·실제 외부 게시 미실행.

## Change 004 — 날짜 선택형 생산계획 캘린더

2026-09-10 사용자 요청: 생산계획 탭에 캘린더를 넣고 날짜를 지정하여 제품군별 계획을 입력. 기준선 `a0525e3`, branch `codex/interior-busbar`. 월간 달력과 월 이동·제품군 필터, 날짜별 제품군 계획/완료 표시, 선택 날짜의 제품군별 신규 등록·기존 수정 동선으로 변경한다. 날짜·제품군이 미리 지정된 편집에서 수량을 저장한 뒤 캘린더를 유지하여 다른 제품군도 이어 입력할 수 있다. 제품 목록 이동은 별도 동작으로 제공한다. 기존 계획 저장에 따른 제품 생성·수량 보정·사진·번호·재고 및 서버 권한 계약은 그대로 사용한다.

프론트엔드 한정 구현·관련 검증 완료, 기존 5197 서버에 반영. 월간 캘린더 날짜 선택 시 상세로 포커스·스크롤 이동하고 제품군별 등록/수정에서 목표 수량에 바로 입력한다. 저장 후 같은 날짜 상세로 복귀하며 달력으로 돌아가기와 별도 제품 보기를 제공한다. 공용 편집기는 첫 활성 필드에 포커스를 맞추어 날짜·제품군이 고정된 새 동선을 지원한다. API/DB/schema/provider 변경은 없고 기존 검수 데이터도 수정하지 않았다.

검증: 합성 UI 15개 전체 통과, 포커스/CSS 보정 후 관련 2개, 최종 첫 활성 필드 보정 후 기존 출하·작업자 정정·캘린더 등록 3개 통과. 관련 ESLint·TypeScript 통과. 연말/연초 이동, 윤년 2월 29일/평년 28일, 날짜/제품군 고정 POST, 동일 ID 수정, 저장 후 달력·선택 날짜 유지, 기존 완료번호 및 필터 반례를 확인했다. 1440px/390px 최종 이미지(`/private/tmp/emi-busbar-month-calendar-1440.png`, `…-390.png`)를 구현자와 parent가 직접 확인했고 모바일은 달력/상세 표 내부 가로 스크롤을 사용한다. parent가 누적 diff와 실제 5197의 빈 날짜 선택→제품군 등록→날짜/제품군 자동 입력·수량 포커스→닫기·달력 복귀를 확인했다. 가역적 단일 FE 화면 변경으로 parent 직접 검토와 관련 검증을 적용했다.

사용자 검수: 원하는 월/날짜 선택→제품군 계획 등록 또는 수정→수량 저장→같은 날짜의 다른 제품군 입력. 사용자 최종 수락·원격 반영·운영 배포는 미실행이며 전체 회귀는 최종 검수 후 후보에서 수행한다.

## Change 005 — 계획 입력 팝업·달력 높이·프로젝트 현황

2026-09-10 사용자 요청, 기준선 `93979fc`. 캘린더 날짜 클릭 시 날짜별 제품군 계획을 팝업 안에서 등록·수정하며 저장 후 팝업을 유지한다. 달력의 모든 날짜 칸(서로 다른 주 포함)은 표시 중 가장 큰 내용에 맞춰 같은 높이를 유지한다. 종합 현황에 납품 잔여가 있는 진행 프로젝트를 납기 오름차순으로 표시한다. 기존 생산/재고/사진/번호/권한 계약을 유지하는 FE 한정 변경이다.

구현·관련 검증 완료, 기존 5197 서버에 반영. 공용 DsDialog 안에 날짜별 제품군 목록과 수량 편집을 넣고 저장 후 유지한다. 팝업은 키보드 포커스 순환·Escape 닫기·선택 날짜 포커스 복귀를 지원하며 저장 중 닫기와 제품 보기 이동을 막는다. 실패 시 팝업 안에서 오류와 입력값을 유지한다. CSS grid의 동일 비율 행으로 월 전체 날짜(월 바깥 칸 포함)를 가장 큰 내용과 같은 높이로 표시한다. 종합 현황의 진행 프로젝트는 잔여>0 조건과 납기/이름/ID 순으로 정렬하고 행에서 기존 프로젝트 상세로 연결한다.

검증: 관련 UI 18개 실행 중 17개 통과, 팝업 실패 후 닫기에서 이전 오류 알림이 포커스를 빼앗는 1개 실패를 발견해 팝업 닫기 시 피드백 정리로 보정했다. 해당 반례와 월 경계·등록/수정 2개 재실행 통과. 최종 ESLint·TypeScript·diff 검사 통과. 합성 5제품군 긴 이름을 다른 주 날짜에 배치해 PC1440/390에서 35칸 높이 차이 1px 미만과 제품군 필터 후 축소를 확인했다. 완료 프로젝트 제외·납기순·동일 납기 순서·프로젝트 상세 이동을 검증했다. 구현자와 parent가 popup 및 동일 높이 이미지(`/private/tmp/emi-busbar-plan-popup-{1440,390}.png`, `/private/tmp/emi-busbar-calendar-equal-{1440,390}.png`)를 직접 확인했다. parent는 누적 diff 검토와 실제 5197에서 종합현황 표·날짜 팝업·수량 입력·Escape 닫기를 확인했다. 실제 입력 저장은 하지 않았으며 사용자 검수 데이터와 API/DB/provider는 변경하지 않았다.

사용자 검수 대기: 날짜 클릭→팝업에서 제품군 선택·수량 입력, 계획이 많은 날짜와 다른 주의 높이 비교, 종합현황 프로젝트 납기순 확인. 원격 반영/운영 배포 미실행, 전체 회귀는 사용자 검수 후 최종 후보에서 수행한다.

## Change 006 — 사진 등록 팝업·조회 전용 현황·프로젝트 상태 필터

2026-09-10 사용자 확인 후 구현 승인, 기준선 `99bee06`. 생산·사진·QR 목록 맨 앞 열에 사진 등록/보기 버튼을 배치하고 팝업 안에서 작업자·앞뒤 사진과 완료 제품번호를 표시한다. 기존 정정·QR·취소 기능과 필터 밖 완료 제품 상세 보존은 유지한다. 종합 현황 프로젝트 행은 탭 이동 없는 조회 전용으로 바꾸고 납품 프로젝트는 전체/진행 중/완료 상태 필터를 기존 검색과 함께 적용한다.

FE 구현·관련 검증 완료, 기존 5197 서버에 반영. 공용 팝업 안에 작업자·앞뒤 사진·완료번호·정정·취소·QR 미리보기를 배치했다. 업로드 중 닫기 제한, Escape 및 원래 버튼 포커스 복귀를 지원하며 완료 후 필터에서 행이 사라진 경우 목록 제목으로 돌아간다. QR 미리보기는 현재 선택한 제품 상세의 버전·게시 상태를 검사하여 필터 목록 밖에서도 유지한다. 종합현황 행 action을 제거하고 프로젝트 상태 전체/진행 중/완료와 텍스트 검색을 AND 적용했다.

검증: 합성 UI 21개 전체 통과, 최종 포커스 보정 후 관련 3개와 늦은 상세 응답/포커스 2개 재검증 통과. ESLint 경고 없이·TypeScript·diff 검사 통과. 기존 업로드 지연 반례는 업로드 중 팝업 닫기 차단→완료 후 필터/계획 이동 동선으로 갱신했고, 이전 제품 GET 응답이 새 제품 팝업을 덮지 않는 별도 반례를 확인했다. Draft 필터 완료번호 유지, 첫 열 사진 버튼, 종합현황 클릭 무동작, 프로젝트 상태+검색을 검증했다. 구현자가 PC1440/390 사진 팝업을 직접 확인하고 parent는 실제5197 팝업 및 합성390 이미지(`/private/tmp/emi-busbar-photo-popup-390.png`)를 직접 확인했다. print media 라벨 표시 확인은 합성 이미지 fixture로 수행했으며 실제 QR 인쇄/스캔 검증은 하지 않았다. parent가 누적 diff 및 실제5197 overview조회전용·프로젝트상태선택·첫열사진버튼·worker/사진팝업·닫기를 확인했다. 실제 사진/작업자/출하 데이터를 변경하지 않았고 API/DB/provider 변경도 없다.

사용자 검수 대기: 사진 팝업 입력과 두 장 완료번호, 종합현황 클릭 무이동, 프로젝트 상태 필터 확인. 원격 반영/운영 배포 미실행, 전체 회귀는 사용자 검수 후 최종 후보에서 수행한다.

## Change 007 — 필터 배치 정리

2026-09-10 사용자 요청, 기준선 `8731ef6`. 필터의 라벨/입력 높이·간격·너비와 액션 버튼 정렬을 통일한다. 현재 5197 사진 화면에서 검색/제품군 사이에 이동 버튼이 끼고 버튼·입력 바닥선과 폭이 불규칙한 것을 직접 확인했다. 전용 필터 스타일로 프로젝트·계획·사진 및 관련 조회 도구를 정리하고 기존 모달/입력 폼/페이징과 업무 동작은 유지한다. PC/390px 줄바꿈 및 기존 관련 UI 검증을 수행한다. API/DB/provider와 실제 검수 데이터 변경은 없다.

구현·관련 검증 완료, 5197에 반영. 프로젝트·월간 계획·생산 사진·발주·소요량 제품군 선택에 전용 filterbar를 적용했다. 입력/선택/버튼 44px, 라벨 간격 6px, 항목 간격 12px로 맞추고 날짜쌍을 인접하게 유지했다. 이동/초기화는 필터 뒤 액션으로 분리했다. 1280/700px 기준으로 PC/중간 폭/모바일 배치를 조절한다. 모달·등록 폼·출하 액션·페이징 및 필터 동작은 보존했다.

관련 기존 UI 4개 통과, 최종 1440/1200/1101/390px × 6탭 시각 검증 및 page overflow 0 확인. ESLint·TypeScript·diff 검사 통과. 구현자는 각 폭 이미지를 직접 확인했고 parent는 실제 5197 전후와 최종 합성1440/390 사진필터 이미지(`/private/tmp/emi-busbar-{1440,390}-생산-사진-QR.png`)를 직접 확인했다. parent 누적 diff 검토 중 중간 폭의 최소 그리드 폭 위험을 찾아 1280px 전환으로 보정하고 해당 폭을 검증했다. 사용자 검수 대기, 원격 반영/운영 배포 미실행.

## Change 008 — 사진 진행 단계·자동 QR·일괄 라벨 인쇄

2026-09-10 사용자 확인 후 구현 승인, 기준선 `19b9c40`. 오늘은 파스텔 빨간색, 선택일은 구분 표시. 목록은 선택 체크박스와 사진등록/제품군/작업자/생산일시/생산 상태/제품번호/외부게시 순서로 표시한다. 사진등록 팝업은 작업자·앞뒤 사진 입력 중심, 기존 정정·취소 등은 별도 제품번호 관리에서 유지한다. 사진 없음/한 장/두 장에 따라 사진 등록 전/1차 사진 등록 완료/생산 완료를 표시하고 취소 상태는 사실대로 유지한다. 두 번째 필수 사진 등록 완료 시 기존 원자적 생산·재고·정식 번호 발급을 유지하면서 QR을 생성·보존한다. 완료 전에는 등록 대기 번호를 표시한다.

외부게시 최신 버전 완료 제품을 여러 개 선택해 QR+제품번호 라벨을 일괄 인쇄한다. 준비 실패 시 일부만 누락된 출력을 제공하지 않으며 출력 직전 상태·버전을 재확인한다. QR 목적은 출력이고 사진등록 팝업과 출력 동선을 분리한다. 공개주소가 미설정인 환경에서는 생산을 막거나 가짜주소를 발급하지 않고 QR 설정 대기로 표시한다. 실제 외부게시 성공 전 출력 금지 기존 계약은 사용자 확인대로 유지한다.

구현·관련 자동 검증·독립 검토 완료, 기존 5197/5097 검수 환경 반영. 두 번째 사진 완료 트랜잭션에서 공개주소가 설정된 경우 QR PNG와 고정 주소를 저장한다. 기존 완료 건은 출력 조회 시 한 번만 보완하며 ReviewSafe 조회는 DB 읽기 전용으로 유지한다. 공개주소 변경·게시 대기·오래된 게시 버전·취소 제품의 출력을 거부한다. 공개주소 미설정 시 생산/번호/재고는 완료되고 QR 설정 대기를 표시한다.

일괄 출력은 선택 제품 전체 준비와 최신 상태 재확인, 실제 라벨 이미지 디코딩을 마친 뒤에만 인쇄 영역을 활성화한다. 일반 미리보기는 브라우저 인쇄에서도 숨기고 afterprint/닫기/실패 시 인쇄 영역을 해제한다. 사진 입력 팝업과 제품 관리·일괄 인쇄 팝업을 분리하고 필터 밖 완료 제품번호 보존을 유지했다.

검증: backend 관련 45개 통과(실패·skip 0), ReviewSafe 보정 후 QR 관련 3개 통과(실제 HTTP 읽기 전용 반례 포함). PNG를 디코딩해 대상 주소를 확인했고 동시 보완·게시 최신성·설정 없음·취소 및 재고 불변을 검증했다. frontend 합성 UI 25개 통과, TypeScript·대상 ESLint·diff 검사 통과. 정확한 헤더/사진 단계, 여러 라벨, 일부 준비 실패, 준비 중 정정/취소, 인쇄 전 이미지 준비와 afterprint 해제를 포함한다. PC1440/390 팝업과 print media를 직접 확인했고 parent는 실제 5197 목록과 오늘 색상·두 QR 라벨 이미지를 확인했다. 실물 프린터 출력·스캔은 미실행이다.

0092 additive migration은 격리된 합성 DB 두 개에서 fresh 92개 전체 및 0091→0092 업그레이드를 검증했다. 기존 검수 DB에 적용 전후 기존 부스바 18개 테이블의 행 수·해시가 동일함을 확인했다. 최종 backend PID 19442, 재기동 시 자동 migration/seed 비활성, 외부 provider 비활성 유지. 현재 검수 환경에는 공개주소가 없어 실제 QR 생성·출력은 설정 대기이며 합성 공개주소로 자동 생성·일괄 인쇄를 검증했다.

독립 검토는 새 reviewer 생성이 agent 한도로 실패하여 기존 `/root/change003_review` 맥락을 재사용했다(실제 모델 NOT_REPORTED, 새 모델 override 없음). P2 R1 ReviewSafe GET의 legacy QR 쓰기는 읽기 전용 분기로, P2 R2 일반 브라우저 인쇄의 최신성 확인 우회는 검증 후 인쇄 영역 활성화로 보정했다. 최종 재검토에서 두 finding 해소 및 추가 P2 이상 없음 판정.

사용자 검수 대기: 오늘 표시, 사진 단계와 두 장 완료번호, 공개주소·게시가 준비된 환경에서 여러 제품 선택→선택 QR 인쇄→실물 라벨 부착. 전체 회귀는 사용자 검수 후 최종 후보에서 수행한다. 사용자 실제 사진/작업자/출하 기록을 대신 수정하지 않았고 원격 push/merge·실제 외부게시·자원 생성·운영 배포는 미실행이다.


## Change 009 — 조회 텍스트·재고 열·선택 및 납기 색상

2026-09-10 사용자 확인 후 구현 승인, 기준선 `240c94d`. 제품번호 관리 팝업과 진입 버튼을 제거하고 사진 보기만 유지한다. 프로젝트명은 일반 텍스트로 표시하고 행 펼침은 유지한다. 상세 최대 출하 가능 KPI 제거, 날짜별 계획에 현재 제품군 공용 재고 열 추가. 캘린더 선택은 파스텔 회색(오늘 빨강 유지), 종합현황 D-3부터 당일까지 진행 프로젝트는 노란색으로 표시한다. FE 한정 구현·관련 검증 및 PC/390px 시각 확인을 진행하며 API/DB/provider·사용자 데이터는 변경하지 않는다.


구현·관련 검증 완료, 기존 5197 Vite 검수 서버에 반영. 제거한 제품번호 관리 팝업의 정정/취소/게시 재시도 UI도 함께 제거했고 사진 입력·보기와 일괄 QR 출력은 유지했다. API/권한/DB 계약은 변경하지 않았다. 표 재고 열은 날짜별 예정량이 아닌 현재 제품군 공용 재고를 표시한다.

합성 UI 25개 실행에서 23개 통과 후, 프로젝트명 버튼을 기대하던 기존 검증과 공용 표 스타일에 가려진 노란색 우선순위를 보정하고 해당 2개 재검증 통과. TypeScript·대상 ESLint·diff 검사 통과. 초기 5197 테스트 시도는 합성 API 주소 설정 불일치를 확인해 중단했고 기존 5196 합성 전용 설정으로 검증했다. 초기 시도에서 실제 사진·출하 저장은 실행하지 않았다. PC1440/390 프로젝트 상세·납기 임박 행·캘린더 이미지를 직접 확인했다. 오늘 빨강, 선택일 회색, D-3/당일 포함 및 D-4/지난 납기 제외, 프로젝트 행/키보드 펼침과 제품번호 일반 텍스트를 검증했다. 사용자 검수 대기이며 전체 회귀·원격 반영·운영 배포는 미실행이다.


## Change 010 — 작업자 정정·게시 재시도 위치

2026-09-10 사용자 지정: 작업자 정정은 사진 보기, 생산 취소 UI는 제공하지 않음, 게시 재시도는 외부게시 열. 기존 서버 API를 재사용하고 사유·권한·제조시간 보존 계약을 유지한다. 실제 외부 자원 생성은 승인되지 않았으며 Azure 비용 추정만 수행한다.

구현·관련 UI 3개(정정 사유 저장/외부게시 열 재시도/읽기 전용/사진 팝업 경계) 및 TypeScript·ESLint 통과. PC1440/390 합성 팝업을 직접 확인했다. 5197에 반영, API/DB/실제 provider 변경 없음. 생산 취소 UI는 계속 없음. 사용자 검수·원격 반영·운영 배포 미실행.

비용 추정 근거(2026-09-10): Microsoft Retail Prices API 한국 중부 General Block Blob v2/Hot LRS 조회, GB·월 $0.02, 쓰기 1만회 $0.05, 읽기 1만회 $0.004. 공개 자료 3MB/제품, 월 조회수=월 생산량×5, 1달러=1,400원 계산용 가정, 세금/PMS내부비용/별도 CDN·도메인·로그 제외. 5년 누적 월1000개=180GB 저장·15GB월송신, 월5000개=900GB·75GB, 월10000개=1800GB·150GB. 인터넷 송신 무료100GB 가용/미가용 두 경우로 추정한다. 보관기간 자동 삭제 정책 확정이나 실제 자원 생성 승인은 아님.


## Change 011 — 외부 QR 페이지 디자인 시안

사용자 요청에 따라 공개 제품 페이지 1차 HTML 시안을 frontend/public/interior-busbar-preview.html로 작성했다. 제품번호·제조일/시간/작업자·앞뒤 사진, 모바일 세로/PC 두 열 구성. 가상 데이터와 명시된 SVG 샘플을 사용했으며 실제 게시 renderer/업무 데이터/외부 자원은 변경하지 않았다. 5197 별도 미리보기 주소 제공. 390/1440px 이미지 로딩·가로 넘침 0 확인 및 두 이미지를 직접 시각 검토했다. 디자인 사용자 검토 대기이며 승인 후 실제 공개 페이지 생성 코드에 적용할 수 있다.


## Change 012 — 공개 HTML 제품번호 제외

사용자가 프로젝트명 기반 번호·출하 시 제품 연결 제안을 철회하고 공개 HTML에서 제품번호만 제외하도록 요청했다. 기존 내부 제품번호 발급·QR 라벨·고정 주소·생산 완료 게시 시점과 수량 기반 출하를 유지한다. 디자인 시안에서 제품번호를 제조정보 제목으로 대체하고 번호 대조 안내를 제거했다. 실제 공개 HTML renderer에서도 제품번호를 제외한다. 외부 게시/자원 생성은 수행하지 않는다.

정상/중지 HTML 렌더링 2개 통과(skip 0), 제품번호 미포함·사진/제조정보 보존 확인. 390/1440 시안 직접 시각 확인 및 diff 검사 통과. 미리보기 반영 완료. 실제 게시 renderer source는 수정·검증했으며 backend runtime binary 갱신/외부 게시는 미실행(현재 게시 비활성). 사용자 디자인 검토 대기.


## Change 013 — 승인된 QR 디자인 실제 renderer 적용

사용자가 제품번호 제외 시안으로 진행 승인. EMI 헤더·제조정보 카드·앞뒤 사진 모바일 세로/PC 두 열을 실제 공개 HTML renderer에 적용한다. 샘플/디자인 시안 문구는 실제 게시에서 제외한다. 사진은 자르지 않고 원본 비율 유지, 제품번호 미표시·독립 데이터 이미지·CSP·이름 escaping·공개 중지 경계를 유지한다. 내부 번호/출하/API/DB 계약 및 외부 자원은 변경하지 않는다.

실제 renderer 합성 출력의 정상/중지 테스트 2개 통과(skip 0). 생성 HTML을 HTTP(S) 차단 후 390/1440px로 열어 사진 두 장 로딩·가로 넘침 0과 시각 배치를 직접 확인했다. 악성 형식 작업자명은 실행 없이 텍스트로 표시됨을 유지한다. 실제 게시 source 적용 완료, runtime binary 갱신·Azure 자원 생성/게시는 미실행이며 실제 연결 시 반영한다.


## Change 014 — Azure 공개 저장소 실제 연결

2026-09-10 사용자 “좋아. 시작해”로 Azure 연결 시작 승인. 기존 구독 Azure subscription 1의 PMS 그룹 emi-pms-pilot 안에 emipmsbusbarqr 신규 StorageV2/한국 중부/Standard_LRS/Hot 저장소 생성. HTTPS 전용/TLS1.2/Blob 익명접근 false, $web ACL off 유지. 기존 PMS 저장소·운영 앱·업무 DB 변경 없음. 기본 페이지는 QR 안내, 404는 일반 안내이며 제품 목록은 없음.

확정 공개주소: https://emipmsbusbarqr.z12.web.core.windows.net/ . Blob endpoint: https://emipmsbusbarqr.blob.core.windows.net/ . 별도 도메인/CDN 없음. 실제 자원은 유지 중이며 사용량 과금 대상이다. 합성 공개 검증 token a6f771f081f75edbeec65db2f1df069ee2025777d0380231053565544dd6e05b, 사진2개는 단색 합성 이미지, 작업자 연결검증(가상). 실제 업무 사진은 전송하지 않았다.

격리된 /private/tmp/emi-busbar-azure-probe에서 실제 renderer/sink를 호출했다. 최초게시→동일주소 정정→정보중지→합성자료복원 통과. HTTPS 익명webGET, UTF-8 HTML/no-store, 번호·내부링크 미포함, oldETag PUT412, 익명blobGET/list 거부, root목록없음/404, 출력QR PNG ZXing 해독 URL일치 확인. 실제 공개사이트 390/1440 이미지2개 로딩·overflow0·HTTP1/PMS0 확인 및 직접 시각검토. /private/tmp/emi-busbar-live-qr.png 와 /private/tmp/emi-busbar-azure-live-{390,1440}.png는 합성 증거.

최소권한 container SAS rcw/HTTPS/2026-09-12 02:42 UTC 만료, 저장소 밖 /private/tmp/emi-busbar-azure-private/publication.json 0600/부모0700으로 임시보관. 토큰·계정키는 로그/기록에 남기지 않는다. 자동게시 운영 활성화 전 지속 자격증명 관리/갱신을 준비해야 한다. SAS가 만료되어도 이미 게시한 공개페이지 조회에는 영향 없음.

독립 reviewer /root/azure_qr_review 새 맥락(요청 Astra/high, 실제 NOT_REPORTED) 코드·계획 검토, 차단finding없음. worker는 기존 Pending 전체를 게시하므로 로컬5097 게시 활성화 보류 및 최종 URL확인 뒤 QR주소 연결 지적을 반영했다. 사용자에게 기존 대기사진도 게시할지 비동기 확인 요청, 아직 답변 없음. 실제 검수 사진의 일괄 공개는 진행하지 않는다.

로컬5197/5097에 최신Release와 확정 PublicBaseUrl/BlobEndpoint 연결, Publication Enabled=false/SAS미주입/seed=false/migration=false 유지. 재기동 중 .NET 경로 오류로 한 차례 시작 실패 후 실제 /usr/local/share/dotnet/dotnet 경로로 복구, PID38316 ready200/workspace200 canWrite=true 제품65개 조회 확인. 기존 검수 DB 유지. production 배포/push/PR/merge 미실행. 남은 단계: 기존 대기 제품 공개 여부 응답→승인 범위의 자동게시 활성화와 실제 사진/라벨 현장검수, 이후 마지막 이카운트 연동. 현재는 공개사이트 실연결 검증 완료·자동게시 비활성이다.


## Change 015 — 실제 제품 한 건 공개 테스트 준비

사용자 “하나만 테스트로 하자”로 단일 테스트 지시. Pending Complete 중 최초 완료 제품 IB-00000001(id88a740bc-1bdd-4faf-ab09-d8fdca72e4db, revision2)을 선정. singleID FORUPDATE/sharedlock→실 renderer/sink→공개본문 해시→해당행만 게시상태 갱신→타제품/원장/사진 해시 보존→PMS QR 해독 검증 도구를 /private/tmp/emi-busbar-one-product에 준비했다. 독립 /root/azure_qr_review(기존 reviewer 맥락 재사용)는 차단finding없음, 외부게시 뒤 후속단계 실패 시 동일대상 상태확인 경계를 확인했다.

실제 실행 요청은 자동 승인 검토가 거부: 특정 실제 제품 및 정확한 사진·제조정보·작업자명 공개 payload의 명시 승인이 부족하다는 이유. 우회 실행하지 않았고 실제 제품은 게시하지 않았다. 5097 worker는 false 유지. 허용된 읽기 전용 DB/로컬 renderer로 /private/tmp/emi-busbar-local-preview/product.html(0600)을 준비, 해당 건 Pending 확인. 공개예정 HTML SHA256 4B5355208B518934EA779BC115FE6279DFC661BEF886199F825D1F4F0DA47A26. 이름·실사진은 Task에 복제하지 않았다. 127.0.0.1:56318/product.html 한 경로만 제공하는 로컬 미리보기(session37240)를 열어 사용자가 정확한 payload를 확인할 수 있도록 했다. 특정 제품·정확한 내용에 대한 공개 승인 대기. 다른 실제 제품 변경 및 외부 전송 없음.


## Change 016 — 승인된 실제 제품 한 건 게시

사용자가 정확한 로컬 미리보기·대상 IB-00000001·앞뒤 사진·제조일시·작업자명 공개 내용을 확인한 뒤 “굿! 승인”으로 명시 승인했다. 이전 자동 승인 거부 사유인 특정 대상/payload 승인을 충족한다. 실행 도구에 승인 id88a740bc-1bdd-4faf-ab09-d8fdca72e4db/rev2 및 HTML SHA256 4B5355208B518934EA779BC115FE6279DFC661BEF886199F825D1F4F0DA47A26 일치 조건을 추가했다. 다른 제품과 자동게시 활성화는 범위 밖이며 비활성을 유지한다.

게시 실행 성공. 승인된 HTML 해시 일치 확인 후 해당 1행만 Published/revision2 반영, 타제품·원장·사진 해시 불변 통과. 인증된 PMS QR GET 성공 및 ZXing 해독 URL 정확히 일치. 공개 URL https://emipmsbusbarqr.z12.web.core.windows.net/p/dd1b60c6a00a443ad50b162c703fe8284022316fbec649f902f312c8d3f94643.html . API 재조회 Complete/Published/revision=publishedRevision=2/qrReady=true, 다른 완료 Pending1건 유지, 자동worker=false 확인. 출력 QR /private/tmp/emi-busbar-one-product-qr.png. 실제 페이지 PC/390 이미지2개 정상/번호미표시/overflow0/HTTP1/PMS0 검증, 실사진 screenshot은 추가 보관하지 않음. 사용자 실제 휴대폰 스캔·물리 프린터 출력 검수 대기. 운영 앱 배포·다른 제품 게시 없음.


### 단일 제품 사용자 확인 완료

사용자가 서버/실제 공개 페이지 안내 후 “좋아 굿. 확인 완료”로 IB-00000001 단일 QR 게시·조회 결과를 수락했다. 해당 단일 테스트의 사용자 검수 완료로 기록한다. 물리 프린터 출력·실물 부착·스캔을 각각 수행했다고 추정하지 않으며, 전체 업무 검수/다른 제품 공개/자동게시 활성화/운영 배포 승인을 의미하지 않는다. 자동 게시 비활성과 다른 제품 대기 상태를 유지한다. 다음 미완료 범위는 지속 게시 자격증명·자동게시 적용 범위 확정, 마지막 이카운트 연동 자료 확보 및 구현, 최종 전체 검수·회귀/원격 반영·운영 배포다.


## Change 017 — 자동 게시 운영 준비

사용자 다음 작업 시작 요청. 현재 임시 SAS 만료(2026-09-12) 대신 Azure 기존 backend 사용자할당 관리 ID 인증을 코드로 준비한다. 실제 운영 앱의 UserAssigned identity 존재를 읽기 전용 확인했다. 인증 방식 명시 선택/혼합금지/고정 storage scope/기존 SAS 호환/ETag 경계 유지. 기존 대기 제품 포함 여부는 비동기 질문 대기이며 자동 게시 활성화·추가 실사진 공개·운영 앱 배포/RBAC 변경은 아직 실행하지 않는다.

구현: `AuthenticationMode` 기본값은 기존 `Sas`; 새 `ManagedIdentity` 모드는 유효한 `ManagedIdentityClientId`를 명시하고 SAS를 제거해야 한다. Azure.Identity 1.17.2(기존 전이 의존성과 같은 버전)를 직접 참조한다. 지정된 사용자할당 ID만 사용하고 개발자 계정/다른 자격증명으로 fallback하지 않는다. HEAD/조건부 PUT 양쪽에 Storage 범위 Bearer 인증을 적용하며 ReviewSafe/비활성 상태에서는 게시 요청하지 않는다. 제품번호·사진·생산·재고·게시 대상 선정 로직은 이번 변경에 포함하지 않는다.

운영 적용 준비값(아직 적용하지 않음):

| 설정 | 값 |
| --- | --- |
| `InteriorBusbar__Publication__Enabled` | 적용 범위·배포 검증 전 `false` 유지 |
| `InteriorBusbar__Publication__AuthenticationMode` | `ManagedIdentity` |
| `InteriorBusbar__Publication__ManagedIdentityClientId` | 배포 대상 backend에 실제 연결된 사용자할당 ID의 client ID |
| `InteriorBusbar__Publication__SasToken` | 제거 |
| `InteriorBusbar__Publication__PublicBaseUrl` | `https://emipmsbusbarqr.z12.web.core.windows.net/` |
| `InteriorBusbar__Publication__BlobEndpoint` | `https://emipmsbusbarqr.blob.core.windows.net/` |

실제 적용 시 해당 backend ID에 전용 저장소 `$web` 컨테이너 범위의 Blob 데이터 읽기/쓰기 권한을 부여하고, 승인된 제품 하나로 실제 ID 인증·게시를 검증한 뒤 승인된 대상 범위로 활성화한다. 현재 로컬 Testing runtime은 Azure 관리 ID를 사용하지 않으며 임시 SAS 자동 주입도 하지 않는다. 운영 backend 배포·DB migration·RBAC 변경은 각각 실제 승인 범위에서 진행한다. 향후 제품만 게시하도록 선택하면 기존 Pending 제외 기준을 먼저 구현해야 하며 현재 worker를 그대로 켜지 않는다.

검증: 격리 `busbar_test` DB와 합성 데이터, fake TokenCredential/HTTP를 사용한 관련 인증·게시 테스트 29/29 통과(건너뜀 0). 잘못된 인증 설정, 토큰 획득 실패 시 전송 없음, 비활성 시 인증/전송 없음, 새 파일/기존 파일 조건부 쓰기를 포함한다. 실제 Azure 관리 ID 인증 및 운영 배포 검증은 미실행이다.

독립 검토: 새 reviewer `managed_identity_review`(요청 Astra/high, 실제 모델 NOT_REPORTED)가 HEAD `4ce512d` 대비 이번 4개 파일을 읽고 계약/품질 GO, P0–P3 finding 없음으로 판정했다. reviewer의 초기 테스트 근거는 26 pass/3 skip이며, 이후 parent가 격리 DB 포함 29 pass/0 skip을 직접 확인했다. 실제 ID/RBAC 검증 미완료 경계는 동일하다. 로컬 코드 준비 완료이며 자동 활성화 범위는 사용자 응답 대기다.

### 자동 게시 범위 승인 수신

사용자가 Azure 공개배포부터 새로 시작하므로 적용 시작 시점은 상관없다고 승인했다. 위 응답 대기는 해소되었다. 기존 Pending 및 앞으로 완료되는 제품 전체를 대상으로 하며 현재 worker의 대상 선정 규칙을 유지한다. 승인된 공개 필드와 정정 시 같은 URL 재게시 규칙은 그대로다. 이 승인으로 즉시 운영 배포하거나 로컬 검수 데이터를 초기화하지 않았다. 로컬 환경에는 Azure 관리 ID endpoint가 없으므로 임시 SAS를 다시 연결하여 상시 자동 게시하지 않고, 운영 배포 단계에서 관리 ID 권한과 실제 게시를 검증한 뒤 활성화한다. 이카운트는 여전히 마지막 구현 Task이며 문서/비식별 발주 응답 샘플 대기다.

## Change 018 — 마지막 이카운트 발주 연동 착수

사용자 “시작하자”에 따라 Task 7 착수. branch `codex/interior-busbar`, 기준 `c0cfaf8`, WIP 없음 확인 후 기존 계약·코드·공식 공개 안내를 읽었다. 이카운트 발주 조회 제공 및 로그인 후 세부 매뉴얼 확인을 공식 안내에서 재확인했다. 세부 API 문서/응답 샘플은 저장소에 없으며 사용자에게 파일 경로 또는 로그인된 매뉴얼 위치를 요청했다. 인증키·비밀번호를 채팅으로 요청하지 않았다.

확인한 연결 지점: `busbar_purchases`는 자재별 발주 행으로 `external_id` unique, `external_status`, `conflict_note` 준비 필드가 있다. `SavePurchase`는 신규 요청에 UUID를 발급하므로 반복 API 응답을 그대로 전달하면 중복 방지가 되지 않는다. 입고는 별도 Receipt 동작에서만 원장·재고를 증가시키며 외부 수신은 이 동작을 호출하지 않아야 한다. 기존 입고 수량보다 작아지는 발주 변경은 충돌로 남겨야 한다.

세부 사양 확인 후 구현할 대조 항목: 인증/세션·테스트 endpoint, 발주 및 품목행의 안정적 식별자, 공통 프로젝트 코드의 실제 필드와 조회 조건, 품목 코드·단위·수량, 변경·취소·삭제의 응답 표현, 페이지/기간/호출 제한. 목록 누락을 취소로 추정하거나 발주번호만으로 서로 다른 행을 합치지 않는다. 기존 준비 필드의 적합성은 이 대조 후 판단한다. 외부 송신 기능은 추가하지 않는다.

이번 착수는 읽기 전용 조사와 상태 기록까지이며 연동 구현·실제 API 호출·DB 변경·새 테스트 실행·배포는 미실행이다. 현재 필요한 입력은 세부 매뉴얼과 비식별 응답 샘플이다. 이를 확보한 뒤 실제 필드 매핑, 반복 수신/변경/취소/재시도와 입고·재고 보존 반례를 합성 환경에서 구현·검증한다.

## Change 019 — 주문·판매 범위 확장 및 실제 회사 양식 확인

2026-09-14 Chrome에 사용자가 연 이카운트 공식 API 매뉴얼의 주문서입력·판매입력·발주서조회·FAQ·이용방법과 회사 판매/주문 입력 및 웹자료올리기 항목을 UI로 확인했다. 브라우저 세션 URL과 인증키는 기록하지 않는다. ERP 전표 저장/API 실행/설정 변경은 하지 않았다. 빈 입력 화면에서 사이트 자체 임시저장 표시가 관측되어 화면 열기를 서버 상태가 전혀 변하지 않았다고 단정하지 않는다.

기존 `/private/tmp/emi-interior-busbar`는 Git 연결과 소스가 사라졌고 일부 기록만 남아 있었다. 보존된 마지막 commit `7645001`에서 `/private/tmp/emi-interior-busbar-ecount`, branch `codex/interior-busbar-ecount`를 만들어 복구했다. 이전 폴더/검수 데이터/타 작업 WIP를 삭제하거나 복원 덮어쓰기 하지 않았다. 복구 source의 v2 AGENTS를 유지한다.

### 확인된 API 계약

| 기능 | 확인한 사양 |
| --- | --- |
| 주문서 | POST `/OAPI/V2/SaleOrder/SaveSaleOrder`, `SaleOrderList[].BulkDatas` |
| 판매 | POST `/OAPI/V2/Sale/SaveSale`, `SaleList[].BulkDatas` |
| 공통 입력 | `UPLOAD_SER_NO`는 전표 묶음 순번이며 재시도 중복 방지 키로 설명되어 있지 않다. `WH_CD`, `PROD_CD`, `QTY` 필수. 회사 양식별 추가 필수 항목 존재 |
| 결과 | HTTP/본문 Status 200만으로 성공 판단 금지. `SuccessCnt`, `FailCnt`, `ResultDetails`, `SlipNos`를 검사하며 전표번호 보관 |
| 수정/삭제 | 공식 FAQ에서 불가능, 신규 코드/전표 입력만 지원한다고 명시. 전송 완료 후 PMS 수정은 자동 덮어쓰기 없이 정정 필요 상태로 처리해야 함 |
| 불명확 결과 | 원격 생성 뒤 응답 유실 가능. 중복 방지 보장이 확인되지 않아 무조건 자동 재전송 금지. 전표 확인 후 조정 필요 |
| 발주 조회 | POST `/OAPI/V2/Purchases/GetPurchasesOrderList`. 날짜 범위 최대 30일, PAGE_SIZE 최대 100. 응답 `PJT_CD`로 공통 코드 필터 가능하나 요청 필드에는 프로젝트 조건이 없음 |
| 발주 제한 | 문서상 응답은 `ORD_DATE`, `ORD_NO`, `PJT_CD`, `P_FLAG`, `UPDATE_DATE`, 수량/금액 합계 등. 품목별 PROD_CD/수량/안정적 행 ID는 확인되지 않음. 합계를 자재 입고로 만들지 않는다. P_FLAG 종결을 취소로 간주하지 않는다 |

### 회사 양식과 확정 매핑

- 사용자가 지정한 엘에스일렉트릭(주)의 거래처검색 결과 코드 `1168119273`, 청주캠퍼스의 창고검색 결과 코드 `CC` 확인. 이름이 다른 청주캠퍼스 자재창고와 구분했다. 코드값은 배포 기준정보로 입력할 대상이지 합성 테스트 기본값으로 하드코딩하지 않는다.
- 주문서/판매의 입력 및 웹자료올리기 양식에 거래처·출하창고·PO No·프로젝트·품목·수량·단가·공급가액·부가세가 존재한다. 주문서에는 W/O No와 납기도 별도 존재한다. 주문서 기본입력화면설정에서 PO No는 `u_memo1`(문자형식1), W/O No는 `u_memo2`(문자형식2)로 대조했다. 사용자 응답에 따라 주문서 `U_MEMO2`에 고객 업무번호를 전달하고 `U_MEMO1`은 생략한다. 판매 양식에는 W/O 항목이 없어 임의 필드에 복사하지 않는다.
- 제품군별 이카운트 품목코드·기준 단가를 관리하고 프로젝트에 단가 snapshot/필요한 변경값을 저장하는 방향. 공통 프로젝트 코드는 `PJT_CD`, 제품군 품목코드는 `PROD_CD`, 수량 `QTY`, 단가 `PRICE`, 공급가액 `SUPPLY_AMT`, 부가세 `VAT_AMT`, 주문 납기는 `TIME_DATE`로 대조한다.
- 사용자 확정: 원화·부가세 별도 10%, 고정 거래처/창고. 원 미만 금액 처리와 회사 API 거래유형 코드의 실제 설정은 검증 전 확인한다.
- 프로젝트 등록 시 주문 1건, 마지막 분할 출하로 해당 등록 건 완료 시 총 수량에 대한 판매 1건을 생성하는 계약. 완료 취소/재완료로 전체 판매를 중복 생성하면 안 된다. 이미 보낸 전표의 수량/가격 변경은 수정 불가 계약에 따라 정정 필요 상태로 관리한다.

사용자 응답으로 번호 매핑은 확정되었다: W/O No만 사용하고 PO No는 입력하지 않는다. 제품군별 실제 코드·가격과 공통 프로젝트 코드 값은 기준정보로 입력할 대상이다. 원 미만 최종 전송 금액 처리, 회사 거래유형 코드, 발주 자재별 상세 취득 경로는 확인이 필요하다.

### 상업 데이터 선행 구현·검증

기준정보에 제품군별 이카운트 품목 코드·기준 단가와 고정 거래처/창고 코드를 추가했다. 신규 프로젝트는 제품군 기준 단가를 복사하고 별도 변경할 수 있다. 기존 프로젝트의 단가는 이후 제품군 단가 변경·일반 정정에 영향받지 않는다. 미설정은 null이며 명시적인 0과 구분한다. 단가 생략은 신규 건의 기본값 적용/기존 건의 보존을 의미한다. 추가 migration 0093은 기존 자료와 재고를 유지하며 과거 가격을 임의로 만들지 않는다.

프로젝트 상세에 원화 공급가액·부가세 10%·합계와 누락 기준정보를 읽기 전용으로 표시한다. preview는 정확한 소수 계산값을 반환하며 아직 확정하지 않은 ERP 반올림 정책을 적용하지 않는다. 실제 전송 비활성을 명시한다. 엑셀은 기존 8열 뒤에 선택 단가 9열만 추가해 기존 양식을 유지한다. 화면 고객 업무번호 라벨은 W/O No로 명확히 했다.

검증: 격리 합성 `busbar_test`(127.0.0.1:55490)에서 부스바 관련 56/56 통과, 건너뜀 0. 가격 보존/변경/미설정/0/잘못된 단가, 설정 저장 후 workspace 재조회, 0093 이전 실제 테이블의 기존 프로젝트·재고 보존, 기존 단가 없는 import 신규/정정 및 읽기 전용 금액 계산을 포함한다. 새 migration 테스트의 취소 토큰 정적검사 실패는 보정 후 통과했다. frontend typecheck/변경 파일 eslint 및 신규 합성 Playwright 1/1 통과. 첫 UI 테스트의 label 선택 실패는 실제 combobox role을 사용해 보정했다. 1440/390px 입력·금액 조회 화면을 직접 시각 확인했다. 증거는 `/private/tmp/emi-busbar-commercial-{editor,preview}-{1440,390}.png`이며 합성 데이터만 포함한다.

독립 reviewer `commercial_review`(요청 Astra/high, 실제 NOT_REPORTED)가 설정 workspace에서 고정 코드를 누락하는 P2를 발견했고 보정 및 회귀 테스트를 추가했다. 최종 diff·테스트·390px 입력/1440px 조회 이미지를 독립 재검토해 계약/품질 GO, 열린 차단 finding 없음으로 확정했다. 이번 완료 범위는 단가·기준정보·조회 선행 구현이다. 주문/판매 전송 대기열·중복 방지·응답 불명확 상태·인증/실제 API 연결과 발주 수신은 아직 미구현이다. 운영 DB migration·실제 전표 전송·공유 서버 갱신·push/PR/merge/배포를 수행하지 않았다. 사용자 검수 후 최종 병합 후보 전체 회귀는 별도 단계로 남긴다.


## Change 020 — 제품군 단가 단일 기준으로 정정

사용자 “단가는 제품군별 단가 고정임. 바뀌면 제품군별 단가를 바꿔야함.”에 따라 Change 019의 프로젝트별 단가 변경/보존 계약을 대체한다. 제품군 기준정보만 단가를 소유하며 프로젝트 신규·정정·엑셀에서 별도 단가를 입력하지 않는다. 프로젝트 금액 조회는 제품군의 현재 단가로 계산한다. W/O만 사용·PO 공란·원화 VAT 별도 10%·고정 거래처/창고는 유지한다. 아직 구현하지 않은 외부 전송의 전표 이력은 향후 전송 당시 값을 보존해야 하며 기준 단가 변경으로 이미 보낸 전표를 덮어쓰지 않는다.

프로젝트 요청/화면/엑셀의 단가와 snapshot 로직을 제거하고 조회를 제품군 기준으로 바꿨다. 제품군 단가 변경 시 펼친 금액 조회도 다시 읽도록 한다. 0093은 main 미반영·운영/검수 DB 미적용이며 disposable 테스트에만 사용했으므로 미배포 migration의 프로젝트 가격 열 추가를 제거했다. 기존 업무 데이터와 재고는 변경하지 않았다.

검증: 합성 busbar_test DB에서 해당 가격·import·설정·기존 데이터 migration 테스트 5/5 통과(0 skip), frontend typecheck/lint 및 PC·390px 합성 UI 테스트 1/1 통과. 프로젝트 요청에 단가가 없고 단가 필드도 표시되지 않는 것을 확인했다. PC 금액 조회와 모바일 입력 이미지를 직접 확인했다. 독립 reviewer `family_price_review`(새 맥락, 요청 Astra/high, 실제 NOT_REPORTED)가 코드·계약·테스트 및 두 화면을 확인해 GO, 열린 finding 없음으로 판정했다. 실제 서버 반영·외부 전표 전송·push/merge/배포 없음.


## Change 021 — 주문·판매 전송 대기와 중복 방지

사용자 다음 작업 시작 요청으로 전송 대기 저장·상태 조회·안전한 재시도 기반을 구현한다. 프로젝트 등록과 주문 대기, 마지막 출하와 판매 대기를 같은 업무 transaction에 저장하고 프로젝트/종류별 1건만 허용한다. 완료 취소 시 미전송 판매는 보류하며 재완료로 중복 생성하지 않는다. 전송 시점 제품군 가격·설정·프로젝트 값을 별도 시도 이력에 보존한다. 결과 불명확 건은 자동 재시도하지 않는다. 실제 인증·회사 거래유형·금액 처리의 확정과 실제 API 연결은 분리하고 외부 전표를 생성하지 않는다. 기존 프로젝트를 migration만으로 주문 전송 대상으로 만들지 않는다.


사용자 추가 확정: 공급가액·부가세는 원 미만 소수 금액을 그대로 유지한다. 임의 반올림·버림을 하지 않는다. 확인된 API 허용 정밀도(공급가액/VAT 소수 4자리, 주문서 PRICE numeric18,6)를 초과하면 전송 시도 생성 전에 보류한다. 제품군 단가 자체의 입력 정밀도는 현재 4자리이며, 부가세 곱셈으로 5자리가 생기는 경우에도 원값을 변형하지 않는다.

구현 결과: migration0094에 프로젝트/종류 unique 대기 건과 불변 전송 시도 payload를 추가했다. 신규 프로젝트/엑셀 등록과 주문 대기를 함께 저장하고 마지막 분할 출하와 판매 대기를 함께 저장한다. 완료 취소는 미전송 판매를 보류하고 재완료 시 기존 대기를 사용한다. 청주 조회/담당자 입력 경계 내 상태 조회 및 사유를 남기는 다시 대기 API/UI를 추가했다. 실패는 전표 미생성이 확정된 경우에만 다시 대기할 수 있고 성공/불명확/변경 확인 건은 차단된다. 동시에 claim해도 시도는 하나이며 과거 응답이 새 시도 상태를 덮어쓰지 않는다. 실제 전송 직전 사용할 제품군 단가·코드·프로젝트·정확한 금액을 시도별로 보존한다.

중요한 구현 경계: 인증된 이카운트 HTTP adapter/주기 실행기는 아직 연결하지 않았다. Claim/Finish/Recover는 후속 adapter용 내부 상태 처리 함수로 합성 테스트에서만 실행했다. 운영에서 대기 내역을 자동으로 보내거나 중단 상태를 자동 복구하는 worker는 아직 없다. 화면에 실제 전송 연결 전임을 명시한다. 결과 확인이 필요한 원격 전표의 대조/해결 흐름도 실제 응답 처리와 함께 후속 구현한다. 현재 코드만으로 실전송 완료라고 표현하지 않는다.

검증: 전용 합성 busbar_test에서 부스바 관련65/65 통과(0skip). 주문 대기/분할 출하/재완료 중복 방지, 동시claim, 시도 당시 금액 보존, 확정실패 후 재시도, 이전 응답 차단, 주문 성공 전 판매 보류, 성공판매 취소·재완료 차단, 중단상태 복구, 소수1234.5 보존/정밀도 초과 보류, 청주 조회·쓰기/오산 직접API 거부를 포함한다. 초기 실행의 새 mutation 감사 registry 누락 및 합성 테스트 숫자 cast 오류를 보정했다. frontend typecheck/lint, 합성 UI2/2 통과. 최초 모바일 페이지 overflow를 발견해 전송상태를 상세 전체 너비로 분리하고 표 내부 스크롤을 유지했다. 최신 390/1440 화면을 직접 확인했으며 합성 증거는 `/private/tmp/emi-busbar-ecount-status-{390,1440}.png`이다.

독립 reviewer `ecount_queue_review`(새 맥락, 요청 Astra/high, 실제 NOT_REPORTED)가 P2 세 건(확정실패 후 변경확인 잔류, 무변경 저장 invalidation, 업무 수정에 따른 복구 기준시각 연장)을 발견했다. 실제 payload 비교, 확정실패 플래그 정리, 불변 시도 시작시각 기준으로 보정하고 각 반례를 추가했다. 최종 재검토에서 계약·품질 GO, 열린 차단 finding 없음으로 판정했고 1440/390 이미지를 직접 확인했다. 로컬 코드/검증만 진행했으며 검수·운영 DB migration, 공유서버 갱신, 실제 ERP 전송, push/PR/merge/배포는 하지 않았다.


## Change 022 — 실제 API 연결 코드 및 자동 실행

사용자 다음 작업 요청으로 인증/단건 주문·판매 HTTP adapter와 자동 실행을 구현한다. 외부 매뉴얼은 읽기 전용 Chrome UI에서 Zone/login/주문 응답을 재확인했고 세션 URL·인증키를 기록하지 않았다. Zone Data.ZONE/DOMAIN, 로그인 Data.Datas.SESSION_ID와 Code00, 단건 전표 SuccessCnt/FailCnt/ResultDetails/SlipNos를 대조한다. 인증 실패는 영속 중지하며 운영 로그인 10분/테스트10초 간격, 저장11초 간격을 DB에서 통제한다. 테스트·운영 환경은 대기 DB에 바인딩하여 동일 DB의 테스트 성공을 운영 성공으로 재사용하지 않는다.

사용자는 자동로그아웃 시간을 모른다고 응답했다. Self-Customizing→보안관리→보안설정에서 자동로그아웃 `4 시간`을 읽기 전용으로 직접 확인했다. ERP 설정을 바꾸지 않았다. 운영 준비값 SessionIdleMinutes는 240분보다 짧은230분으로 권장하며 환경변수에 명시한다. 활성화 기본값 false, ReviewSafe에서는 인증/DB처리/외부전송 모두 차단한다. 실제 API키·발신IP등록/운영설정·provider 호출·배포는 이번 구현에서 실행하지 않는다.

구현: Zone 조회와 인증키 API 로그인, 세션 재사용, 단건 주문/판매 전송 adapter를 연결했다. 사용자의 웹사이트 직접 로그인은 정상 전송에 필요하지 않다. 요청 필드 대소문자를 그대로 보존하고 W/O는 주문 U_MEMO2만, PO는 생략한다. 소수 금액을 유지하며 전송일·거래유형도 시도에 고정한다. 0095에 환경·회사 바인딩, 영속 호출 간격·중지 상태를 추가했다. worker는 legacy 설정을 포함해 청주 DB identity를 반드시 확인하며 동시에 하나만 전송한다. 잘못된 DB·다른 회사/테스트 환경에서는 인증 전에 중지한다. 응답 유실·형식 불명확·중단 복구는 Unknown으로 남기고 자동 전송을 중지한다. 확정 실패 3회도 중지한다. 판정과 중지 상태는 같은 transaction에 저장한다.

담당자 화면에 연결 상태·사유를 남기는 자동 전송 재개·전표 확인 반영을 추가했다. Unknown은 실제 ERP 조회 후 전표 있음(번호 필수)/없음으로 확인한다. 없음도 자동 재전송하지 않고 별도 다시 대기 동작이 필요하다. 성공 후 변경 확인은 실제 ERP 정정·대조 사유와 현재 기준정보/누적 출하 수량을 보존해 해소한다. 원래 전송 payload는 유지하고, 확인 이후 단가 또는 일부 분할 출하 수량이 다시 바뀌면 재확인을 표시한다. 전표 확인 반영은 외부 전표를 수정하거나 연결을 자동 재개하지 않는다. 재개/확인은 worker 진행 중 거부하며 청주 담당 권한·감사 이력을 적용한다.

운영 준비 설정 이름: `InteriorBusbar__Ecount__Enabled`(기본 false), `Environment`(Test 또는 Production), `CompanyCode`, `UserId`, `ApiKey`, `SessionIdleMinutes`(확인한 4시간보다 짧은230), 선택 `IoType`(검증된2자리, 공란이면 회사 기본값). 뒤의 이름도 같은 `InteriorBusbar__Ecount__` 접두사를 사용한다. 인증키는 승인된 환경변수/비밀 저장소에 주입하며 저장소·채팅에 기록하지 않는다. 실제 테스트 전에는 테스트 인증키 유효성, 등록 발신 IP, 테스트용 품목/공통 프로젝트/거래처/창고와 전송할 합성 건을 대조해야 한다. 실제 연결 활성화·전표 생성은 해당 환경의 실행 승인 후 진행한다.

검증: 합성 전용 busbar_test(127.0.0.1:55490)와 fake HTTP를 사용했다. adapter63/63(0skip), 최초 부스바138/138 통과 후 확인 흐름/권한/회사 경계 추가 검증144건 중143통과·테스트 SQL 숫자 변환1실패를 확인했다. 실패는 `100.0000` 문자열을 bigint로 직접 변환한 테스트 오류였고 numeric 변환으로 고쳤다. 추가 분할 출하 반례까지 포함한 최종 queue10/10(0skip) 통과로 영향 범위를 재검증했다. 운영 로그인10분 간격, 실패3회 중지, 다른 회사/오산/Directory DB 인증 차단, 동시 worker/재개 거부, 중단 상태의 원자적 중지, 실제 Store snapshot→HTTP adapter→가짜 ERP의 소수1234.5/W/O/납기 전달을 확인했다. frontend typecheck/eslint 통과, 관련 합성 UI3/3 및 최종 연결 화면1/1 통과. 최종390/1440 전체 확인 입력창을 직접 시각 확인했다. 합성 화면 증거는 `/private/tmp/emi-busbar-ecount-connection-{390,1440}.png`이다. 초기 테스트 실행기/화면 서버의 로컬 소켓 제한은 승격된 합성 테스트 실행으로 해소했다.

미실행: 실제 ERP 인증·전표 생성, 사용자 검수, 공유 runtime 전환, 검수/운영 DB migration, push/PR/merge/배포. 사용자 검수 후 최종 병합 후보 전체 회귀는 별도다. 다음 실전 연결 검증은 승인된 테스트 환경의 합성 주문1건→완료판매1건 대조이며, 발주 자재별 상세 취득 경로 확인/수신 연동은 마지막 단계로 남는다.

독립 reviewer `ecount_connection_review`(요청 Astra/high, 실제 NOT_REPORTED)가 기준fa8230d 이후 누적 diff·반례·최종390/1440 이미지를 확인해 계약/품질 GO를 판정했다. R1청주 identity 생략(P1), R2결과/중지 원자성(P1), R3재개 경쟁(P2), R4불명확 오류 배열(P2), R5확인 이후 추가 부분출하 감지(P2)를 모두 보정했고 열린 P0/P1/P2 없음. 추가 감사 mutation coverage4/4 통과. 완료한 범위는 로컬 commit으로 보존하며 원격/운영 반영과 구분한다.

## Change 023 — 이카운트 테스트 환경 단건 실전 검증 준비

사용자 “진행하자”는 직전 제안의 테스트 환경 합성 주문1건→완료판매1건 대조 실행 승인으로 확인한다. 운영 endpoint·운영 전표·공유 runtime/배포 승인은 포함하지 않는다. 기준1e3a330, clean branch 확인. 현재 process 환경과 해당 앱 설정에 Ecount 인증 설정이 없으며 UserSecretsId도 없다. 실제 인증키·회사코드·API사용자 및 테스트 대상 품목/공통 프로젝트 코드가 필요해 인증/전표 요청은 아직 실행하지 않았다. 저장소 밖 소유자 전용 입력 파일 `/private/tmp/emi-busbar-ecount-test-settings.json`을 생성했다. Environment=Test, Enabled=false, 수량1·단가12345·W/O=PMS-API-TEST이며 실제 계약의 고정 거래처/창고 코드를 준비했다. 비밀값은 기록·출력하지 않는다. 제공된 설정/기존 비밀 파일을 확인한 뒤 승인된 두 전표만 처리하는 격리 실행을 준비·실행하고 결과를 대조한다. 현재 브라우저 도구는 이전 Chrome 제어 도구를 제공하지 않고 Aside CLI도 없어 이전 열린 세션에서 키를 가져왔다고 주장하지 않는다. 설치나 브라우저 설정 변경은 하지 않았다.

사용자 추가 요청: 사이트에서 인증 설정을 직접 확인·주입하고 사용자 본인이 로컬 화면에서 데이터를 입력해 검증한다. 이 범위의 테스트 연결 준비를 진행한다. 현행 도구에는 이전 Chrome 제어 기능이 없고 Aside CLI는 미설치여서, 적용 aside-browser 스킬의 명시 지침에 따라 CLI 설치 허용을 요청했다(응답 대기, 미설치). 기존5197/5097 프로세스는 이전 `/private/tmp/emi-interior-busbar` source 소유임을 확인했으며 변경하지 않았다. 현재1e3a330 Release build 경고0/오류0으로 실행 파일을 준비했다. 사이트 인증 확인·설정 주입·새 로컬 검수 연결은 미완료이므로 현재5197 입력이 실제 이카운트로 전송된다고 안내하지 않는다.

사용자가 Aside CLI 설치를 명시 승인해 공식 설치를 실행했다(1.26.906.1630). Aside 브라우저를 열어 연결 복구 후 공식 이카운트 페이지의 로그인 링크를 따라 로그인 화면에 도달했다. 이전 Chrome 세션은 이 브라우저에 공유되지 않으며 회사코드·아이디·비밀번호 필드가 비어 있고 자동 입력 경로도 표시되지 않았다. 사용자에게 열린 Aside 화면에서 최초1회 로그인만 요청했다. 로그인 정보·API키를 채팅으로 요청하지 않았다. 실제 인증키 확인/주입이나 API 전송은 아직 미실행이다. 로그인 후에는 사용자 직접 입력 검증 요청을 우선하며 합성 주문/판매를 대신 등록하지 않는다.

사용자 로그인 완료 후 Aside에서 실제 API인증키발급과 공식 API 이용방법을 확인했다. 테스트 키는 API인증현황에서 별도 발급(2주 유효)하며 테스트용 허용 IP가 필요하다. 테스트 키발급 시 이카운트가 '테스트 인증키 발급 횟수를 초과하였습니다. [제공 횟수 : 5/3]' 안내를 반환했다. 반복 발급·운영키 대체 호출 없이 추가 발급 요청 양식을 열어 SUBIN.PARK를 선택하고 주문서/판매 각1건의 로컬 테스트 목적·소수금액 검증 사유를 입력했다. 지원 담당자에게 외부 요청을 전송하는 승인은 별도 필요하여 전송 버튼은 누르지 않았다. 현재 실제 테스트 키 발급·로컬 주입·ERP API 인증/전표 전송 미완료이며, 테스트 키 승인 후 사용자가 직접 데이터를 입력하는 검증을 계속한다.

사용자 정정: 사용할 계정은 SUBIN.PARK 또는 NAYOON.LEE이며 테스트 키 필요성을 재확인 요청. 추가 테스트 키 요청창을 전송하지 않고 닫았다. 실제 API인증현황의 렌더링된 내용에서 주문서입력·판매입력이 '검증이 완료된 API 리스트'에 포함됨을 확인했다. 기존 테스트 키(NAYOON.LEE)는 2026-05-25 만료지만, 현재 필요한 두 API의 운영 검증은 이미 완료 상태다. 따라서 기존 유효 운영 키로 운영 API를 이용하는 경로에 추가 테스트 키 발급이 선행조건이라고 안내한 것은 정정한다. 별도 테스트 endpoint에서 시험하는 경우는 테스트 키가 필요하다. 로컬 PMS도 운영 endpoint에 연결하면 실제 전표가 생성되므로, 기존 테스트 환경 승인과 구분해 운영 전표 생성 연결 여부를 사용자에게 확인한다. 실제 API키 추출/주입·IP설정 변경·운영 API 요청/전표 생성은 아직 하지 않았다.


사용자 최종 승인: SUBIN.PARK 운영 계정으로 진행하고 실제 회사 주문서·판매 전표 생성도 허용한다. 생성된 전표 종류·번호를 명확히 보고한다. 사용자가 로컬에서 직접 입력하는 흐름을 우선하며 임의 합성 프로젝트/전표를 대신 만들지 않는다. 이 승인으로 위 테스트 환경 한정 실행 경계를 해당 로컬 연결의 Production 사용으로 확장한다. 원격 반영·운영 PMS 배포·ERP 전표 삭제 승인은 포함하지 않는다.

2026-09-14 로컬 연결 적용: ERP에서 유효한 SUBIN.PARK 운영 키와 저장된 로그인 회사코드66294를 확인해 저장소 밖0600 설정에 주입했다(키 비출력). 현 발신 IP는 이미 ERP 허용 목록과 일치하여 IP 설정을 변경하지 않았다. SessionIdleMinutes230 적용. 기존 소유 preview5097/5197의 PID·cwd를 대조하고 현재1e3a330 source로 전환했다. API는5097, 화면은 http://127.0.0.1:5197/interior-busbar 이다. 최초 프론트엔드 API 기본주소 연결 실패를 발견해 VITE_API_BASE_URL 공란·5097 proxy로 재기동했고 실제 관리자 기준정보 화면 정상 표시를 직접 확인했다.

검수 DB busbar_review_20260909(Docker emi-busbar-test-20260909)는 삭제/초기화하지 않았다. 컨테이너 내부 새 custom backup을 생성·비어있지 않음을 확인한 후 공식 --migrate-only로0093~0095를 적용해95개 migration 검증 성공을 확인했다. 프로젝트1건·제품65개 보존. 기존 비어 있던 DB identity는 해당 청주 검수 DB에 한정해 CHEONGJU/0086 계약으로 바인딩했다. Ecount runtime은 Production/66294, paused=false로 확인했다. migration/seed/QR공개게시/알림 provider는 비활성 유지. ERP 고정 거래처1168119273·창고CC는 관리자 API로 저장해 감사 기록을 남겼다.

현재 전표 전송 대기0건·시도0건·생성 전표0건이며 API 로그인은 아직 실행되지 않았다. 이카운트 실제 인증과 주문/판매 성공은 사용자 첫 입력 이후 대조할 검증으로 남는다. 기존 공통 프로젝트 코드'a'와 제품군6000a/6000a dc의 미설정 이카운트 품목코드·기준 단가는 사용자 실제 기준정보로 채워야 한다. 임의 코드·단가를 만들지 않았다. 기존 프로젝트를 주문 대기로 소급 생성하지 않는다. 사용자는 기준정보를 먼저 저장한 후 새 프로젝트를 등록하고, 마지막 출하 완료로 판매를 확인한다. Git 원격 반영·merge·운영 PMS 배포는 수행하지 않았다.


독립 runtime 검토에서 launcher의 AdminDeletionPurge 명시 비활성 누락(P1)을 발견했다. 기본값에 의존하지 않도록 AdminDeletionPurge__Enabled=false를 추가하고 해당 backend만 재기동했다. 인증된 runtime-mode 응답에서 adminDeletionPurgeWorkerEnabled=false·backgroundWorkersEnabled=false를 확인했다. backup을 복원하지 않고 COPY 행 수만 현재 DB와 대조해 사용자12→12, 부서10→10, 휴일0→0, 부스바 프로젝트1→1, 제품65→65로 삭제 영향 없음을 확인했다. 공통 코드'a'는 비어 있지 않아 자동 차단 대상이 아니므로, 실제 코드로 먼저 변경한 뒤 신규 프로젝트를 등록해야 한다고 안내한다. 신규 프로젝트는 당시 공통 코드를 보존하므로 나중에 기준정보만 바꿔도 기존 프로젝트 코드가 자동 변경되지 않는다.

독립 reviewer ecount_connection_review가 보정된 실행기·loopback listener·비밀파일 권한과 실행자가 제공한 runtime/보존 증거를 재검토하여 Change023 로컬 연결 준비 GO, 열린 차단 finding 없음으로 판정했다. 실제 ERP 인증·전표 검증은 아직 대기다.


## Change 024 — 최초 사용자 등록 확인과 실제 응답 형식 보정

사용자 '생성 완료' 후 신규 'pms 테스트'(수량1) 등록과 Order Pending1건을 확인했다. 시도0·전표번호 없음. 인증 worker는2026-09-14 08:47 UTC에 중지되었다. 실제 읽기 전용 Zone 응답은 Status 숫자200·Error 생략인데 adapter가 문자열200·Error:null만 허용해 로그인 전에 실패한 원인을 확인했다. 비밀값·원시 응답은 기록하지 않는다.

Envelope은 숫자200 또는 문자열200과 Error 생략/null을 허용하도록 최소 보정했다. 그 외 상태·타입·nonnull Error는 거부하며 회사·사용자·세션·호스트 검증과 저장 성공 증거·Unknown/재시도 경계는 유지한다. 합성 회귀6건 추가 후 adapter69/69(0skip), Release build 경고0/오류0, diff check 통과. 최초 테스트의 실행기 소켓 sandbox 제한은 승인된 로컬 실행으로 해소했다. 작성과 분리된 기존 reviewer ecount_connection_review가 이번 diff와 결과를 재검토해 GO·열린 finding 없음으로 확정했다.

수정을 해당 로컬5097에 적용했으며 영속 전송 중지는 유지한다. 실제 인증 성공·전표 생성은 여전히 미확인이다. 사용자 신규 건의 제품군 품목코드/단가가 미설정이고 프로젝트 공통코드는'a'여서 실제 사용할 세 값을 요청했다. 사용자가 기준정보의 공통코드만 변경해도 이미 등록한 프로젝트는 자동 갱신되지 않으므로 승인된 실제 코드 확인 후 해당 등록 건의 보정 경로를 확인한다. 승인되지 않은 추측 코드·가격으로 전송하거나 프로젝트를 다시 만들어 중복 대기를 만들지 않는다. 원격/merge/운영 배포 없음.


사용자 '입력해놨어' 후 공통코드ps26-045, 6000a 품목코드/단가307407 입력을 확인했다. 기존 테스트 프로젝트에만 남은 임시코드는 사용자 요청에 따라 DB·paused·Pending·attempt0을 transaction에서 확인하고 advisory lock 및 before/after 감사 기록을 남겨 보정했다. 새 프로젝트나 전송 대기를 추가 생성하지 않았다. 재개 후09:01:32UTC 인증이 다시 실패해 attempt0·전표0 상태다.

인증 실패 원인 구분을 위해 adapter의 고정 단계명과 공식 매뉴얼 오류코드에 대응하는 고정 분류명만 로그에 남기도록 보완했다. provider 원문·인증키·세션·URL·예외값은 로그에 포함하지 않는다. 인증 성공/실패 조건과 재시도 간격은 변경하지 않았다. 합성 adapter74/74(0skip), Release0warnings/0errors, 독립 reviewer GO·열린 finding 없음. 로컬 runtime에 반영해 기존09:01:32 기준10분 제한을 유지하며 재개했다. 웹사이트 키 재추출·파일 저장을 통한 비교는 자동 승인 검토가 거부하여 중단했다. 우회하거나 다시 추출하지 않고 기존 설정을 사용한다.


09:11:37UTC 재개 진단은 ZoneHost에서 종료되어 실제 로그인 요청 전 실패임을 확인했다. 인증키 없는 별도 .NET Zone 조회에서 JsonContent를 버퍼링하지 않으면 Data에 EMPTY_ZONE만 오고, 전송 전 본문을 읽어 버퍼링하면 정상 ZONE/DOMAIN이 반환되는 것을 재현했다. 실제 adapter를 UTF-8 application/json StringContent로 바꿔 Content-Length를 제공했다. 같은 직렬화 옵션·소수·필드 매핑은 유지한다. fake handler가 본문 읽기 전 ContentLength를 검사하게 해 테스트 자체 버퍼링이 실제 문제를 가리던 부분을 보정했다. 합성74/74, Release0warnings/0errors 및 고정 길이 실제 Zone 조회 정상 응답 확인. 독립 reviewer GO. 해당 local runtime 반영·재개했고09:21:37UTC 이전 로그인 재시도는 차단된 채 유지한다.


09:21:40UTC 실제 Zone은 통과했으며 다음 실패는 LoginCode로 확인했다. 공식 로그인 Result 표는 Data에 COM_CODE/USER_ID/SESSION_ID를 직접 나열하고 과거 Example은 Code00+Datas로 감싸고 있어 두 구조를 지원하도록 보정했다. nested 또는 Code가 존재하면 반드시 문자열00이어야 하며 성공 envelope·정확한 회사/사용자·유효 세션 검증은 공통으로 유지한다. Datas가 잘못되면 flat로 대체하지 않는다. 실제 응답 분별용 로그는 해당 필드의 존재 여부·JSON형식만 기록한다. 합성80/80(0skip), Release0warnings/0errors, 독립검토 GO. local 적용·재개했으며09:31:40UTC 이전 재로그인 제한을 유지한다. 로그인 성공과 전표번호는 아직 대기다.


09:31:44UTC 응답의 Code=String/Datas=Object와 flat 필드 없음이 확인되었으며 LoginCode 실패가 지속됐다. 실제 성공 코드 불일치를 구분하도록 Data.Code만 최대10자 ASCII영숫자/_/-로 제한해 진단한다. 그 외 값은 Unrecognized로 표시하고 응답 메시지·세션·키는 기록하지 않는다. logger capture4건을 추가해 총84/84(0skip), Release0warnings/0errors, 독립 reviewer GO. auth조건을 변경하지 않고09:41:44 이후1회 확인하도록 local반영·재개했다. 아직 전송 시도와 전표 생성은0이다.


09:41:45UTC 실제 로그인 Data.Code=205를 확인했다. 공식 매뉴얼에서205는 허용되지 않은 접속IP다. 현재.NET/Python 발신IP61.39.208.57은 이전 확인값14.33.156.30과 달랐으며 ERP 허용목록에 없었다. 승인된 로컬 실전 연결 범위에서 API IP등록에 현재 단일IP만 추가했다. 저장 후 팝업을 다시 열어 새IP와 기존4개IP 모두 보존됨을 확인했다. 초기 DOM 집계는 popup 로딩 전0건으로 나왔으나 후속 scoped snapshot에서 저장을 확인했다. 키 재발급·기존IP삭제·전체IP허용은 하지 않았다.09:51:45UTC 이후 재개하도록 기존간격을 유지했다.


09:51:47UTC SUBIN.PARK 운영 인증 성공 후 사용자 등록 건 Order를 정확히1회 전송했다. 저장 응답은 Unknown으로 분류되어 자동 전송을 중지했다. ERP 주문서조회에서 공통 프로젝트PS26-045로 검색하고 2026/09/14 -2 상세를 열어 품목 주문서-INT-BB-6000A, 수량1, 공급가액307407, 부가세30740.7, 거래처1168119273, 창고CC 및 빈 PO/W/O를 대조했다. 실제 주문서 생성 확인 후09:57:57UTC reconcile API(Recorded)로 해당 전표번호를 연결했다. 최종 Order Succeeded/needsReview=false/attemptCount1이며 판매 전표는 생성하지 않았다. ERP 수정·삭제·중복 전송은 하지 않았다. 자동 전송은 paused=true로 유지한다. 저장 성공 응답 자동 판정은 아직 해결되지 않았으며 실제 저장 응답 구조 근거를 확보해 보정하는 작업이 남는다. 수동 전표 확인으로 성공 연결한 것을 자동 응답 처리 성공으로 간주하지 않는다. 사용자에게 추가 데이터 입력 전 이 한계를 안내한다. 원격 반영·merge·운영 PMS 배포 없음.


## Change 025 — 프로젝트 최초 등록자와 ERP 담당자

사용자 승인: 최초 프로젝트 등록자의 이름을 담당자로 자동 사용. 공식 주문서 매뉴얼 EMP_CD는 ERP 담당자코드 STRING(30)임을 확인했다. 신규 등록 시 서버 actor/name을 보존하며 수정·엑셀 갱신은 등록자를 덮어쓰지 않는다. 기준정보에서 PMS 사용자별 ERP 담당자코드를 연결하고 주문/판매 snapshot에 사용한다. 연결 누락은 전송 보류로 표시한다. 기존 등록자 미보존 건에 임의 등록자를 채우거나 이미 생성한 실제 전표를 재전송하지 않는다. 0096 additive migration, 권한 공통필터 유지, 격리 합성 검증과 독립검토 진행. 자동 전송 중지 및 원격/배포 경계 유지.

Change025 validation: 174/174 InteriorBusbar and AuditMutation tests passed (0 skipped); UI employee mapping test passed with inspected 1440/390 screenshots; frontend lint/typecheck and diff check passed. Initial registry startup failures were fixed by adding the new mutation endpoint and authorization tests. Independent employee_review final GO, no open blockers. Migration recovers only unique creation-audit evidence; names recovered from current profiles use CreationAuditCurrentName provenance.

Local application NOT executed: automatic approval review rejected the backup plus migration command for persistent busbar_review_20260909 because explicit authorization for that environment mutation was required. No workaround or retry, no backend restart, no new ERP slips, no remote changes. Before the rejected action, runtime was paused=true with one Succeeded order. Request user approval to back up this database, apply0096, and restart the owned5097 backend. Build outputs now contain the new version, so do not restart the old server before migration approval. Frontend optional fields tolerate old responses until backend application.

Change025 local application approved by user explicitly, including automatic ERP operation. Backup /tmp/busbar-before0096-approved.dump inside owned container; official migrate-only completed96verified. Restarted owned backend5097 PID25668. Read workspace confirms3projects/65products and recovered creator names. ERP remains paused pending user answer for shared dev-admin mapping (Park Subin or Lee Nayoon); no assumption between API identity and project creator. ERP employee search confirms Park Subin code08436; lookup auto-select filled unsaved order form, no Save invoked. New user-created project has Order Pending attempt0. Added fixed-field save-shape diagnostics only (types,cappedcounts,booleans),85adaptertests pass including private-text nonleak; independent employee_review GO. Previous confirmed real order never retried. Actual automatic-save response parsing and elapsed registration time still require new pending job after mapping decision. No remote changes.

User selected Park Subin. Audited PUT mapped local dev-admin50000000-0000-0000-0000-000000000001 to verified ERP08436; resume API succeeded. Existing confirmed order is needsReview after mapping change, never resent. New user project8cf6aa69-53b3-4452-aeff-13a4af172ad0 (6000a dc,qty3) was processed at12:02:16UTC and Held before any save attempt: family standard price and ERP product code are null. Asked user to supply those actual values. No new slips; actual successful registration latency and Save response format remain unverified. Runtime automatic processing resumed; mapping decision is resolved.

Change025 automatic transmission completed after user supplied DC code and255458 price. Existing user Order retry queued12:05:24.650UTC; save started12:05:27.368, response finished12:05:28.139 (queue-to-response3.49s, HTTP0.77s). Live response success1/fail0/true/singleSlip had Errors:null, causing oldparserUnknown. Corrected only null-or-emptyErrors success while retaining all successproofs and explicit failure errors; adapter89/89pass, independentreviewGO. ERP list verified order2026/09/14 -3, Park Subin, PS26-045,6000A DCqty3 supply766374 VAT76637.4 total843011.4. Audited Recorded reconciliation, no resend. Restarted owned backendPID26846 and resumed; final pausedfalse, currentjobSucceeded needsReviewfalse attempt1. Oldorder-2 remains reviewflag from employee mapping change, never resent. No sale created. New projects enqueue automatically; 5-second worker polling and11-second minimum sendgap. Restart lost in-memory session: next permitted auth12:12:16UTC (21:12:16KST), as persisted10-minute login gate; do not bypass. First post-fix live save still to be user tested; no extra synthetic ERP slip generated. Local changes only.

User accepted verification1(new automatic order). New sales contract: each partial shipment may create a sale for that shipment quantity; order-to-sale linkage is mandatory, preferably order shipment processing or sale importing the source order. Investigation only this turn; no sales code/runtime changed. Official logged-in SaveSale manual example contains REL_DATE/REL_NO/MAKE_FLAG but actual parameter rows do not document their meaning or how to link source orders. Official available API list does not list order shipment processing. Therefore supported public API linkage is unconfirmed, not proven impossible. Current adapter sends no source order identifier, and queue still emits one sale at project completion. Must verify provider-supported source order/line identity and remaining-quantity behavior before changing actual sales transmission. No real sales generated during investigation.

## Change 026 — 업무별 하위 메뉴와 독립 주소

2026-09-15 사용자 승인: 탭을 하위 메뉴로 재구성하고 명칭을 홈·프로젝트·생산계획·발주, 입고관리·생산·기준정보로 변경한다. 오산 디자인을 재사용한다. 기존 공통 desktop/mobile 하위 메뉴, OsanPageHeading, 오산 project theme를 사용한다. 기존 /interior-busbar 진입은 홈으로 유지하며 각 하위 페이지의 직접 진입·새로고침·뒤로/앞으로 이동을 지원한다. 계획에서 생산 목록으로 이동할 때 제품군·날짜 필터를 유지하고, 페이지 이동 시 이전 편집/사진/QR 팝업은 닫는다.

이번 범위는 FE 메뉴/주소/화면 구성이다. 부서별 입력 권한과 기준정보 소유권은 별도 미구현 상태이며 기존 서버 권한·생산·재고·출하·ERP 전송 계약은 변경하지 않는다. Ecount 문의2481394는 사용자 직접 접수 후 담당자 후속 답변 예정 안내만 확인했으며 원주문 연계 API는 아직 미확정이다. 실제 provider/DB mutation 없이 합성 브라우저 검증을 진행한다. 기존 task WIP 보존. 로컬5197은 이 worktree frontend source를 사용하는 PID4429로 확인했다.

Change026 검증: 기존 합성 busbar UI30/30 통과. 별도 주소·reload·history 신규 검증과6화면1440/1200/1101/390 검증2/2 통과. 재검증 중 모바일 resize 직후 helper가 오래된 layout을 읽는 race와 기존 inline Editor를 dialog로 오인한 assertion을 보정했으며 제품 동작 결함은 아니었다. 모바일 제목/설명과 새로고침 겹침은 scoped grid로 보정하고 합성 screenshot을 직접 열어 desktop 캘린더·홈과390 생산·입고 화면을 시각 확인했다. screenshot은 task 소유 /private/tmp/emi-busbar-*에만 보관. typecheck/build 성공, 변경 파일 eslint 오류/경고0, 전체 lint 기존 main.tsx warning1(추가 warning 제거), diff check 통과. 초기 격리5196 listen sandbox 제한은 해당 테스트 실행 승인을 통해 해소했다. 독립 busbar_navigation_review에서 route/history/filter/modal/print/access boundary 차단 finding 없음. 실제 운영 자료/전표 변경 없음. 사용자 검수·원격 반영·merge·배포는 미실행이며 기존5097 backend는 재기동하지 않았다.


## Change 027 — 원주문 연계 분할 출하 판매 API

2026-09-15 사용자 구현 승인. 발주 상세 문의는 사용자가 접수했으며 별도 답변 대기. 판매 문의2481394의09:15:42 공식 답변은 주문 일자·번호를 판매 API에 전달하면 원주문 연결 및 미판매 잔량 계산이 가능하다고 확인했다. 답변 REL_DATA 표기는 기존 공식 예제 REL_DATE와 다르므로 wire는 예제 REL_DATE/REL_NO에 맞추고 실제 ERP 연결 검수는 별도 남긴다.

완료 조건: 신규 분할 출하마다 해당 수량·금액의 판매 대기1건, 확정 원주문 필수 연결, 출하 중복 요청/동시 claim/Unknown 자동 재전송 차단, 출하 취소에 따른 전송 보류 또는 전표 확인, 과거 합계 판매 자동 재발송/출하 backfill 없음.0097 additive migration과 출하별 상태 표시. 격리 합성 DB/provider 검증 후 독립 review. 현재 운영 전표/영속 DB/runtime 변경은 실행하지 않음.

Change027 구현 검증 완료: 관련 backend196/196(0skip), adapter 단독102/102, 화면3/3(ecount retry/reconcile/출하별 수량 및 전표), frontend tsc·변경 파일 eslint·build 및 diff check 통과. 테스트 최초 실행의 로컬 소켓 sandbox 제한은 해당 격리 테스트 실행 승인으로 해소했다. 추가 테스트의 xUnit 취소 토큰 경고3건을 보정 후196건 통과. 합성1440/390 화면 /private/tmp/emi-busbar-shipment-sales-{width}.png를 직접 확인했고 모바일은 기존 표 내부 가로 스크롤을 유지하며 페이지 overflow 없음. 기존 bundle 크기 경고 외 build 오류 없음.

작성과 분리된 partial_sale_review(요청 GPT-6 Astra High, 관측 모델 NOT_REPORTED)가 HEAD2a4f90a+Change027 allowlist WIP의 계약·diff·검증을 읽고 GO, 열린P0–P2없음으로 판정했다. 기존 project-level legacy 판매는 보존·확인 대상으로 두고 신규 출하로 역산하지 않는다. 원주문 없음/미확정/잘못된 전표번호, 다른 출하 Unknown·변경 미확인, 취소 출하는 전송하지 않는다. 정상 출하3건은20개씩 별도 판매로 기록하며 반복 요청은 같은 출하와 판매 대기를 사용한다.

로컬 runtime5097의0097 migration/재기동 및 실제 판매 호출은 미실행. 실제 ERP 원주문 연결/미판매잔량 검수, 사용자 검수, 원격 반영/merge/배포는 미완료. 영속 busbar_review_20260909 DB 백업·0097 적용·해당 backend 재기동의 구체적 실행 승인을 받은 뒤 적용한다. 새 코드는 Debug 검증 빌드했고 기존 Release runtime은 재시작하지 않았다. 현재 Chrome용 Aside 확장 연결은 끊겨 이번 턴 매뉴얼 재조회는 못 했으며 이전 턴 직접 확인한 공식 예제/고객지원 답변을 근거로 구현했다.

Change027 local application approved by user on2026-09-15 for backup/migration/restart and automatic real ERP testing through their project/shipment input. Verified oldPID26846 source and listener5097, noPending/InFlight jobs (OrderSucceeded3), pausedfalse. Release build0warnings/0errors. Stopped only ownedPID26846; confirmed port closed; created nonempty custom backup /tmp/busbar-before0097-20260915T005517Z.dump inside emi-busbar-test-20260909. Official migrate-only applied0097,97verified. Before/after counts projects4/products65/shipments1 and OrderSucceeded3 preserved, no historicalSale jobs backfilled. Existing launcher restarted ownedbackendPID93123 with existingProductionSUBIN.PARK configuration; seed/migrationstartup/purge/notification/publication disabled. Authenticated workspace and newecountstatus endpoint succeeded, transmissionEnabledtrue/environmentProduction/pausedfalse/connectionMessagenull; frontend5197/projectsHTTP200. No new ERP slip created by agent. User will enter newproject+shipments to verify actual linked sales and ERP remaining quantity. No remote/merge/operational PMS deployment.

## Change 028 — 판매 품목 적요에 프로젝트 도착지

2026-09-15 사용자 수정·로컬 재시작 승인. 로그인된 이카운트 공식 판매 API 매뉴얼에서 SaleList[].BulkDatas.REMARKS(적요, STRING200)를 직접 확인했다. 새 출하 판매 snapshot에 `납품처: {프로젝트 도착지}`를 보존하고 판매 wire REMARKS로 전달한다. 주문 wire는 유지한다. 접두사를 포함한200자 초과는 전송 시도 전에 Held로 표시하며 임의 절삭하지 않는다. 이전 판매 snapshot/실제 전표는 수정·재전송하지 않는다. 원본 payload의 필드 유무를 기준으로 legacy 비교 양쪽에서 새 적요를 제외해 수동 Reviewed 이후에도 불필요한 확인 상태를 만들지 않는다.

관련 합성DB/fakeprovider 테스트139/139(0skip), Release build 경고0/오류0, diff check 통과. partial_sale_review 독립 검토에서 legacy Reviewed 비교 P2를 발견해 보정하고 해당 경로 테스트를 추가했으며 재검토 GO, 열린P0–P2없음. 200/201자 경계, 분할출하 적요 보존, 신규 판매 도착지 변경 감지, 기존 판매/주문 보존 검증 포함.

승인된 로컬5097 PID93123 소유/source와 InFlight0 확인 후 PID97507로 재시작. DB migration/리셋 없음. 기존 운영 연결 설정 유지, transmissionEnabled=true/environmentProduction/paused=false/connectionMessage=null. frontend5197/projects 및 인증 workspace HTTP200. 에이전트가 실제 전표를 새로 생성하지 않았으며 변경 후 실제 ERP 품목 적요 반영은 사용자 출하 검수 대기. 원격 반영·merge·운영 PMS 배포 없음.

## Change 029 — Team-owned inputs, LSE Task No, weekend colors

User approved implementation and clarified administration: busbar manager, Cheongju administrator and overall administrator manage master data, settings and stock corrections. Sales owns projects/shipments; production planning owns plans/purchases/receipts; manufacturing owns product worker/photos/publication retry. Administrators retain existing full access. Current department determines team ownership; a stale department role cannot grant it. Active, approved Cheongju boundaries remain. Server allowlist and workspace permissions govern API/UI together. Unknown mutation routes fail closed. LSE Task No replaces the customer job number display/template only; existing internal property and ERP U_MEMO2 (W/O No) mapping remain. Saturday blue/Sunday red preserve today and selected backgrounds. Synthetic API/UI validation and independent review in progress. No live identity/DB modifications or remote deployment.

Change029 validation: related backend208/208 passed (0skip), including real HTTP team allow/deny, shipment/receipt success, department transfer revocation, manufacturer two-photo completion, existing Cheongju/Osan/ReviewSafe boundaries. Initial empty multipart test returned an invalid-body error; changed fixture to an explicit invalid image to test the intended image-validation branch. Related UI5 tests passed after aligning assertions with optional label text and the read-only banner now shown on work pages. PC1440/mobile390 weekend screenshots and sales/manufacturing access screens inspected directly. FE typecheck/changed-file eslint/build passed, existing bundle-size warning only; BE Release0warnings/0errors. Independent review and local runtime application pending.

Independent partial_sale_review final GO, no open P0-P2. Confirmed no production-cancel UI; shipment/ledger reversals use administration access. Existing CancelProduct Store remains functional behind admin-only API; no new cancellation capability was added. Local owned backend PID97507/source/InFlight0 verified and restarted as PID12849 with existing DB/provider configuration, no migration/reset/identity changes. Authenticated admin workspace exposes all five permissions true, frontend5197/plans HTTP200; ERP production transmission enabled/pausedfalse/connectionMessagenull. No agent-created ERP slip, push/merge/operational PMS deployment. User acceptance of this permission/UI change remains pending.

## Change 030 — Actionable home overview

User approved replacing cumulative home metrics with near-term operational status. Show outstanding projects due through KST today+7 (including all overdue); overdue red, today through+3 yellow. Family metrics: today's plan, actual manufacture today, unfinished earlier plans, current stock, near-term delivery remainder and positive net production need. Hide families with none of those signals. Materials compare latest BOM demand for today's/overdue unfinished plans against actual stock, including existing negative balances; do not count ordered-but-unreceived goods. Explicitly flag missing BOM and partial estimates. Today's actual production is aggregated server-side over all products, independent of product paging/filter, with server KST date. No stock writes, reservations or ERP changes. Synthetic validation in progress.

Change030 complete: Store tests10/10 with KST boundary/filter/page-independent manufacture counts; overview calculation tests2/2; browser2/2 (overdue/three-day/seven-day/completed/future boundaries, current quantities/material demand, desktop1440/mobile390) passed. Latest BOM/shared-material/negative-stock/missing-BOM/completion/replenishment assertions passed. Typecheck/eslint/build successful, existing bundle-size warning only; backend Release0warnings/0errors. Screenshots /private/tmp/emi-busbar-home-current-{1440,390}.png inspected; mobile tables retain internal horizontal scrolling without page overflow. Independent partial_sale_review GO, no P0-P2. Local owned5097 source/InFlight0 verified and restarted PID18208; authenticated workspace overview date and frontend home HTTP200 confirmed. No DB migration, stock writes, ERP slips, remote push/merge/deployment. User acceptance pending.

Change030 follow-up: user requested home section title '진행중인 프로젝트' and original request/shipped/remaining quantities together. Updated heading and table to show requested quantity, cumulative effective shipments and remaining delivery. Existing overdue/seven-day filter and colors preserved. Synthetic desktop1440/mobile390 UI passed, screenshots inspected; frontend-only local source update, no backend restart or data mutation required.

Change030 destination follow-up: home project table now includes destination immediately after project name. Synthetic PC/mobile home test passed and screenshots inspected. Read-only architecture confirmation: product/plan/photos/manufacture/QR/public token are linked, while shipment records project+quantity only, without per-product shipment membership. Current local launcher keeps publication disabled; previous single Azure page acceptance is distinct from continuous production publication. No product-to-project tracking implemented in this display change.


## Change 031 — Panel shipment trace and immutable evidence

User approved implementation: scan actual panels into each shipment, lock both photos at production completion, and replace expandable project rows with independent project detail pages including shipment/panel evidence. New shipments require unique completed panels of the project's family; selected count equals quantity, with transaction lock, active-link uniqueness, stock and request remainder checks. Shipment destination/name/LSE Task No are captured at shipment time; reversal preserves history and releases active links. Historical shipments remain explicitly unlinked. Worker correction remains separate. No public project/destination information, no QR address/number changes, no ERP purchase integration. Additive migration0098 and synthetic validation planned; persistent migration/runtime application and user acceptance tracked separately.

Change031 backend validation: related InteriorBusbar suite210/210 passed, no skips, after fixing the synthetic shipment fixture to seed a valid worker for completed panels. New migration/trace tests4/4 passed: existing quantity-only shipments/photos/stock preserved, no fabricated legacy links; QR/number lookup, snapshot persistence, replay/reversal/reshipment, competing projects and immutable photo Store/SQL paths. Additional real HTTP authorization tests22/22 passed: new detail/scan routes reject Osan before DB access, team shipment boundaries remain, manager photo replacement with a reason is rejected, detail returns linked panels/remaining quantity. Release build0warnings/0errors. Independent backend review GO, no P0-P2. Frontend/visual review pending; persistent UAT0097 and actual providers untouched.

Change031 frontend implementation: independent `/interior-busbar/projects/{id}` route reuses existing project table, dialog, commercial preview and ERP status components. Project/user key remount and response generation checks isolate navigation. Camera scanning lazy-loads ZXing; keyboard QR/product-number entry remains available. Selection count determines shipment quantity; first submitted payload/request ID are frozen across retries, and pending scan results cannot change it. Shipment snapshots show explicit missing legacy records rather than today's project values. Panel tables open only the selected front/back evidence images. Completed photo upload controls were removed; worker correction stays in production photo viewing.

Frontend validation: related busbar browser run37/38 passed initially; the sole stale home test expected the pre-Change030 table label. Updated that test to the approved current home and reran it successfully. Targeted UI5/5 passed including stable shipment retries, linked/legacy history, invalid/duplicate scan, Escape/focus restoration, actual QRCoder-generated QR decoded through the browser's ZXing camera path using a synthetic canvas stream, and worker correction. Final cold-read corrections fixed re-adding a removed code and immutable reversal-retry reason; each regression re-ran1/1 successfully. Final typecheck, changed-file ESLint and build passed; existing main bundle-size warning only. Parent inspected PC1440/mobile390 history and visible synthetic front/back photo screenshots at `/private/tmp/emi-busbar-project-{history,photos}-{1440,390}.png`; table scrolling stays inside the page. Parent independently reviewed Sol-authored frontend code and final fixes with no P0-P2; separate backend Astra review GO remains valid. Full user-device camera/label UAT and final candidate regression remain separate. No persistent migration, restart, real ERP slip, remote push, merge or deployment executed.

Final frontend review follow-up: independent reviewer identified two P2 findings (commercial-preview failure did not recover on unchanged-value refresh, and clickable rows lacked a named accessible detail action). Added a successful-load revision to refetch the preview and corrected recovery guidance; added a named 상세 보기 button in the 작업 column while preserving the user's plain project-name requirement and whole-row activation. Parent re-review confirmed both closed. Targeted action/re-add/reversal tests passed3/3; preview recovery passed1/1 using an availability gate to avoid React StrictMode request-count assumptions. Final typecheck/ESLint/build/diff-check passed. No open P0-P2. Initial implementation local commit f876578; this follow-up is source-only. Persistent0098 application and5097 restart await explicit approval; no real slips created.


Change031 local application: user explicitly approved backup, migration and restart on2026-09-15 (재시작하고 안내해). Verified owned PID18208/source and ERP InFlight0, stopped only that backend, and created/verified custom backup `/tmp/busbar-before0098-20260915T070024Z.dump` inside `emi-busbar-test-20260909` (restricted permissions; pg_restore list verified). Official migrate-only exited0; migration ledger count98. Before/after projects5/products65/shipments3/photos8 preserved; panel_links0, with no fabricated historical membership. Existing launcher restarted backend5097 as PID30290 with original Production SUBIN.PARK ERP configuration; transmissionEnabled=true/paused=false. Authenticated workspace and new project detail GET returned200; existing shipment has no linked panels as expected; frontend5197/projects returned200. Publication remains disabled as before. No agent-created ERP slip, remote push, merge or operational deployment. User can now perform panel scanning/shipment/evidence acceptance on local5197; physical camera/printed QR UAT and final candidate regression remain pending.

## Change 032 — Continuous one-panel shipment scanning

2026-09-16 user approved scan → number/photos confirmation → one-panel shipment immediately → next scan, with camera retained and one sales transmission per shipment. Replaced selection/batch submission with sequential confirmation in the existing dialog. Each success advances the session count and remaining/stock display, keeps the video stream alive, and gives optional sound/vibration. No per-panel workspace/ERP refresh blocks scanning; closing refreshes detail. One pending scan and synchronous submission lock prevent overlapping actions. Failed writes retain the exact panel/request ID; successful codes and product IDs are suppressed. Backend transaction, permission, immutable-photo and ERP queue contracts remain unchanged.

Validation: affected browser tests4/4 passed using synthetic API data only, including two sequential panel writes with distinct IDs, duplicate suppression, cancellation/re-scan, exact retry request, and real ZXing decoding of a synthetic QR video with live track retained after success. Scan mock omits photo flags to match actual endpoint; confirmation loads both photos using the validated Complete-panel contract. Typecheck/scoped ESLint/build/diff-check passed (existing bundle-size warning only). Parent directly inspected 1440/390 screenshots `/private/tmp/emi-busbar-quick-scan-{1440,390}.png`; compact two-photo layout and large confirmation button visible. Independent quick_scan_review (requested Astra/high) identified missing scan photo flags P1; corrected and reviewer closed it with GO/no open P0-P2. Local Vite5197 serves the changed screen; no backend restart/migration/provider call needed, no agent-created real shipment/slip. User physical-device latency/camera/sound/vibration verification remains pending; remote/merge/deployment not performed.

Change032 discoverability follow-up: user approved always showing the project-detail QR shipment button. It is disabled with an adjacent accessible reason in priority order: no shipment permission, completed delivery, no finished stock. Existing server/permission contracts unchanged. Synthetic UI test covering all three states passed1/1; typecheck/scoped ESLint/diff-check passed. Directly inspected 1440/390 screenshots `/private/tmp/emi-busbar-shipment-disabled-{1440,390}.png`; button and reason visible. Frontend-only local source update; no backend restart, data writes or provider calls.

## Change 033 — Busbar-only UI UX Pro Max styling

2026-09-16 user authorized applying installed ui-ux-pro-max only to Interior Busbar and removing Osan theme inheritance. App theme condition now retains only actual Osan list/detail routes. Busbar uses the shared neutral DsPageHeader instead of OsanPageHeading. Skill search for manufacturing produced an unsuitable marketing pattern; narrowed enterprise operational dashboard style search matched data-dense operational UI. Applied scoped screen-only busbar palette, surfaces, controls, filters, typography, focus and responsive spacing; retained system fonts, current six menus, semantic date colors, all workflows and QR print styles. No global wireframe/theme files edited. Scoped important declarations are needed because the existing global wireframe normalizes all descendants with important rules.

Validation: affected UI10/10 passed; final theme/6-menu/QR/weekend subset4/4 passed after cascade corrections; selected calendar descendant color regression1/1 passed. Six menus checked at1440/1200/1101/390 for no page overflow, absence of Osan theme and actual dedicated background color. Parent visually inspected synthetic home, projects, production, purchases, masters, scanner and calendar screenshots under task-owned /private/tmp/emi-busbar-*. Final selected-calendar image confirms readable nested text. Typecheck, scoped ESLint, build and diff-check passed; pre-existing chunk-size warning only. Independent quick_scan_review closed selected-day white-text P2 after scoped descendant reset and returned GO; no open P0-P2. Frontend-only local source update; no provider calls, DB changes, backend restart, remote push/merge/deployment. User visual acceptance pending.

## Change 034 — Project Excel actions and plan-month totals

User requested project Excel actions beside project registration, two expandable download/upload actions, product-family monthly planned/produced totals beside the month filter, and consistent colors/control sizes. Project ImportBox now exposes an expandable button and hidden file input; selected file uses existing preview API and opens existing BusbarDialog with errors/new-update rows and explicit apply. Purchase import remains in its existing details section. Monthly totals sum plans in the selected month and selected family, with completed units from those plans (explicitly labeled selected-month plan basis, not actual manufacture-month aggregation). Existing calendar behavior and APIs unchanged. Dedicated busbar styles align controls, emphasize enabled primary actions, improve table headers and adapt totals/menu at390px.

Validation: synthetic browser2/2 passed including six menus across1440/1200/1101/390, Excel file chooser→preview modal, month totals60/1 then0/0. Directly viewed `/private/tmp/emi-busbar-{excel-menu,month-totals}-{1440,390}.png`; no page overflow. Typecheck/scoped ESLint/build/diff-check passed; existing bundle warning only. No real upload/data/provider call, backend restart or deployment. Local frontend source updated; physical/user acceptance pending.

Change033 theme rollback (2026-09-16): user rejected dedicated visual direction and requested restoring Osan common theme. Restored the exact pre-theme App condition, OsanPageHeading and base busbar CSS from75ff344; removed experimental palette/control sizing. Preserved Change034 Excel menu/monthly plan totals and their responsive structural layout, plus selected-day readability correction. No feature/data rollback. Related browser3/3 passed: six menus1440/1200/1101/390 with Osan attribute/heading, calendar filtering/readability, Excel preview and monthly totals. Typecheck/diff-check passed. Parent viewed restored desktop home/mobile plans screenshots. Local frontend update only; no backend/provider/DB changes or remote deployment. Installed skill left untouched; its experimental theme is no longer applied.

Change034 compact-plan-toolbar follow-up: user approved shrinking monthly quantity cards so desktop family/month/totals/previous-current-next controls share one row. Reduced totals padding/type/gaps and capped the totals scroll area; desktop uses four columns, narrow views stack responsively. Browser2/2 passed, including explicit desktop month-input/action baseline alignment and six menus across four widths. Parent inspected1440/390 monthly-total screenshots. Diff-check passed; CSS-only behavior change, no data/provider/runtime changes.

### Change 025 follow-up — employee name live API probe (2026-09-16)

User explicitly requested testing EMP_CD with 정호민, 박수빈, 김기태 against actual Ecount sales input. Sent each literal name once using existing Production SUBIN.PARK credentials, existing customer/warehouse/product/common project codes, quantity1 and zero price/supply/VAT; remarks identify the name API test and state it is not an actual delivery. No PMS project/stock/queue writes and no source-order linkage were made. Each response returned SuccessCnt1/FailCnt0/IsSuccess=true: 정호민=20260916-1, 박수빈=20260916-2, 김기태=20260916-3. No retries/deletions. These are real ERP sales records and may affect ERP inventory despite zero amount. Thus the manual's code-only wording does not justify claiming literal names are rejected. Actual displayed employee association remains to be checked: previous logged-in browser tab no longer exists, and newly opened official login page requires web login. Existing PMS mapping implementation unchanged; no remote/deployment changes. Restricted local probe/result artifacts are outside git under /private/tmp/busbar-employee-name-probe* and contain no stored authentication response.

### Change 035 — PMS registrant name as Ecount employee

User approved removing manual employee-code mapping and automatically sending the PMS original project registrant name for Order/Sale. New attempts use the stored registered_by_name as EMP_CD (internal employeeCode payload key retained for historical compatibility); require a captured creator and nonblank name of at most30 characters without control characters. UI explains automatic naming, removes mapping editor/list; obsolete mapping write endpoint/request/authorization entry/audit registry entry removed. Existing mapping table/audits and frozen attempts remain untouched; no migration. New snapshot employeeSource marker lets old code-based snapshots ignore only the representation change during comparisons; captured name and actual business changes still require review. No real ERP slips created during this implementation.

Validation: related Ecount/authorization/store tests176/176 passed,0skipped on dedicated disposable busbar_test schemas with fake provider; names go unchanged to both API request kinds, profile rename/project edits retain original creator, invalid names hold, old mapped Order/Sale snapshots remain unchanged without spurious review and subsequent sales use names. Typecheck/changed-file eslint passed. Mock browser1/1 passed; PC1440/mobile390 screenshots directly inspected. Independent employee_name_review found legacy comparison regression, fixed and re-reviewed approved with no remaining findings. Initial sandbox socket restrictions were retried with approved execution permissions. User acceptance/remote/deployment remain separate; local runtime application recorded below.

Local application complete: verified ownedPID30290 source and Pending/InFlight0, restarted only5097 using existing approved launcher to PID61165. Frontend5197 and authenticated workspace5097 bothHTTP200; workspace no longer includes ecountEmployees. FE production build passed (existing bundle-size warning). Existing Production configuration remains enabled and public publication disabled; no DB migration or new ERP write initiated. Remote push/merge/deployment not performed.

### Change 036 — project detail information and shipment actions

User approved removing the project list action column, moving project editing into detail, simplifying base information and monetary preview, placing Ecount below shipment history, and renaming per-shipment reversal to 출하 정정. Implemented with existing Osan common theme: row navigation retained, detail project-edit action reuses a shared existing editor specification and server projects permission, plain borderless base information plus only family unit price/supply amount, separate shipment quantity summary retained. Each shipment keeps its own reversal button; dialog identifies date/quantity, requires reason, explains cancellation/restoration, and retains original endpoint/idempotency/administration permission. No backend business-policy/DB/provider changes.

Verification: related mock UI7 distinct scenarios passed (project list/detail PC1440/mobile390, preview retry, commercial read-only values/registration, project edit payload, linked-panel history/photos, shipment reversal retry, department permissions); focused final runs5/5 and4/4 passed with overlap. Typecheck and changed-file eslint passed; UI screenshots directly inspected after removing the remaining toolbar border. Local5197 uses current frontend source; no backend restart or real data input needed. User acceptance, remote reflection and deployment are pending/separate.

Production frontend build passed with existing bundle-size warning; no new build errors.

### Change 037 — designated master-data access and removal of correction-history screen

User approved removal of the purchases correction-history screen (not deletion of immutable ledger/audit records) and per-user master-data view/edit grants managed within masters. Explicit clarification: Cheongju system administrators and overall administrators always access/manage; all others including busbar managers require explicit Read/Edit assignment. Preserve team workflow permissions and administration permissions for stock/ERP controls. Add0099 grant table, server access checks, manager-only grant administration with reason/audit, hidden masters navigation/direct-route denial, active approved local user selection. Implementation and isolated validation in progress; runtime migration/backup to be assessed after tests, no provider data creation in scope.

Change037 validation complete: API/auth/audit tests30/30 passed,0skip, including admin user-list shape, overall-admin capability, explicit Read/Edit/revoke, busbar-manager denial, no self-grant, inactive authentication401, Osan/ReviewSafe/team boundaries and mutation registry. First expectation wrongly expected403 for an inactive account; corrected to the earlier authentication401 rejection. Dedicated synthetic fixture applies0099; separate transaction-scoped upgrade probe preserved existing sentinel and inserted valid grant, then rolled back all probe objects. UI four scenarios passed (six workspaces, team input permissions, automatic employee name, designated masters None/Read/Edit/Admin and grant save); combined role scenario initially exceeded30s during contention, rerun with60s limit passed in4.3s. PC1440/mobile390 screenshots directly inspected. Typecheck/eslint and FE build passed (existing bundle-size warning); BE API/test project Release build succeeded as part of tests, separate whole-solution build hit execution limit without compiler diagnostics and is not recorded as passed. Independent master_access_review GO with no P0-P2; requested user-list/overall checks added and passed.

Awaiting explicit local application response: requested backup of busbar_review_20260909, additive0099 migration, restart owned5097 preserving current provider settings and business data. No persistent DB migration/user grant/runtime restart has been performed for this change. Frontend source5197 updates live; current old backend lacks new access endpoints until approved application. No provider requests or remote push/merge/deployment. Local implementation commit follows.

Change037 local application approved by user (“승인”) and completed2026-09-16. Verified owned61165 source and no Pending/InFlight jobs. Stopped only5097; created and pg_restore-listed verified backup `/tmp/busbar-before0099-20260916T073358Z.dump` inside `emi-busbar-test-20260909`. Existing migrate-only launcher (all providers disabled during migration) applied0099; ledger entry verified and new grant table empty. Before/after project/product/shipment/operation/ledger/audit/ERP-attempt counts and full stock snapshot equal. Restarted with unchanged approved configuration, PID11521. Authenticated dev-admin access/masters/master-access200, all three master permissions true,11 selectable users with1 automatic administrator; dev-sales master read/edit/manage false and direct masters/grant-list403. Frontend5197HTTP200. No individual grants or real ERP slips created. Local applied; user acceptance pending. No remote push/merge/operational deployment.

### Change 038 — compact master-access summary and immediate popup toggles

User requested only authorized people on the masters page, with the full list in a permission-edit popup and immediate click/re-click assignment/revocation. Replaced inline all-user editing with a compact granted-user summary, administrator-only 권한 설정 수정 dialog, searchable scrollable user list, and separate 조회/수정 checkboxes. Edit includes read; removing edit retains read, removing read removes both. Automatic administrators remain checked and immutable. Existing server endpoints, authorization and audit preservation remain unchanged; each toggle supplies an action-specific audit reason. Synchronous mutation locking and recovery reads prevent duplicate/uncertain writes from presenting stale permissions; failed recovery disables controls until reload.

Validation: focused mock browser scenarios2/2 passed, covering None/Read/Edit/Admin and100 synthetic users, search, immediate grant/revoke, reload persistence and committed-write/error recovery. After scoped CSS correction,100-user scenario reran1/1 with viewport-wide backdrop and mobile list horizontal-fit assertions. Typecheck, changed-file ESLint, production frontend build and diff-check passed (existing bundle-size warning only). Parent directly inspected desktop1440/mobile390 summary and popup screenshots. Independent grant_toggle_review found backdrop width applied to the wrapper; corrected to inner dialog, added regression assertion, and reviewer confirmed no remaining actionable findings. Local frontend5197 updates live; no backend restart, migration, actual user-grant mutation or provider write performed. User acceptance and remote/deployment remain separate.

### Change 039 — remove nested decorative borders within busbar menus

User approved the menu-by-menu border audit and requested all identified cleanup. Scoped CSS to busbar pages/dialogs: preserve outer work-area surfaces and input/button/calendar boundaries, remove inner toolbar/filter/month-summary frames, flatten nested import details and dialog editors, and separate shipment records with horizontal lines. Empty-list messages inside a surface are borderless; error/permission states retain their distinctions. Osan shared styling and other menus remain unchanged. No behavior, authorization, API, database or provider changes.

Validation: existing six-workspace scenario passed at1440/1200/1101/390 with no horizontal page overflow or page errors; linked/legacy shipment detail and immutable photo dialog passed; immediate permission popup passed; Excel/month totals passed on focused rerun after a transient viewport-transition alignment assertion failed in the combined run. Desktop and390 screenshots directly inspected for filters, masters, production/calendar and shipment separators. Final empty-state cascade correction is included in the final six-workspace rerun. CSS-only change; no new implementation-mirroring tests or backend restart needed. Local5197 serves current source; user acceptance and remote/deployment remain separate.

### Change 040 — purchase page cleanup and receipt balance

User approved removal of the leftover ledger pagination, addition of a 발주·입고 현황 heading and 미입고 잔여 column, and movement of purchase Excel actions beside registration. Purchase page no longer displays ledgerCount or ledger paging; production pagination remains. Receipt balance displays max(0, ordered minus received). Reused the existing project Excel toggle/download/upload/preview dialog for purchases with purchase-specific labels and unchanged preview/apply endpoints. Removed the separate purchase import surface, reducing the vertical gap and extra block.

Validation: typecheck, changed-file ESLint and diff-check passed. Focused synthetic browser tests2/2 passed for project Excel compatibility and purchase30/received20/remaining10, hidden stale8-ledger pagination, co-located Excel actions, upload preview and close. Desktop1440/mobile390 screenshots directly inspected, no horizontal page overflow. Only synthetic upload used; no real purchase/receipt/import/provider mutation. Local5197 source applied without backend restart; remote/deployment and user acceptance remain separate.

### Combined Osan and interior busbar release - 2026-09-16
User explicitly approved merging and deploying both changes together. Integration base: production main7cbfbc8, Osan18dff87, busbarb5654f8. Isolated branch codex/osan-busbar-release preserves both existing review runtimes and unrelated WIP. App retains both Osan notification styling and busbar shared theme; lockfile retains jsQR and ZXing.
Only unpublished busbar migrations0090-0099 are renumbered0106-0115. Existing production migrations and Osan0104/0105 remain intact. New0116 adds central audit capture for mutable busbar relations; classification/menu/latest-version tests updated. Original busbar review database and historical ledger are untouched. CI gets an explicitly disposable busbar PostgreSQL service so database tests execute instead of skipping.
Integration validation ongoing. Independent reviewer spawn and existing reviewer resume both rejected with agent thread limit; integration independent review remains incomplete. Direct verification is not independent review. No main merge or production deployment yet. No database reset, local review-data import or artificial ERP transaction is authorized/needed. Production provider readiness is checked separately. Final whole regression belongs to integrated PR CI.

### Change 041 — mobile workflow, QR lifecycle and operational error fixes (2026-09-18)

User approved implementation after calendar and production/photo prototypes: mobile calendar ends at month grid, date opens plans dialog; mobile production list and front/back photo preview/save. Remove Projects 관리 header text. Preserve completed evidence; cancel untouched panels with deleted plans and hide cancelled panels in default production list. Reuse Osan image handling/QR printing and protect against prior metadata/type/color/scanner regressions.
QR contract: generate at plan/panel creation, print from Production. Before shipment the same QR enters authenticated PMS panel production/product information. Shipment scanner checks eligibility. After shipment the same static-storage URL serves self-contained HTML with embedded photos: no PMS URL, login, API, DB lookup, remote images or runtime dependency. Earlier public waiting QR mock is withdrawn. Existing printed URLs remain valid. Record publish failures explicitly and do not report successful shipment before independent publication succeeds.
Operational fixes: additive menu allowlist migration for repeated SiteAccess P0001; bounded fail-closed scanner transport recovery. No production DB writes, alert suppression, paid resources, remote merge or deployment in this implementation authorization. Related isolated tests and independent review required; user runtime acceptance remains separate.
Baseline d31c0f8 main / local c43d21c. Branch codex/busbar-mobile-qr-lifecycle. Prototypes /private/tmp/emi-busbar-calendar-design/{index.html,production.html}; both user-approved. Reference/source-first design rules copied into current AGENTS.

Implementation details (local candidate): 0120 preserves existing panel numbers/tokens and printed URLs, assigns numbers at creation, queues existing routing pages, and stores frozen shipment HTML separately from product lists. Shipment requires independent HTML publication; a separately committed recovery intent survives partial writes/DB rollback and the existing worker restores the committed lifecycle under the same mutation lock. This is recoverable external delivery, not a distributed atomic transaction. A failed/ambiguous write can briefly leave a prepared page before the worker restores the pre-shipment route; shipment/inventory/ERP records remain rolled back. Successful shipment pages contain embedded images only and do not call PMS. Reversing shipment requeues the authenticated pre-shipment route. Later worker-name corrections do not rewrite the frozen shipped snapshot.
Operational rollout: publication requires HTTPS `InteriorBusbar:Publication:PmsBaseUrl` or existing `Frontend:Origin`, plus the existing separate Azure static-storage settings. Existing rows are queued once by 0120; verify publication backlog before label handover. No new paid resources are introduced. ClamAV reconnect is bounded to one retry and remains fail-closed; actual server-side reset cause (restart/resource pressure/connection termination) remains unproven and needs post-deployment observation. The missing-department-head workflow configuration is not silently assigned by this change.
Regression additions: planned QR before photos, same panel number/URL preservation, waiting-panel worker correction and reprint, lost storage acknowledgement rollback/recovery/retry, shipped snapshot independence/freeze, original HEIC-to-browser preview without saving, original-byte scan before sanitation, fragmented/reset TCP scanner responses, existing plan deletion/stock/shipment races, and desktop/mobile overflow/preview/print checks. Prototype approval is complete; product user acceptance and production rollout are separate.

Backend evidence: `/private/tmp/emi-busbar-final-tests.log` ran 243 related tests against explicitly disposable `busbar_test` on127.0.0.1:55490 with fake providers:241 passed,2 outdated assertions failed (planned number expected null; scanner count omitted newly added preview). Those expectations were corrected to the approved creation timing and original-byte preview scan; `/private/tmp/emi-busbar-focused-final.log` then passed21/21, including both failures, lifecycle2, scanner4 and audit13. No skips in these executed scopes. Earlier interrupted runs exposed missing exact preview audit registration (fixed) and per-schema connection-pool accumulation (test fixture now disables pooling); those are not counted as passes. Additive0119 upgrade with full0120 catalog also passed1/1 in isolated tmpfs PostgreSQL via the repository test script. This is focused development verification, not final required CI or whole-product regression.
Independent GPT-6 High code review: GO, no open findings after R1 mobile panel-switch stale draft isolation, R2 Draft worker correction publication revision, and R3 zoom focus/Escape containment fixes. Successful QR iframe print evidence was requested and included in the frontend regression. Review did not run live providers or change data.

Frontend final evidence: 19/19 unit tests across3 files; calendar/weekend2/2 and photo/successful QR iframe print2/2 isolated mock browser tests passed. Typecheck, targeted ESLint and production frontend build passed (existing large-chunk warning only). Final390 calendar directly inspected with circular selected date, Saturday blue/Sunday red and plan dot;1440 photo workspace directly inspected. Stable synthetic screenshots: `/private/tmp/emi-busbar-ui-evidence/`. Product-switch/HEIC cancellation, zoom focus/Escape, and three separate50mm print sheets are covered. No live API/provider or shared runtime was used.
Implementation and focused verification complete, prepared for scoped local commit on codex/busbar-mobile-qr-lifecycle. User product acceptance, physical phone/camera/label-printer checks, final required CI, operational alert observation, migration rollout and remote merge/deployment remain pending. No production configuration/data changes or new paid resources were made.

### Change 042 — desktop workspace visual refresh (2026-09-18)

User requested a reference-led redesign of all interior busbar PC screens. Scope: overview, projects/detail, plans, purchases/receipts, production/photos, masters and their existing dialogs. Existing approved mobile design, business actions, permissions, APIs and QR print sizing remain unchanged. Applied a separate screen-only stylesheet from861px, scoped to busbar containers. Neutral canvas and white work areas, consistent page titles/actions, compact field and button sizes, highlighted registration actions, soft table headings/row rhythm, calendar cell hierarchy, desktop photo areas, detail quantity summary and dialog spacing. Reuses all existing components and handlers; no new workflow or database changes.
References actually consulted: UI/UX guide — IBM Carbon data table usage https://carbondesignsystem.com/components/data-table/usage/ (title/toolbar/table hierarchy, dense rows, clear actions); operating website — GitHub microsoft/vscode Issues https://github.com/microsoft/vscode/issues (opened actual live public listing in browser, search/filter/action placement and restrained row boundaries); supplementary product documentation — Linear Display options https://linear.app/docs/display-options (existing list/view controls). ui-ux-pro-max used as supporting guidance; marketing-layout search recommendations were not applied to this operational UI. No external assets or fonts imported.
Verification:7 focused mocked browser scenarios passed (six menus at1440/1200/1101/390, project detail/history/photos, calendar, photo workspace, successful QR iframe print, masters permission popup, purchase Excel). After PC alignment corrections,3 affected scenarios passed again. Read-only live isolated preview checked all6 menu URLs with no page errors/overflow;900px project layout checked separately with zero page overflow. Desktop calendar, projects, masters, photo dialog, shipment detail and390 mobile screens directly inspected. Typecheck, targeted ESLint, frontend production build and diff-check passed; existing bundle-size warning remains. Independent reviewer found no confirmed findings in desktop scope; final narrow cosmetic adjustments directly verified. Screenshots under /private/tmp/busbar-pc-design and /private/tmp/busbar-pc-test-results.
Local5251 shows this source; isolated5098 API and busbar_preview_clean_20260918 contain synthetic preview records with external providers disabled. Existing review/production DBs not modified by this design change. User visual acceptance pending; no push/merge/public deployment.

Change042 user correction: removed direct delete action and empty action column from active project list; detail deletion and deleted-list restoration remain available. Removed desktop calendar-internal overrides so original date/cell/plan quantity presentation is reused, while the refreshed page heading/filter/surface remains. No API/data changes. Related mocked browser tests3/3 passed (PC/mobile delete-detail and restore flows, calendar); typecheck and diff-check passed. Updated live5251 project/calendar screenshots directly inspected; local only, no deployment.

Release authorization 2026-09-18: user requested all current changes merged into remote main and publicly deployed. Integration branch codex/busbar-production-release starts from d31c0f8; the previous squash baseline exactly matches7ce4f19, and replaying approved commits produced a tree identical to226b777. PR150 final CI initially found6 stale mock-browser expectations (hidden duplicate mobile/desktop labels, pre-preview upload flow, old selection colors and hidden mobile month input). Updated those tests to exercise the approved visible controls, planned number preservation and explicit preview/save; affected6/6 passed locally. Product code unchanged. Existing14-day Azure DB recovery retention and production static-publication configuration checked; no DB reset or new resources. Required CI, merge and production rollout still in progress.
Final CI35313602969: frontend and full-stack passed; backend933/934 passed, only exact stored QR PNG decoding returned null for a random payload. Deterministic diagnostic200 URLs with the actual p/{32-byte-hex-token}.html shape and unchanged QRCoder ECC-Q/8px found4 general finder-detection misses; direct pure-symbol decoding succeeded200/200. Exact-PNG test now uses PureBarcode while retaining expected payload, artifact preservation, publication gating and inventory atomicity assertions. Isolated QR tests2/2 passed,0 skipped. Independent review found no issue with that test scope; this is not physical camera/printed-label recognition evidence. Product QR generation unchanged. Required CI rerun pending.

Production release completed 2026-09-18: PR150 https://github.com/emisubin/emi-qms/pull/150 squash-merged to main6ccaa5f7173cc610f12e0b3cf934521a547ed8d5 after CI35316698600 passed all required jobs (backend934/934; mock browser70; full-stack64; isolated access1 and Osan registration1). Integrated source tree matched the verified candidate exactly. Azure workflow35320608789 succeeded with existing environment approvals, additive migrations0119/0120 (migration-k4vdvo7 Succeeded), backend--0000061 and frontend--0000051 Healthy/latestReady. Public health200, anonymous root/API401. Immediate backend console sample102 lines had no failure/unhandled-exception or publication-failure markers. No reset/drop, review-data import, artificial ERP transaction, role bootstrap, membership backfill, or new resource creation.
Limits: current interactive Azure identity lacks Blob Data Reader, so internal publication backlog/static blob inventory was not directly verified and no key/role workaround was used. Immediate logs are a short observation window, not proof that historical alert causes are eliminated. Physical phone/camera and label-printer acceptance remain separate from automated original-PNG/print-template tests. Release result mirrored to PR150; this local documentation follow-up does not change deployed product code. Existing5251 synthetic preview and unrelated WIP preserved.

### Change 043 — label tracking and Busbar usability corrections (2026-09-21, design review)

User approved recommended output-confirmation and attachment-confirmation workflow, plus production-plan date in lists, QR preview and30/50mm labels. Preserve panel numbers/URLs, existing evidence, campus permissions, and post-shipment standalone publication. Existing panels begin as verification-required rather than unprinted; reprint retains attachment state and records history. Browser printing is not proof of physical success. Label actions must have actor/time and persist per panel when implemented.
Scope also approved: remove decorative editor left accents, mobile horizontal-table/plan-popup usability, shared mobile header layout, readable active/disabled controls, remove evidence grayscale at preview/saved/zoom, desktop alignment/photo spacing, correct empty-state and pre-production QR text. Audit confirmed live photo CSS grayscale; stored original color has not been directly verified. Active mobile photo-save text has a stylesheet priority conflict; no production upload done for reproduction.
New UI prototype /private/tmp/emi-busbar-calendar-design/qr-labels.html served5250, synthetic only. Reuses production table/cards; adds plan-date/label status and batch confirmation dialogs. Reference guide Carbon data table usage; operating reference GitHub microsoft/vscode Issues, both opened2026-09-21. PC1440/mobile390 visual checks, no mobile page overflow,30mm label box/content check, multi-date output and explicit output/attachment confirmation flows checked.50mm option available. User visual confirmation pending; physical scan/print, backend implementation, product regression and rollout not completed. No production mutation or deployment in this phase.
Change043 design extension: user requested same-account mobile app-entry prompt for output-confirmed/unattached labels, grouped by plan date, partial completion and defer until next entry. Numeric panel input must accept1/0001/fullIB ID and exact numeric matching in attachment and shipping lookup; retain actual shipment eligibility checks. Updated synthetic prototype with entry/lookup flows;390 visual inspection and partial3→2 pending behavior checked, accountB fixture excluded, invalid/not-found inputs and production-incomplete shipping blocked in prototype. No real camera/printing/shipping or persistent cross-device implementation yet. User visual review pending.

Change043 implementation approval and result (2026-09-21): user approved the final entry/label prototype and requested the next implementation stage. Implemented in the existing codex/busbar-production-release worktree based on48ffb62; unrelated original-checkout WIP and5250 prototype/5251 preview preserved. No new remote/deployment authorization used.

Additive0121 records per-panel label state, output/attachment actor/time and immutable operation events with actor/action/payload-bound request replay. Existing panels are LegacyUnknown; new panels Unprinted. Explicit output confirmation requires current printable publication and stored QR; it never interprets print-dialog closure as physical success. Reprint retains Attached and original QR/number. Production permission gates printing and label mutations consistently. Pending reads use the signed-in account's output history and exclude attached/cancelled/shipped panels. Mobile entry prompts group by plan date, support partial attachment/defer and refresh after app backgrounding for at least60seconds or a fresh load; ordinary route changes do not reopen. Max200 selected per save with explicit limit feedback. Exact numeric normalization is shared by attachment and shipment lookup; shipment business checks remain.

Production desktop/mobile rows and authenticated panel dialog show plan date;30/50mm labels contain panel number, family and plan date inside the existing square. Added label filters/history, QR/number attachment lookup and camera lifecycle cleanup. Existing mobile tables render labelled rows inside viewport, including plan-day actions. Removed editor left accents, corrected mobile header sizing, photo grayscale and dark-save text contrast; compacted desktop completion banner, expanded month totals, aligned production filters and clarified filtered-empty/early-print text. Stored-photo pipeline itself was not recolored or replaced.

Verification: backend27 related tests passed, authorization33 passed, catalog/schema classification2 passed. Includes original stored JPEG/PNG/HEIC/WebP chroma, ReviewSafe423/no writes, migration preservation, shared-lock atomicity/concurrent replay, cross-account request rejection, numeric exact matching and shipped exclusion. Frontend24 tests across4 files passed (9 new label cases, existing photo/overview); typecheck, changed-file ESLint and frontend build passed (existing large-bundle warning only). Logs /private/tmp/emi-busbar-label-final.log; migration latest expectations updated to0121, existing120 audit-trigger count preserved.

Review split by author because a third reviewer spawn/resume hit agent-thread limit: backend author reviewed parent-authored frontend; parent reviewed agent-authored backend transaction/authorization/migration/lookup contracts. Frontend findings fixed: stale pending on resume, >200 default selection, inconsistent print/write authorization, and false selected message at limit. Reviewer confirmed all closed. No independent third-person whole-change review is claimed.

Direct CUA verification on isolated5253/5105 and newly copied synthetic DB busbar_labels_synthetic_20260921:390 mobile and1440 desktop, existing status/plan date,1/0001 numeric lookup, actual attachment persistence, same-account2-panel entry prompt and partial2→1 remaining, defer,30mm113.38px/50mm188.97px preview dimensions, mobile day-plan actions without page horizontal overflow. Synthetic RGB PNG selected, saved through API, viewed and enlarged in color; computed filter none and active save white text onrgb(40,40,40). No browser error logs observed. QR preview uses reserved-domain synthetic fixture artifacts; test output records do not represent real printing. Public static publisher, ERP and notifications are disabled in this preview. Existing screenshots/prototype are not real scan/print evidence.

Remaining: user product acceptance, physical mobile camera and30/50mm label-printer check, final requiredCI/regression before a separately authorized merge/deploy. No productionDB/provider change, remote push/merge or public deployment performed forChange043.
