# 이 PMS에서 작업 시작하기

이 컴퓨터의 같은 프로젝트 폴더를 Codex에서 열면 된다. 계정을 바꿨다는 이유로 repository나 개발환경을 다시 만들지 않는다. 원격 접근·모델·연결 서비스 권한은 새 계정에서 별도로 달라질 수 있다.

## 채팅 설정과 지시

일상 코딩은 **GPT-6 / Medium**, 인증·사업부 권한·DB·배포 판단은 **High**로 시작한다. 더 높은 강도를 모든 작업에 고정할 필요는 없다. 설정은 [모델 안내](docs/development/codex-model-workflow.md)를 따른다.

예를 들어 “오산 진행 관리 Task를 이어서 구현해. 기존 계약을 지키고 관련 테스트와 화면 확인까지 해줘.”라고 지시하면 현재 상태를 확인한 뒤 필요한 구현·검증·보정까지 이어간다. 새 Task와 문서를 매번 먼저 만들거나 모델을 직접 번갈아 바꿀 필요는 없다.

기획만 원하면 “기획만 해줘”라고 범위를 말한다. 특정 모델만 원하면 모델·강도를 명시한다. 명확한 구현 지시는 그 범위의 작업과 완료 local commit을 포함한다. 병합·공개배포·실데이터 변경은 [Root의 명시 승인 경계](AGENTS.md)를 따른다.

## 처음 읽을 것

1. [Root AGENTS](AGENTS.md), 현재 branch·WIP와 해당 작업의 최신 Task/change.
2. 다음 작업을 고를 때 [Roadmap](docs/00-product-roadmap.md).
3. 수정하는 영역의 하위 AGENTS와 필요한 계약·검증 자료만 읽는다.

모든 과거 MD를 매번 읽거나 완료 기능을 재구현하지 않는다. [PMS 안내](docs/development/pms-project-guide.md)에서 업무/환경 자료를 찾을 수 있다.

## 실행과 결과 확인

기존 서버를 먼저 확인하고 소유·source·DB·provider를 파악한 뒤 해당 환경 SOP를 사용한다. 문서의 오래된 주소를 현재 실행 주소라고 안내하거나 기존 DB를 초기화하지 않는다.

결과는 바뀐 동작·검증 근거·남은 검수·Git/배포 상태로 받는다. 테스트 결과를 실제 사용자 검수로 대체하지 않는다. 새 프로젝트 설정·명령 규칙은 신뢰된 프로젝트의 새 Codex 실행에서 적용될 수 있으며 기존 대화에 자동 반영됐다고 가정하지 않는다.
