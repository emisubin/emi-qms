# TASK-EXPERIMENT-PROMOTION-001 Change 003 사용자 검수 체크리스트

상태: `자동 검증 완료 / Change 004 원격 main 병합 완료`

## Change 004 긴급 수정 확인

- [x] 실제 제조 표가 42행이고 마지막 현재 순번이 `42`, 영구 코드는 `P52`로 유지됨
- [x] 실제 품질 표가 42행이고 마지막 현재 순번이 `42`, 영구 코드는 `P52`로 유지됨
- [x] 실제 물류 표가 42행이고 마지막 현재 순번이 `42`, 영구 코드는 `P52`로 유지됨
- [x] 비연속 영구 번호를 가진 unit·격리 Full-Stack 회귀 통과
- [x] 사용자가 Change 004 구현과 commit·push·Ready PR·CI 확인 뒤 main 병합을 명시적으로 승인

## 검수 전 확인

- [ ] 화면이 통합 `main` 기준 Graphite 디자인인지 확인한다.
- [ ] Change 003 통합 후보 Frontend `http://127.0.0.1:5191`인지 확인한다. 검수 데이터·API는 기존 UL891 Backend `5082`를 재사용한다.
- [ ] 검수용 UL891 프로젝트의 기존 활성 패널이 42면으로 표시되는지 확인한다.

## UL891 설계

- [ ] 설계 화면에 사용자용 version·code 입력이 없고 `수정`과 `저장`으로 이해되는지 확인한다.
- [ ] 저장 뒤 같은 현재 설계를 다시 수정하고 저장할 수 있는지 확인한다.
- [ ] 같은 세트 안에 `ABCDCBF`처럼 반복되는 패널 사양을 입력·저장할 수 있는지 확인한다.
- [ ] 사양 값만 바꿔도 기존 물리 패널 identity와 진행 이력이 유지되는지 확인한다.
- [ ] 위치를 추가·삭제했을 때만 패널 생성·취소가 반영되고, 현재 화면에는 활성 위치·활성 패널만 보이는지 확인한다.
- [ ] 제조 탭에서도 42면이 취소 이력과 합쳐져 54면으로 늘어나지 않는지 확인한다.

## UL891 생산계획

- [ ] 한 프로젝트 기본계획을 저장하면 모든 활성 세트에 같은 계획이 입력되는지 확인한다.
- [ ] 이후 개별 세트 계획을 수정할 수 있고, 기본 적용을 다시 실행해도 값이 있는 세트는 기본적으로 보호되는지 확인한다.
- [ ] 명시적으로 덮어쓰기를 선택한 경우에만 기존 세트 계획이 바뀌는지 확인한다.
- [ ] 새 세트를 추가하면 프로젝트 기본계획을 상속하는지 확인한다.
- [ ] 계획 구조에서 필수 checkbox와 실적 연결 선택창이 한 행에 있고 checkbox 크기·정렬이 자연스러운지 확인한다.
- [ ] 이미 보이는 실적 연결을 선택한 상태에서 저장할 때 `실적데이터 하나를 선택` 오류가 다시 나오지 않는지 확인한다.

## 일정표

- [ ] 계획 막대는 흰색, 실적 막대는 검은색인지 확인한다.
- [ ] 날짜 헤더 영역에는 세로선이 없고 본문에만 날짜선이 있는지 확인한다.
- [ ] 굵은 주요 날짜선은 실선, 얇은 보조 날짜선은 점선인지 확인한다.
- [ ] 표의 위·아래·왼쪽·오른쪽과 왼쪽 항목 구분선은 일반 실선인지 확인한다.
- [ ] 첫 날짜선이 왼쪽 구분선과 겹치지 않고 마지막 날짜선이 오른쪽 외곽선을 넘지 않는지 확인한다.
- [ ] 일정표 아래에 생산관리가 입력한 담당자 목록이 보이는지 확인한다.
- [ ] PC와 390px 화면 모두 페이지 전체 가로 넘침 없이 읽고 조작할 수 있는지 확인한다.

## 검수 결과

- [ ] 위 항목 전체 통과
- [ ] 실패 항목의 화면·행동·기대 결과를 기록
- [ ] 통합 후보 게시 승인 여부를 별도로 결정

검수 완료는 commit·push·PR·merge, Persistent UAT migration·runtime handover, Azure image 재배포·traffic·provider 승인을 대신하지 않는다.

게시 결과: Ready PR #65 CI 3/3 성공, merge commit `79b90b8`. 이 결과는 Persistent UAT·Azure 변경 승인을 포함하지 않는다.
